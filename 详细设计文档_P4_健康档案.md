# 详细设计文档 P4：个人资料、设置与同步入口

> 当前版本：本地 profile/settings，不再是用户账号  
> 最后更新：2026-06-11  
> 文档用途：指导 Profile、Settings、WebDAV 配置、隐私与 secret 存储相关开发。

---

## 1. 目标

P4 只承担本地个人资料和应用设置，不承担账号系统职责。

保留：

- 昵称。
- 性别。
- 身高。
- 体重。
- 出生日期。
- 默认重量单位。
- WebDAV 同步设置。
- 上次同步时间。

移除：

- email 登录身份。
- 密码登录。
- JWT token。
- role/权限。
- 用户 API。
- 头像上传服务。

---

## 2. 相关文件

页面：

```text
src/pages/ProfilePage.tsx
src/pages/SyncPage.tsx
```

服务：

```text
src/services/profile.ts
src/sync/syncService.ts
src/sync/webdavClient.ts
```

数据：

```text
src/core/models.ts
src/repositories/localJsonRepository.ts
src/platform/documentStore.ts
```

---

## 3. ProfileDoc

```ts
interface ProfileDoc {
  id: string;
  nickname: string | null;
  avatarUrl: string | null;
  gender: string | null;
  height: number | null;
  weight: number | null;
  birthDate: string | null;
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null;
  schemaVersion: number;
}
```

默认值：

```json
{
  "id": "profile-local",
  "nickname": "训练者",
  "avatarUrl": null,
  "gender": null,
  "height": null,
  "weight": null,
  "birthDate": null
}
```

ProfilePage 兼容 `User` 类型展示，但这不是账号：

```ts
{
  id: profile.id,
  email: "local@ironlog",
  role: "local"
}
```

注意：

- email 只为兼容旧 UI 类型。
- 不得用 email 做身份或同步 key。

---

## 4. SettingsDoc

```ts
interface SettingsDoc {
  id: string;
  weightUnit: "kg" | "lb";
  webdav: {
    url: string;
    username: string;
    passwordRef: string | null;
  };
  lastSyncAt: string | null;
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null;
  schemaVersion: number;
}
```

规则：

- `settings.json` 不保存密码。
- `passwordRef` 指向 secret 存储。
- `lastSyncAt` 只在同步成功后更新。
- 推送到 WebDAV 的 `settings.json` 必须将 `passwordRef` 与 `lastSyncAt` 置为 `null`，避免同步本地 secret 引用和设备本地同步状态。

---

## 5. Secret 存储

Web 开发：

- IndexedDB 中使用 `secret:<key>` 保存。

Android：

- Capacitor Preferences 保存。

当前限制：

- Web fallback 不等同系统级安全存储。
- Android 后续可换更强 secure storage 插件。

禁止：

- 把密码写入 `settings.json`。
- 把密码写入 WebDAV 远端。
- 在日志中打印密码。

---

## 6. ProfilePage 行为

展示：

- 顶部显示昵称。
- 标识“本地单人版”。
- 身高/体重/性别摘要。
- 个人资料编辑卡片。
- 数据同步与备份入口。

保存：

```ts
updateProfile({
  nickname,
  gender,
  height,
  weight,
  birth_date
})
```

规则：

- 空字符串转 null。
- 数字字段 parseFloat。
- 保存成功 Toast。
- 保存失败 Toast。

---

## 7. SyncPage 行为

字段：

- WebDAV URL。
- 用户名。
- 密码。
- 上次同步时间。
- 同步状态。
- 冲突日志。

状态：

```ts
type SyncStatus = "unconfigured" | "syncing" | "success" | "failed" | "conflict";
```

按钮：

- 保存设置。
- 测试连接。
- 手动同步。

### 7.1 保存设置

流程：

1. 如果输入了新密码，生成或复用 passwordRef。
2. 将密码写入 secret。
3. 将 url、username、passwordRef 写入 settings。
4. 清空页面 password state。

### 7.2 测试连接

流程：

1. 先保存设置。
2. 创建 WebDavClient。
3. 确保配置 URL 下的 `ironlog-data/` 同步根目录存在。
4. 对该目录执行 PROPFIND。
5. 成功显示连接成功。
6. 失败显示错误。

### 7.3 手动同步

流程：

1. 先保存设置。
2. status = syncing。
3. 调用 `syncNow()`。
4. success/conflict/failed 更新 UI。
5. 成功后更新 lastSyncAt。

---

## 8. WebDAV 同步数据

上传文件：

```text
manifest.json
profile.json
settings.json
exercises.json
templates.json
workouts/YYYY-MM.json
```

注意：

- 训练月分片由 `manifest.json` 的 `shards` 列表发现；不再上传单一训练索引文件。
- 上传前 `settings.json` 必须将 `passwordRef` 与 `lastSyncAt` 置为 `null`，且绝不包含密码。
- 上传前写 backups。
- 上传使用 tmp + MOVE。
- Android 使用原生 OkHttp 插件发送 `MKCOL`、`MOVE`、`PROPFIND`，以支持 WebDAV 非标准 HTTP 方法。

---

## 9. 隐私原则

默认：

- 所有数据只在本地。
- 不配置 WebDAV 时不上传。
- 不联网也可完整训练。

用户主动同步后：

- 训练数据上传到用户自己的 WebDAV 目录。
- 密码不随 JSON 上传。

---

## 10. 边界 case

- 未配置 WebDAV，进入 SyncPage 显示未配置。
- 已保存 username/url 但无 passwordRef，仍视为未配置。
- 密码输入后保存，输入框清空。
- 测试连接失败，settings 仍保留但 status failed。
- 同步失败不影响本地数据。
- 冲突后 status conflict，并展示日志。
- 修改 profile 后 WebDAV 未配置，应用仍正常。

---

## 11. 禁止事项

- 不得恢复登录/注册页面。
- 不得恢复 JWT token。
- 不得把 ProfileDoc 当远端用户账号。
- 不得把 WebDAV 密码写入 JSON。
- 不得要求用户配置 WebDAV 才能使用应用。

---

## 12. 开发验收清单

- [ ] ProfilePage 无登录/退出概念。
- [ ] 个人资料可保存并刷新显示。
- [ ] SyncPage 未配置状态正确。
- [ ] 密码不写入 JSON。
- [ ] 测试连接失败有提示。
- [ ] WebDAV 未配置时训练、计划、日历全部可用。
- [ ] 同步成功更新 lastSyncAt。
- [ ] 冲突日志可显示。
