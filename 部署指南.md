# IronLog 部署指南

> 本文档描述 IronLog 从本地开发到线上发布的两阶段部署策略：
> - **阶段一（开发中）**：PWA 部署，快速上线供演示和日常使用
> - **阶段二（开发完毕）**：Capacitor 生成 APK，正式发布到 Android 设备
>
> 两种形态使用同一份代码，互不冲突，可长期并行维护。

---

## 整体架构

```
src/（前端源码）
    │
    ├── npm run build:pwa ──→ dist/ ──→ 上传服务器 ──→ 浏览器访问（PWA）
    │
    └── npm run build:apk ──→ dist/ ──→ npx cap sync ──→ Android Studio ──→ APK
                                              │
                                        android/（Capacitor 壳）
```

后端（FastAPI + PostgreSQL）是两种分发形态的共同依赖，部署一次即可。

---

## 前置准备：代码适配（一次性）

在进行任何部署之前，需要对前端代码做少量适配，使其同时支持 PWA 和 Capacitor 两种构建模式。

### 1. 修改 `frontend/src/services/api.ts`

将硬编码的 `baseURL` 改为读取环境变量，使 APK 打包时能指向公网后端：

```ts
// 修改前
const api = axios.create({
  baseURL: "/api/v1",
  ...
});

// 修改后
const api = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || "/api/v1",
  ...
});
```

同文件中 refresh token 的请求也需同步修改（第 25 行附近）：

```ts
// 修改前
const { data } = await axios.post("/api/v1/auth/refresh", { ... });

// 修改后
const { data } = await axios.post(
  `${import.meta.env.VITE_API_BASE_URL || "/api/v1"}/auth/refresh`,
  { ... }
);
```

### 2. 新建环境变量文件

**`frontend/.env.development`**（本地开发，不变）：
```
VITE_API_BASE_URL=
```

**`frontend/.env.pwa`**（PWA 生产构建）：
```
VITE_API_BASE_URL=https://你的域名/api/v1
```

**`frontend/.env.capacitor`**（APK 打包）：
```
VITE_API_BASE_URL=https://你的域名/api/v1
```

> 两个生产环境变量文件内容相同，分开命名是为了后续灵活扩展（如 PWA 和 APK 对应不同后端环境）。

### 3. 在 `frontend/package.json` 中添加构建脚本

```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:pwa": "tsc -b && vite build --mode pwa",
    "build:apk": "tsc -b && vite build --mode capacitor && npx cap sync",
    "preview": "vite preview"
  }
}
```

---

## 阶段一：PWA 部署

PWA（渐进式 Web 应用）让用户通过浏览器访问后点击"添加到主屏幕"，表现与原生 App 几乎相同：全屏启动、桌面图标、无浏览器地址栏。

### 步骤一：安装 PWA 插件

```bash
cd frontend
npm install -D vite-plugin-pwa
```

### 步骤二：修改 `frontend/vite.config.ts`

```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";
import path from "path";
import { VitePWA } from "vite-plugin-pwa";

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
    VitePWA({
      registerType: "autoUpdate",
      manifest: {
        name: "IronLog",
        short_name: "IronLog",
        description: "健身训练记录应用",
        theme_color: "#10b981",
        background_color: "#f8fafc",
        display: "standalone",
        start_url: "/",
        icons: [
          { src: "/icon-192.png", sizes: "192x192", type: "image/png" },
          { src: "/icon-512.png", sizes: "512x512", type: "image/png" },
        ],
      },
    }),
  ],
  resolve: {
    alias: { "@": path.resolve(__dirname, "./src") },
  },
  server: {
    proxy: {
      "/api": { target: "http://localhost:8000", changeOrigin: true },
    },
  },
});
```

### 步骤三：准备图标

将两张图标文件放入 `frontend/public/` 目录：
- `icon-192.png`（192×192 像素）
- `icon-512.png`（512×512 像素）

可用任意图片编辑工具（如 Figma）导出，主题色建议用 `#10b981`（emerald-500）。

### 步骤四：构建并部署

```bash
cd frontend
npm run build:pwa
```

生成 `frontend/dist/` 目录，将其上传到服务器（见"后端与服务器部署"章节）。

### 步骤五：Android 安装方式

1. 用 Chrome 浏览器访问 `https://你的域名`
2. 点击右上角菜单（三个点）→ **"添加到主屏幕"**
3. 桌面出现 IronLog 图标，点击即可全屏启动

> **提示**：如果弹出"安装应用"横幅，直接点击即可，无需进入菜单。

---

## 阶段二：Capacitor APK

将 Web 应用打包为真正的 `.apk` 文件，可通过数据线安装或分发给他人。

### 前提条件

| 工具 | 说明 | 下载地址 |
|-----|------|---------|
| Android Studio | 含 Android SDK，用于构建 APK | https://developer.android.com/studio |
| JDK 17+ | Android Studio 通常自带 | — |
| ADB | Android Debug Bridge，随 Android Studio 安装 | — |

安装 Android Studio 后，确保 `ANDROID_HOME` 和 `adb` 已加入系统 PATH。

### 步骤一：安装 Capacitor

```bash
cd frontend
npm install @capacitor/core @capacitor/cli @capacitor/android
```

### 步骤二：初始化（仅首次）

```bash
npx cap init "IronLog" "com.yourname.ironlog" --web-dir=dist
npx cap add android
```

执行后会在 `frontend/` 下生成 `android/` 目录，包含完整的 Android 项目结构。该目录需要提交到 Git。

生成的 `capacitor.config.ts` 确认内容如下：

```ts
import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.yourname.ironlog",
  appName: "IronLog",
  webDir: "dist",
};

export default config;
```

### 步骤三：处理 Android 网络安全配置

**如果后端使用 HTTPS，跳过此步骤。**

如果后端是 HTTP（如局域网 IP），Android 9+ 默认拦截明文请求，需要添加白名单：

新建 `frontend/android/app/src/main/res/xml/network_security_config.xml`：

```xml
<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">你的IP或域名</domain>
    </domain-config>
</network-security-config>
```

在 `frontend/android/app/src/main/AndroidManifest.xml` 的 `<application>` 标签中添加：

```xml
android:networkSecurityConfig="@xml/network_security_config"
```

> **建议直接使用 HTTPS，一劳永逸。**

### 步骤四：构建并同步

```bash
cd frontend
npm run build:apk
```

此命令等价于：
```bash
tsc -b && vite build --mode capacitor && npx cap sync
```

`npx cap sync` 会将 `dist/` 的内容拷贝进 `android/` 项目。

### 步骤五：Android Studio 构建 APK

```bash
npx cap open android
```

在 Android Studio 中：
1. 等待 Gradle 同步完成
2. 菜单 **Build → Build Bundle(s) / APK(s) → Build APK(s)**
3. 构建完成后点击右下角"locate"定位 APK 文件

APK 默认输出路径：
```
frontend/android/app/build/outputs/apk/debug/app-debug.apk
```

### 步骤六：安装到手机

**方式一：ADB（需开启 USB 调试）**

```bash
adb install frontend/android/app/build/outputs/apk/debug/app-debug.apk
```

**方式二：文件传输**

将 `app-debug.apk` 传输到手机，在文件管理器中点击安装（需在"设置 → 安全"中开启"允许安装未知来源应用"）。

---

## 后端与服务器部署

PWA 和 APK 都需要一个可公网访问的后端服务器。

### 服务器选型建议

| 场景 | 方案 | 成本 |
|-----|------|-----|
| 手机与电脑同一 WiFi，仅个人使用 | 后端跑在本机，`VITE_API_BASE_URL=http://192.168.x.x:8000/api/v1` | 免费（但离家不可用） |
| 随时随地可用 | 腾讯云/阿里云轻量服务器（2核2G） | 约 60 元/月 |
| 免费公网（可接受冷启动） | Railway 或 Render 免费层 | 免费 |

### Docker Compose 一键部署（推荐）

在服务器上创建以下文件结构：

```
~/ironlog/
├── docker-compose.yml
├── nginx.conf
├── dist/              ← 前端构建产物，上传至此
└── backend/           ← 后端代码
```

**`docker-compose.yml`**：

```yaml
version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ironlog
      POSTGRES_PASSWORD: 改为强密码
      POSTGRES_DB: ironlog
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

  backend:
    build: ./backend
    environment:
      DATABASE_URL: postgresql+asyncpg://ironlog:改为强密码@db/ironlog
      SECRET_KEY: 改为随机长字符串
    depends_on:
      - db
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./dist:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  pgdata:
```

**`nginx.conf`**：

```nginx
server {
    listen 80;
    server_name 你的域名或IP;
    root /usr/share/nginx/html;
    index index.html;

    # API 反向代理
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # React Router 支持（所有路径回退到 index.html）
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

**启动服务**：

```bash
docker-compose up -d
```

**执行数据库迁移**：

```bash
docker-compose exec backend alembic upgrade head
```

### HTTPS 配置（推荐）

使用 Let's Encrypt 免费证书，需要一个域名：

```bash
# 服务器上安装 certbot
apt install certbot python3-certbot-nginx -y
certbot --nginx -d 你的域名
```

certbot 会自动修改 nginx 配置并续期证书。

---

## 日常迭代流程

### 更新前端（PWA）

```bash
cd frontend
npm run build:pwa
scp -r dist/* user@服务器IP:~/ironlog/dist/
# nginx 会自动生效，无需重启
```

### 更新前端（APK）

```bash
cd frontend
npm run build:apk        # 构建 + cap sync
npx cap open android     # Android Studio 重新构建 APK
```

### 更新后端

```bash
# 服务器上
cd ~/ironlog
git pull
docker-compose restart backend
# 如有新迁移
docker-compose exec backend alembic upgrade head
```

---

## 两种形态并行维护对照表

| 维度 | PWA | Capacitor APK |
|-----|-----|--------------|
| **构建命令** | `npm run build:pwa` | `npm run build:apk` |
| **分发方式** | 浏览器访问 → 添加到主屏幕 | 传输 `.apk` → 安装 |
| **更新方式** | 部署新 `dist/` 即可，用户自动获取 | 重新构建 APK，重新安装 |
| **离线支持** | Service Worker 缓存（有限） | WebView 内置，同 PWA |
| **原生能力** | 受浏览器限制 | 可通过 Capacitor 插件扩展 |
| **开发工具** | 任意浏览器 DevTools | Android Studio + Chrome DevTools（`chrome://inspect`） |
| **代码改动** | 无（在 PWA 适配完成后） | 无（同一份 dist/） |

---

## 常见问题

### Q: APK 安装后 API 请求全部失败

检查 `frontend/.env.capacitor` 中的 `VITE_API_BASE_URL` 是否指向可公网访问的地址。手机不能访问 `localhost`。

### Q: Chrome 没有弹出"添加到主屏幕"提示

确认：① 网站已通过 HTTPS 访问；② `manifest.json` 中 `icons` 包含 192×192 和 512×512 两种尺寸；③ Service Worker 已成功注册（DevTools → Application → Service Workers）。

### Q: `npx cap sync` 报错找不到 `dist/`

先执行 `npm run build` 生成 `dist/` 目录，再执行 `npx cap sync`。

### Q: Android Studio Gradle 构建失败

检查 JDK 版本（需要 17+），以及 `ANDROID_HOME` 环境变量是否正确指向 Android SDK 目录。

### Q: 如何在 APK 中使用 Chrome DevTools 调试

手机开启 USB 调试并连接电脑，在 Chrome 地址栏输入 `chrome://inspect`，找到设备中的 WebView 进程点击 inspect 即可。
