# IronLog 构建与发布指南

> 当前版本不部署服务器，只构建本地 Android-first 应用  
> 最后更新：2026-06-11

## 1. 部署策略变化

旧版本的部署目标是：

- FastAPI 后端。
- PostgreSQL 数据库。
- Nginx/反向代理。
- 服务器长期运行。

当前版本的发布目标是：

- 构建 Web 静态产物。
- 同步到 Capacitor Android 工程。
- 使用 Android Studio 或 Gradle 构建 APK。
- WebDAV 由用户自行配置为备份目标。

因此，单人版不需要服务器部署文档。

## 2. 构建检查

```bash
cd ironlog/frontend
npm run build
npm test
npm run android:sync
```

必须通过：

- TypeScript build。
- Vite build。
- Vitest。
- Capacitor sync。

## 3. Android Debug APK

```bash
cd ironlog/frontend/android
./gradlew assembleDebug
```

Windows：

```powershell
.\gradlew.bat assembleDebug
```

产物位置通常为：

```text
ironlog/frontend/android/app/build/outputs/apk/debug/app-debug.apk
```

## 4. Android Studio 构建

```bash
cd ironlog/frontend
npm run android:open
```

在 Android Studio 中：

1. 等待 Gradle sync 完成。
2. 选择设备或模拟器。
3. 点击 Run。
4. 需要 APK 时使用 Build APK。

## 5. 版本发布前检查

发布前执行：

```bash
cd ironlog/frontend
npm run build
npm test
npm run android:sync
```

再执行 grep：

```bash
rg -n "window\.(confirm|alert|prompt)" src
rg -n "blue-|gray-" src
rg -n "axios|auth|token|access_token|refresh_token|/api|services/api|Bearer" src vite.config.ts package.json
```

期望结果：

- 原生弹窗零残留。
- 主题旧色类零残留。
- 旧 HTTP/auth/JWT 运行路径零残留。

## 6. WebDAV 发布说明

应用不内置 WebDAV 服务端。用户可以使用任意支持基础 WebDAV 方法的服务：

- PROPFIND
- GET
- PUT
- MOVE
- DELETE
- MKCOL

同步目录建议单独创建：

```text
webdav-root/ironlog-data/
```

应用会上传：

```text
manifest.json
profile.json
settings.json
exercises.json
templates.json
workouts/index.json
backups/
```

## 7. 已知发布风险

### Gradle 下载

命令行构建首次运行会下载 Gradle wrapper 分发包。若无法访问 `services.gradle.org`，构建会失败在下载阶段。

处理方式：

- 使用可访问网络。
- 配置 Gradle 代理。
- 手动准备 Gradle wrapper 缓存。

### WebDAV 兼容性

不同服务商对 MOVE/ETag/LOCK 支持不一致。

当前实现：

- 使用 tmp 上传 + MOVE 发布。
- 不强依赖 LOCK。
- 记录冲突日志。
- 上传前写 backups。

### 数据迁移

发布新版本前，新增字段必须进入 schema migration，不能只改页面默认值。
