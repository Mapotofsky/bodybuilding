# IronLog 发布与交付指南

> 适用版本：Android-first 本地单人版
> 当前目标：发布应用名为“IronLog录铁”的 Android 内部测试版；也可交付静态 Web 构建产物。不部署 FastAPI、PostgreSQL、Docker、Nginx 或服务器。

---

## 1. 发布模型

IronLog 当前有两种交付形态：

| 形态 | 产物 | 适用场景 | 数据位置 |
|---|---|---|---|
| Android APK | `app-debug.apk` 或签名 release APK | 日常单人使用、WebDAV 原生同步 | Capacitor Filesystem |
| 静态 Web | `frontend/dist/` | 开发预览、功能演示 | 浏览器 IndexedDB |

静态 Web 不等同在线 SaaS：它没有业务后端，也不能替代 Android 对 WebDAV 非标准方法的原生支持。对外主交付应以 Android APK 为准。

## 2. 发布前检查

```powershell
cd D:\workspaces\vscodeWorkspace\project\bodybuilding\ironlog\frontend
npm ci
npm run build
npm test
npm run android:sync
```

还应人工走查：

- 无 WebDAV 配置时可创建、编辑、复制和删除训练。
- 模板训练的 URL 入口和页内选择器都能真正过滤动作。
- WebDAV 测试连接、手动同步和冲突日志可显示。
- Android 真机可访问 HTTPS WebDAV 服务。
- `settings.json` 远端副本不含密码引用与最后同步时间。

在发布说明中必须明确当前限制：LWW 冲突策略、无自动备份清理、Preferences 尚非 Keystore 级安全存储、没有用户可操作的导入/导出 UI。

## 3. 构建 Android APK

### 3.1 开发版 APK

```powershell
cd D:\workspaces\vscodeWorkspace\project\bodybuilding\ironlog\frontend
npm run android:sync

cd android
.\gradlew.bat assembleDebug
```

输出通常在：

```text
android/app/build/outputs/apk/debug/app-debug.apk
```

debug APK 仅用于测试或内部体验，不应作为正式可信分发包。

### 3.2 签名 release APK

当前仓库未固化 release 签名密钥。首次正式发布时，在 Android Studio 中执行：

1. 打开 `frontend/android`。
2. 选择 **Build -> Generate Signed Bundle / APK**。
3. 创建或选择 keystore，生成 APK。
4. 将 keystore 和密码保存在仓库外的受控位置。
5. 不提交 keystore、密钥密码或 `key.properties` 到 Git。

若需要 CI 发布，再新增不含密钥的签名配置模板和环境变量说明；在此之前不要伪造可复现 release 命令。

### 3.3 安装与验证

```powershell
adb install -r app-debug.apk
```

或将 APK 传到设备后手动安装。首次安装后，至少完成一次离线训练和一次 WebDAV 同步测试。

## 4. 静态 Web 预览

```powershell
cd D:\workspaces\vscodeWorkspace\project\bodybuilding\ironlog\frontend
npm run build
npm run preview
```

`dist/` 可以部署到任意静态托管。静态站点不需要 API 反向代理、数据库环境变量或服务器密钥。若在浏览器中配置 WebDAV，服务器仍需正确允许相应 CORS/HTTP 方法；因此不能把 Web 预览当成 Android 同步兼容性的验证替代品。

## 5. 数据与版本兼容性

### 5.1 本地数据

本地数据在 `ironlog-data/` 逻辑目录中按 JSON 分片保存。升级应用前，若数据重要，先进行一次成功的 WebDAV 同步。卸载应用、清除应用数据、清除浏览器站点数据都会删除本地副本。

### 5.2 schemaVersion

每次新增或变更持久化字段时，发布说明必须记录数据兼容性、迁移和回滚限制，并完成相应验证；具体模型、migration 和测试规则见 P2。

### 5.3 WebDAV 备份

每次同步前，应用会把远端已有分片复制到 `backups/`。该行为是同步失败时的恢复线索，不是完整的用户级备份/恢复产品；发布前不要承诺“一键恢复”能力。

主题、动作库 Tab 与动作详情已进入当前发布能力；主题语义映射已完成，但内部测试前仍须按 P4 完成 Android 真机视觉验收。AI 仍为规划能力，不能写入当前发布说明；未来 AI 未配置或调用失败时，现有离线能力必须保持可用。

### 5.4 内部测试版发布元数据方案（规划）

当前 Web 包版本为 `0.1.0`，Android 为 `versionCode 1` / `versionName 1.0`，口径不一致。内部测试版采用以下方案，实施前不得把它描述为已完成：

1. 产品展示名统一为“IronLog录铁”；`applicationId` 暂时保持 `app.ironlog.local`，避免在内部测试阶段引入数据目录迁移。
2. 语义版本以根发布版本为权威。首个内部测试版建议使用 `0.1.0-internal.1`；同一功能版本后续构建递增 `internal.N`。
3. Android `versionCode` 必须单调递增且不可复用；首个内部测试包保留 `1`，之后每次分发依次使用 `2`、`3`。`versionName` 与发布版本一致。
4. `package.json` 保存基础版本 `0.1.0`；发布时由一个受版本控制的元数据文件或固定脚本同时校验/写入 Web 展示版本、Android `versionName` 和 `versionCode`，避免手工修改多处。脚本实现前，发布检查必须人工核对三处值。
5. 升级到正式 `1.0.0` 时继续递增现有 `versionCode`，不得从 1 重新计数；发布说明记录上一内部版本、数据 schemaVersion 和最低兼容版本。

验收：构建前检查展示名、applicationId、versionName、versionCode；安装覆盖上一内部包并确认本地数据保留；在 APK 元数据中复核最终值。

### 5.5 WebDAV 密码迁移到 Android Keystore 的方案（规划）

内部测试版继续使用当前 Capacitor Preferences 存储，并在测试说明中明确它不是 Keystore 级加密存储；仅允许 HTTPS WebDAV，建议使用权限受限的专用账户。正式对外版本前按以下路径迁移：

1. 在 platform 层新增 `SecretStore` 接口，保持 `readSecret`、`writeSecret`、`removeSecret` 语义稳定；service、repository 和同步层不得直接依赖具体插件。
2. Android 实现使用 Keystore 保护的密钥加密密码正文，或采用经过维护、明确使用 Android Keystore 的 Capacitor 安全存储插件；Web 开发环境保留独立实现，并明确其安全等级较低。
3. 首次读取时若 Keystore 中没有目标 secret，但旧 Preferences 中存在，则写入安全存储、读回校验成功后再删除旧值。迁移失败时保留旧值并提示用户重新输入，不得清空同步端点或训练数据。
4. `passwordRef` 继续只作为本机引用；密码正文、密文、Keystore alias 和可用于解密的材料均不得进入 `settings.json`、manifest、WebDAV 分片、backup 或日志。
5. 清除同步配置同时删除对应安全存储项；卸载、系统凭据失效或设备迁移导致无法解密时，要求用户重新输入密码，不尝试静默恢复。

验收至少覆盖：新安装写入/读取/删除、旧 Preferences 原地迁移、迁移中断重试、错误密码与凭据失效、清除同步配置、日志/远端 JSON 零秘密、升级后既有训练与同步端点非秘密字段保留。完成前不得宣称“系统安全存储”“硬件加密”或“端到端加密”。

## 6. 发布清单

- [ ] `npm ci`、`npm run build`、`npm test`、`npm run android:sync` 成功。
- [ ] Android Studio/Gradle 成功生成目标 APK，或环境失败原因已记录。
- [ ] 在至少一台真机完成离线训练、模板训练、复制训练和同步测试。
- [ ] 检查 APK 包名为 `app.ironlog.local`，版本号策略已确认。
- [ ] 应用展示名为“IronLog录铁”，Web/Android 版本元数据与本次内部测试发布说明一致。
- [ ] release APK 已签名，keystore 未进入 Git。
- [ ] 文档未把旧 FastAPI/PostgreSQL/社区功能描述为当前依赖。
- [ ] 发布说明列出同步限制与数据备份建议。
