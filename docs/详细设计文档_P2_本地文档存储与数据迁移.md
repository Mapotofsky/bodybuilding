# P2 详细设计文档：本地文档存储与数据迁移

> 对应概要设计：M3 本地存储、迁移与同步中的本地持久化部分；M5 主题设置；M6 AI 非敏感配置
> 状态：`0.1.0-internal.2` 的 v5 已成为兼容基线；v6 曾在开发构建中保存旧爬楼机枚举，本轮升级 v7 并显式兼容 v5/v6
> 前置依赖：概要设计的数据不变量；P0/P1 的领域文档模型。

---

## 1. 设计目标与边界

本模块在无服务端环境中提供训练、动作、计划、模板、资料和设置的本地真源。它解决三个问题：

1. 同一业务代码在浏览器开发和 Android 上可读写相同 JSON 逻辑文件。
2. JSON 以静态文件与训练月分片保存，不退化为单一巨型文件。
3. 文档格式变化可通过 schemaVersion 归一化，而不是要求页面兼容所有旧字段。

本模块不负责：WebDAV 网络 IO、同步冲突合并、用户可操作的导入/导出 UI、Android Keystore 加密细节。它提供 `DocumentStore`、`SecretStore` 和 `LocalJsonRepository` 的平台边界；Android 加密与升级迁移流程由 P3 定义。

---

## 2. 文件、快照和 manifest

### 2.1 逻辑文件

| 路径 | 内容 | 更新来源 |
|---|---|---|
| `manifest.json` | 应用标识、schema、设备 ID、分片列表 | 任意 repository mutation 后重建。 |
| `profile.json` | ProfileDoc | 更新本地资料。 |
| `settings.json` | SettingsDoc | 更新可同步偏好，不保存 WebDAV 端点配置。 |
| `exercises.json` | ExerciseDoc[] | 内置/自定义动作、tombstone 与替代重定向。 |
| `templates.json` | `{ plans, templates }` | 计划、模板变更。 |
| `body-metrics.json` | BodyMetricDoc[] | P6 身体指标稀疏测量记录，已实现。 |
| `timeline-notes.json` | TimelineNoteDoc[] | P6 日期/时间段背景备注，已实现。 |
| `workouts/YYYY-MM.json` | WorkoutDoc[] | 该月训练创建、编辑、删除、复制。 |
| `exercise-performance/YYYY-MM.json` | ExercisePerformanceRecordDoc[] | P7 动作成绩刷新事件，已实现。 |
| `assets/avatar/*` | 头像资源文件 | ProfilePage 选择、替换或清除头像。 |

`DataSnapshot.workouts` 是运行时聚合数组。训练的磁盘真源只允许是月分片；不得新增全量 `workouts.json` 或另一份训练索引文件。

### 2.2 IronLogManifest

```ts
interface IronLogManifest {
  app: "ironlog";
  schemaVersion: number;
  deviceId: string;
  updatedAt: string;
  shards: Array<{
    path: string;
    updatedAt: string;
    etag?: string | null;
  }>;
}
```

`buildShardList(snapshot)` 输出六个静态分片、实际存在的训练月分片、动作成绩月分片以及头像资源文件。某月只有 tombstone 训练或成绩事件时，该分片仍必须列出并同步；否则另一设备可能恢复已删除记录。

### 2.3 分片规则

```ts
workoutShardPath("2026-06-22") === "workouts/2026-06.json"
isWorkoutShardPath(path) === /^workouts\/\d{4}-\d{2}\.json$/
```

日期不合法不能用于构造分片路径。当前 service 依赖页面传入 `YYYY-MM-DD`；后续若开放导入，导入层必须在写入前校验日期。

---

## 3. DocumentStore 平台接口

定义位置：`frontend/src/platform/documentStore.ts`。

```ts
interface SecretStore {
  readSecret(key: string): Promise<string | null>;
  writeSecret(key: string, value: string): Promise<void>;
  removeSecret(key: string): Promise<void>;
}

interface DocumentStore extends SecretStore {
  load(): Promise<Partial<DataSnapshot> | null>;
  save(snapshot: DataSnapshot): Promise<void>;
  readSyncEndpoint(): Promise<SyncEndpointConfig>;
  writeSyncEndpoint(config: SyncEndpointConfig): Promise<void>;
  clearSyncEndpoint(): Promise<void>;
  exportFiles(snapshot: DataSnapshot): Record<string, unknown>;
  importFiles(files: Record<string, unknown>): Partial<DataSnapshot>;
}
```

### 3.1 浏览器实现：IndexedDbDocumentStore

- 使用一个 IndexedDB object store 保存逻辑路径到 JSON 对象的映射。
- manifest 是读取入口；先读 manifest，再读静态文件和其中列出的训练分片。
- 浏览器 secret 当前也保存在 IndexedDB 的独立 key 前缀下，只用于开发兼容，不代表安全凭据库。
- 清理站点数据或隐私模式结束会删除这些数据。

### 3.2 Android 实现：CapacitorDocumentStore

- 使用 Capacitor Filesystem `Directory.Data` 下的 `ironlog-data/`。
- 保存时先写所有非 manifest 分片，再写 manifest，最后删除旧 manifest 中已不存在的训练月分片。
- 只有 Capacitor Filesystem 明确返回“不存在”错误码时读取才返回 `null`，供旧版本缺少静态分片时由 migration 补齐；权限、I/O 等其他读取错误必须携带逻辑路径显式阻断。文件存在但 JSON 无法解析时同样阻断，不得伪装成缺失。manifest 已引用的训练、成绩或资源分片若缺失，IndexedDB 与 Capacitor Filesystem 都必须阻断加载和后续写回，避免把无法读取的记录当作空集合删除。
- Preferences 保存 URL、用户名和 `passwordRef` 等本机端点配置，并只作为旧版密码迁移源。新密码通过 Android `SecretStore` 写入 Keystore 保护的加密存储；密码、密文和解密材料均不进入 DocumentStore JSON。

### 3.3 平台选择

`createDocumentStore()` 仅在 `window.Capacitor.isNativePlatform()` 为真时选择 Capacitor 实现，否则选 IndexedDB。SecretStore 同样由 platform 层选择：Android 为 Keystore 加密实现，Web 为 IndexedDB 开发实现。业务模块不得自行分支判断平台。

---

### 3.4 主题与规划中的 AI 设置

当前 SettingsDoc 包含 weightUnit、themeId、lastSyncAt。WebDAV url、username、passwordRef 是本机-only 同步端点配置，通过 DocumentStore 本地配置接口保存；password 明文只能通过 DocumentStore secret 保存。它们不得进入 `settings.json`、manifest、远端 JSON 分片或 backup。themeId 使用稳定 string ID，默认 emerald-slate；未知值由 UI 回退但 migration 和同步不得改写原值。AI 仅保存非敏感 provider/model/能力配置、同意版本及 apiKeyRef；API key 明文只能通过 DocumentStore secret 保存。

`0.1.0-internal.2` 已建立内部测试兼容基线。新增持久化字段必须递增 schemaVersion，添加显式 migration、读取后写回、DocumentStore export/import 与 WebDAV 往返测试；不得假定已有快照为空，也不得用清空或覆盖代替升级。

### 3.5 动作详情元数据

`ExerciseDoc` 的完整领域字段、受控记录方式、重量口径、计数口径、成绩方向、竞速指标、器械、分段 description、肌群和 provenance 只由 P1 第 2.3 节定义。P2 不再重复一份不完整接口，也不把上游 `category/body_part/target/muscle_group` 当作存储模型。

本节只定义持久化边界：`DataSnapshot.exercises` 整体写入 `exercises.json`；IndexedDB、Capacitor Filesystem、DocumentStore export/import 与 WebDAV 必须原样保存所有 `ExerciseDoc` 字段。`description` 仍是一个 `string | null`，其中的 `\n`/`\n\n` 必须原样往返；不增加平行的步骤字段。`provenance` 位于同一文档内但不经过页面 DTO，不能因为 UI 未展示而在 mapper、repository 更新或同步往返中丢失。Media 字段和非中文 instructions 不进入 `DataSnapshot` 或构建产物。

v5 是 `0.1.0-internal.2` 的兼容基线。v6 曾在开发构建中把爬楼机保存为 `recordingMode=step_count_duration`、`rateMetric=steps_per_time`，部分或全部动作/训练快照缺少 `contextKind`；不能因为 manifest 已标 v6 就跳过迁移。v7 将这两个旧枚举显式映射为 `reps_duration/reps_per_time`，缺失上下文补 `none`，并继续兼容 v5 的 `contextKind/contextValue` 补齐。历史训练不按当前目录重解释、不补造阻力档位；自定义动作、未知动作、嵌套 ID、未展示字段和 tombstone 保留。椭圆机、固定自行车、爬楼机等已知内置动作按稳定 ID 融合当前受控目录字段。阻力动作的旧派生成绩写 tombstone，WorkoutDoc 原始事实不删除。

生成目录采用确定性两段式：`candidates.md` 只保存 IronLog 与固定上游记录的映射及生成所需字段；内容评价、上游 `instruction_steps.zh`、人工纠错和未来交接规则保存在版本化内容候选文档。生成文件只输出不含 BaseDoc 元字段的 `DefaultExerciseSeed[]`，六项记录配置逐条来自映射，不得按器械或名称推断；`defaultData.ts` 使用固定 seedTime 补齐元字段。

### 3.6 头像资源

`ProfileDoc.avatarUrl` 保存头像资源路径引用，例如 `assets/avatar/profile-local.txt`，不保存 base64 正文。头像正文由 DocumentStore 作为资源文件保存；Web 端和 Android 端均通过同一 service 读取为 data URL 预览。ProfilePage 选择或替换头像时先写入资源文件，再把 profile 引用改为该路径；清除头像时将 `avatarUrl` 置为 null 并删除旧资源。

头像资源必须进入 manifest shards，并随 P3 同步流程 tmp/MOVE 发布。远端已有头像资源在覆盖前写入 `backups/`；清除头像后，下一次同步根据新 manifest 删除远端过期头像资源。profile 与资源文件的边界是：profile 只保存引用，资源正文不进入 `profile.json`、`settings.json` 或训练分片。当前工程先实现 Web 文件选择和本地资源接口；若 Android WebView 的文件选择能力受设备限制，后续再增加 Capacitor 原生选择器适配。

### 3.7 规划中的批准计划批量导入

当前 createPlan、createTemplate、createExercise 是独立 mutation，不能满足 AI 导入的原子性要求。P2 只定义并实现 repository 批量提交原语：在 snapshot 副本上应用已验证的变更、一次保存、成功后替换正式 snapshot，失败则保持正式 snapshot 不变。候选 JSON 校验、未知动作决议、预览、导入业务顺序和回滚规则由 P5 权威定义。

### 3.8 持久化链路矩阵

本节记录跨层持久化状态，不取代 P0/P1/P6/P7 的领域字段权威。P6/P7 和动作目录当前字段均已接入 core model、service、repository、migration、DocumentStore 和 SyncService。后续新增、删除或重命名持久化字段仍必须先更新本矩阵，再修改代码。

| 字段/文档 | 页面展示 | 页面编辑 | service/core | mapper/repository/migration | WebDAV 往返 | 状态 |
|---|---|---|---|---|---|---|
| `ExerciseDoc.category/recordingMode` | P1 列表、详情、选择器 | 共享表单受控单选 | P1 定义分类和每动作唯一记录方式 | mapper/repository 保留；v5/v6→v7 按稳定 ID 融合内置项，并将 `step_count_duration` 显式映射为 `reps_duration` | `exercises.json` | 已实现 |
| `ExerciseDoc.loadBasis/countBasis/loadDirection/rateMetric` | P1 详情显示重量口径、计数口径与成绩摘要；每侧输入明确标注 | 共享表单编辑“整组填写 / 每侧填写” | core 注册表校验完整组合；器械和动作名称不参与推断 | mapper/repository/DocumentStore 原样保留；v6→v7 将 `steps_per_time` 显式映射为 `reps_per_time` | `exercises.json` 与 WebDAV 静态分片 | 已实现 |
| `ExerciseDoc/WorkoutExerciseDoc.contextKind` 与 `WorkoutSetDoc.contextValue` | 历史和详情显示阻力或坡度上下文 | 复用可清空数值输入；空值表示未记录，0 为有效值 | 单一 kind 保证互斥；阻力 `[0,200]` 可含小数且 `rateMetric=none`；坡度非负 | v5/v6→v7 对缺失 kind 补 `none`；仅 v5 组补 `contextValue=null`，v6 已有值原样保留；内置目录按稳定 ID 融合，保留自定义/未知动作、WorkoutDoc、tombstone 和未展示字段 | `exercises.json`、训练月分片与 WebDAV | 本轮实现 |
| `WorkoutExerciseDoc` 六项记录快照 | P0 创建、编辑、详情、历史、统计、分享 | 选择动作时写入，历史不可切换；不增加左右拆组或侧别输入 | create/update/copy 与草稿恢复均保留 | 嵌套 mapper/repository/聚合编辑/DocumentStore 原样保存；未展示字段不得丢失 | `workouts/YYYY-MM.json` 与 WebDAV 月分片 | 本轮实现 |
| `ExerciseDoc.equipment` | P1 列表、详情、选择器筛选 | 共享表单单选 | create 必传 `EquipmentId \| null`；update 中 undefined 不改、null 清空 | mapper/repository 显式保留；新快照直接生成 | `exercises.json` | 已实现 |
| `ExerciseDoc.description` | P1 详情保留换行 | textarea | create 必传 `string \| null`；update 中 undefined 不改、null 清空；trim 后最多 500 字 | 内部换行原样保存；无平行步骤字段 | `exercises.json` | 已实现 |
| `ExerciseDoc.primary/secondaryMuscleGroupIds` | P1 详情与肌群图 | 共享表单 | 主 3、次 6，枚举合法且主次不交叉 | mapper/repository 原样保留；目录生成期全量校验 | `exercises.json` | 已实现 |
| `ExerciseDoc.provenance?` | 页面 DTO 运行时不得含该 key | 不编辑 | 默认目录生成链只读写入；页面 service 不接收、不返回 | 白名单 mapper 显式排除；repository patch/聚合更新不得因未展示而丢失；无旧 migration | `exercises.json` | 已实现 |
| `BodyMetricDoc.id` 与 BaseDoc 元字段 | 身体数据历史、趋势来源 | 新增、编辑、删除单条记录 | P6 service 统一创建与 tombstone | `DataSnapshot.bodyMetrics` 初始化空数组，静态分片 `body-metrics.json` | `body-metrics.json` | 已实现 |
| `BodyMetricDoc.recordedAt` | 历史排序、趋势归属、当前值来源时间 | 表单允许补录过去时间 | 校验 ISODateTime 且 `<= now`；派生默认过滤未来记录 | 固定字段；不得只保存日期 | `body-metrics.json` | 已实现 |
| `BodyMetricDoc.heightCm` | 当前身高、趋势 | 身体数据页编辑；ProfilePage 不再编辑 | 稀疏指标，最新非空派生；cm 固定单位 | 旧 `ProfileDoc.height` 不迁移、不兼容读取 | `body-metrics.json` | 已实现 |
| `BodyMetricDoc.weightKg` | 当前体重、趋势、统计只读摘要 | 身体数据页按 `SettingsDoc.weightUnit` 输入 | kg 规范持久化；输入/展示 kg/lb 转换 | 旧 `ProfileDoc.weight` 不迁移、不兼容读取 | `body-metrics.json` | 已实现 |
| `BodyMetricDoc.bodyFatPercent` | 当前体脂、趋势、统计只读摘要 | 身体数据页编辑 | 百分比数值；最新非空派生 | 固定字段，未测量为 null | `body-metrics.json` | 已实现 |
| `BodyMetricDoc.measurementsCm` | 围度当前值、左右差值、趋势 | 固定围度表单 | 固定键校验；左右成对围度双序列派生 | 所有固定键写出，未测量为 null；拒绝自由键 | `body-metrics.json` | 已实现 |
| `BodyMetricDoc.note` | 单条测量备注 | 身体数据表单 | trim 后空为 null；不得创建 note-only 记录 | 固定字段 | `body-metrics.json` | 已实现 |
| `TimelineNoteDoc.id` 与 BaseDoc 元字段 | 日历/趋势标记、备注列表 | 新增、编辑、删除备注 | P6 service 统一创建与 tombstone | `DataSnapshot.timelineNotes` 初始化空数组，静态分片 `timeline-notes.json` | `timeline-notes.json` | 已实现 |
| `TimelineNoteDoc.content` | 备注详情，短摘要由截断生成 | 备注表单 | trim 与长度校验；不新增 title/category/tag | 固定字段 | `timeline-notes.json` | 已实现 |
| `TimelineNoteDoc.rangeType/startDate/endDate` | 日历和趋势标记 | 备注表单 | single_day/date_range/open_ended 规则；`startDate <= today`；查询裁剪到 today 后闭区间重叠 | 使用 ISODate，不升级为时间戳 | `timeline-notes.json` | 已实现 |
| `TimelineNoteDoc.workoutId` | 可选来源训练跳转 | 可选关联训练 | 为空或解析有效训练；不支持 exerciseId/templateId | 保留引用，不自动写入 WorkoutDoc | `timeline-notes.json` | 已实现 |
| `ExercisePerformanceRecordDoc.id` 与 BaseDoc 元字段 | 动作详情、统计摘要、分享图摘要 | 用户不手动编辑；可重算/清空重建 | P7 service/core 确定性生成刷新事件 | `DataSnapshot.exercisePerformanceRecords` 初始化空数组，按月分片 `exercise-performance/YYYY-MM.json` | 月分片 | 已实现 |
| `ExercisePerformanceRecordDoc.exerciseId` | 动作详情归属、统计 Top 提升 | 不编辑 | P1 替代链解析归并，保留来源引用 | 固定引用 | 月分片 | 已实现 |
| `kind/metricType/value/unit` | 指标名称、当前最佳、周期摘要 | 不编辑 | 真实 PR 与基于 RPE 修正 RM 比较；规范单位覆盖 kg、kg_reps、kg_seconds、kg_meters、kg_meters_per_second、m、sec、m_per_sec、reps | 枚举校验；展示单位转换不改写规范值；比较器逐项声明 min/max | 月分片 | 已实现 |
| `achievedAt` | 周期归属、排序、分片归属 | 不编辑 | 来源训练/组达成时间；不得未来 | 决定 `exercise-performance/YYYY-MM.json` | 月分片 | 已实现 |
| `sourceWorkoutId/sourceWorkoutExerciseId/sourceSetId` | 来源跳转 | 不编辑 | 新完成训练增量写刷新事件；已完成训练编辑/删除全量重算兜底；同步仅在本地训练或动作来源数据变化后重算 | 保留来源引用；不保存名称快照 | 月分片 | 已实现 |
| `input` 与 `rm` | RM/复合负载摘要、公式展开、分享摘要 | 不编辑 | 保存原始重量及单位、`loadBasis`、`countBasis`、次数、距离、时间、RPE 和必要公式结果；聚合值、倍率、换算重量、有效次数均重新推导，RIR 不持久化 | 真实 PR 的 `rm=null`，RM 事件必填公式明细 | 月分片 | 本轮实现 |
| 分享图 PNG | 训练详情预览、保存、系统分享 | 运行时“显示明细”选项 | P4.1 生成本地输出物 | 不进入 DataSnapshot、manifest 或 Settings；不新增默认偏好 | 不同步 | 已实现 |

## 4. LocalJsonRepository

文件：`frontend/src/repositories/localJsonRepository.ts`。

### 4.1 初始化

```text
getSnapshot()
  -> DocumentStore.load()
  -> makeEmptySnapshot(deviceId)（空存储时）
  -> migrateSnapshot(raw, deviceId)
  -> DocumentStore.save(snapshot)
```

设备 ID 用 `localStorage` 中的 `ironlog_device_id` 保存；在 Android WebView 中它仅用于 manifest 标识，不是账户标识。

### 4.2 CRUD 规则

| 操作 | Repository 行为 |
|---|---|
| create | `withDoc` 生成 string ID、createdAt、updatedAt、deletedAt=null、schemaVersion=`CURRENT_SCHEMA_VERSION`。 |
| update | `withoutUndefined` 后合并；`undefined` 不覆盖旧字段；更新 `updatedAt`。 |
| delete | `tombstone` 写 `deletedAt` 和 `updatedAt`。 |
| list/get | 默认过滤 tombstone；需要历史/同步时显式 `includeDeleted`。 |
| mutate | 更新 manifest.updatedAt，重建 manifest.shards，调用 DocumentStore.save。 |

训练创建/更新还有额外规则：若嵌套动作或组 ID 为 `""`，repository 生成新 ID。复制训练必须显式把来源嵌套 ID 置空，不能只复制数组引用。

### 4.3 原子性限制

当前 repository 是内存快照 + 全量相关分片写入模型；它没有跨文件事务或本地临时文件发布。一次写入中断时可能留下部分新分片和旧 manifest。当前恢复策略是以 manifest 为读取入口；后续要提高可靠性时，应引入本地临时文件、校验和或写入日志，而不是在页面中补偿。

---

## 5. schema migration

文件：`frontend/src/core/migrations.ts`。

### 5.1 当前行为

`CURRENT_SCHEMA_VERSION = 7`。存储不存在时创建完整 v7 快照；读取 v5 或 v6 时执行显式增量迁移，读取 v7 时严格归一化并原样保留。v4 及更早或未来版本明确拒绝，不转换、不清空。JSON 损坏或 manifest 引用分片缺失时也明确拒绝，并且不得进入 migration 写回。

v5/v6→v7 同时处理静态动作、WorkoutDoc 月分片和成绩月分片：v6 旧爬楼机枚举映射为当前枚举，缺失 `contextKind` 补 `none`；v5 组补 `contextValue=null`，v6 已有组上下文值不覆盖；内置目录按稳定 ID 融合，自定义/未知动作和历史训练保留；旧阻力成绩 tombstone。迁移后重建 manifest.shards，并经 DocumentStore 与 WebDAV 使用同一路径。

### 5.2 开发期动作目录替换与兼容基线后的迁移

v5 基线之后不再使用开发期整体替换。目录融合只覆盖稳定内置 ID 的受控内容字段，不删除本地额外记录；WorkoutDoc 永远是训练事实真源。只有用户明确指定的浏览器测试数据、Android 虚拟机和隔离 WebDAV 测试目录可以人工清理。

首个兼容基线建立后，当 `schemaVersion` 再次增加时才必须：

1. 在 `models.ts` 定义新字段和默认/空值语义。
2. 新增显式 migration 分支，例如 `v1 -> v2`，不能仅靠对象展开。
3. 同时迁移静态文件和 workout 月分片。
4. 保留 tombstone 与未知字段的兼容策略。
5. 添加旧快照升级测试、无数据初始化测试和同步后加载测试。
6. 更新 manifest schemaVersion，并在发布说明描述不可逆风险。

测试必须覆盖非空 v5 快照升级、AVD 实际 v6 旧枚举升级、内置目录融合、自定义/未知动作保留、未结束草稿恢复、WorkoutDoc 与嵌套 ID 保留、阻力旧成绩 tombstone、迁移写回后稳定读取、JSON/WebDAV 往返、JSON 损坏与 manifest 引用分片缺失阻断、v7 幂等读取以及 v4/未来版本拒绝。应用本身不得执行清空。

P6 已直接用 `BodyMetricDoc` 替换旧 `ProfileDoc.height` / `ProfileDoc.weight` 身体数据模型。当前阶段不迁移旧开发/虚拟机测试数据，不在读取层兼容旧字段，也不把旧 Profile 单值生成初始身体记录；验证时按运行指南清空 IndexedDB 或 Android 模拟器旧测试数据后重建。这个规则只适用于当前开发测试阶段，不能作为面向真实用户发布的数据保留策略。

### 5.3 导入/导出边界

DocumentStore 已提供 `exportFiles` / `importFiles` 辅助函数，用于把 snapshot 转为逻辑文件集合或反向解析。当前没有用户 UI、文件选择器、完整格式校验或覆盖确认流程；不得在文档中宣称“已提供数据导入导出”。

后续导入流程至少要：校验 manifest、校验路径白名单、执行 migration、显示将覆盖/合并的文档数量、创建本地备份，再调用 `replaceSnapshot`。

---

## 6. 关键测试与验收

现有测试文件包括：

| 文件 | 覆盖方向 |
|---|---|
| `src/core/migrations.test.ts` | 默认快照、元字段和分片列表。 |
| `src/platform/documentStore.test.ts` | 文件/快照转换和分片持久化约束。 |
| `src/services/bodyMetrics.test.ts` | 身体指标当前值派生。 |
| `src/services/performance.test.ts` | PR/RM 刷新事件和动作趋势派生。 |
| `src/services/plan.test.ts` | 计划规则的服务层约束。 |

修改本模块后至少增加/执行：

```powershell
cd D:\workspaces\vscodeWorkspace\project\bodybuilding\ironlog\frontend
npm test
npm run build
```

人工验收：

- Web 开发刷新后训练仍可读取。
- 训练写入正确月分片，不生成全量 `workouts.json`。
- 删除训练后常规列表隐藏、分片保留 tombstone。
- 更新设置不把 `undefined` 写成字段值。
- Android 环境下由 Capacitor Filesystem 读取同一逻辑文件结构。
- 默认目录在清空不兼容的 IndexedDB、Android 虚拟机和隔离 WebDAV 测试数据后，从空快照初始化；内置集合与 `candidates.md` 精确一致，废弃测试动作不残留。
- `exercises.json` 往返后稳定 ID、category、六项记录配置（含 `countBasis`、`contextKind`）、equipment、分段 description、主次肌群和只读 provenance 原样保留；主次肌群合法且不重复，description 不超过 500 字。
- `workouts/YYYY-MM.json` 往返保留每个动作的六项历史快照、组上下文、嵌套 ID、`supersetGroup`、`restSeconds` 和未展示字段；`exercise-performance/YYYY-MM.json` 往返保留允许生成的成绩、完整输入摘要和比较方向所需数据。
- 构建产物和本地快照不包含 Media 字段、媒体 URL、图片/GIF、`media_id` 或非中文 instructions。
- `body-metrics.json`、`timeline-notes.json`、`exercise-performance/YYYY-MM.json` 必须进入 manifest、DocumentStore export/import、WebDAV pull/merge/push、backup 与 tmp/MOVE 往返；分享图 PNG 不进入 manifest。
