# IronLog 训练动作评价指南

> 职责：本文件是当前准入指标、评价指标、视频证据提取方法、证据等级、评价流程和输出规范的唯一权威。
>
> 逐动作的原始来源、完整 `instruction_steps.zh`、现用或拟用文本、当前评价结论及差异原因，分别保存在[《默认动作库内容候选 v1》](默认动作库内容候选v1.md)和[《默认动作库内容候选 v2》](默认动作库内容候选v2.md)。本文件不保存逐动作证据。
>
> 默认目录的唯一生成输入是 `candidates.md`；内容候选文档和本指南均不参与生成。

---

## 1. 使用方式与证据边界

评价结论必须同时回答四个不同的问题，不能用一个“最好”替代：

1. 该动作在已声明的目标下能扮演什么角色？
2. 在当前目录中，它是否提供了足够的新增价值？
3. 现有 IronLog 模型、记录方式、器械枚举和来源契约能否实施它？
4. 支撑结论的是已核验事实、合理推断，还是视频作者的观点？

文中标记含义如下：

- **[已验证]**：已核验当前代码、固定上游记录、视频时间对齐证据或外部一手/高质量研究。
- **[推断]**：从已验证材料得出的目录策展判断；应写明适用条件。
- **[视频观点]**：视频作者明确说出或展示的排序、训练建议或经验判断；不能单独证明效果、安全性、伤病预防或专项迁移。
- **[待核验]**：候选的来源、模型、记录方式或证据还不足以支持实施。

外部训练研究主要适用于健康成人、持续至少数周的训练干预。伤病、疼痛、孕期、术后、慢病、竞技专项或未成年人的动作选择，需要另行的个体评估；本指南不能替代该评估。

---

## 2. `ref/` 视频证据方法与处理状态

### 2.1 证据处理状态

八段视频均已按视频证据提取流程完成预检、转写、关键帧提取与验证。原始证据保存在被 Git 忽略的 `ref/.evidence/` 下。五段源视频在末尾帧导出时失败，已在新的目录用保守抽帧预算重跑；重跑目录全部通过验证，但只有开场帧，故其动作技术只能以时间对齐口述为证，不能视为完整视觉示范。

| 视频 | 通过验证的证据目录 | 视觉证据 | 主要限制 |
|---|---|---|---|
| 核心训练第一期 | `01-core-1` | 60 张关键帧 | 无内嵌字幕；专业名词可能被转写误识 |
| 核心训练第二期 | `02-core-2-retry` | 1 张开场帧 | 主要依赖转写，动作名需按上下文复核 |
| 下肢运动表现（下） | `03-lower-performance-2` | 60 张关键帧 | 无内嵌字幕；研究转述未提供完整引文 |
| 下肢安全稳定训练 | `04-lower-safe-stable-retry` | 1 张开场帧 | 计划与技术细节主要依赖口述 |
| 下肢运动表现（上） | `05-lower-performance-1` | 60 张关键帧 | 视频排名只覆盖作者定义的下肢表现目标 |
| 爆发力动作 | `06-power-retry` | 1 张开场帧 | 口述动作与等级不构成完整视觉技术证据 |
| 上肢推动作 | `07-upper-performance-1-retry` | 1 张开场帧 | 两种 F 级卧推变式的名称无法可靠还原 |
| 上肢拉动作 | `08-upper-performance-2-retry` | 1 张开场帧 | 视频中的研究、风险和迁移说法未给出可复核引文 |

#### 逐视频方法论与推荐动作

下表中的“推荐动作”严格表示视频作者的推荐或高评价动作，不表示 IronLog 已采用或本指南已验证其优越性。

| 视频 | 视频直接提出的方法论（时间） | 作者推荐/高评价的动作 | 可保留的策展启发 | 不可直接升格的结论 |
|---|---|---|---|---|
| 核心训练第一期：抗伸展 | 以动作纯度、超负荷/进退阶空间、风险收益比排序（`1:41–2:03`） | RKC 平板支撑、腹轮/吊环前伸、龙旗、泽奇支撑/行走、双壶铃前架位行走；平板、死虫、空体支撑作为基础或教学选择 | 用抗伸展、抗旋转、抗侧屈等目标组织核心动作；把肋骨—骨盆控制、呼吸和代偿作为验收点 | S/A/B 排名；吊环前伸优于腹轮；某动作是普通人的“最佳核心动作”；视频转述的肌电或研究结论 |
| 核心训练第二期：抗旋转与抗侧屈 | 继续使用纯度、进阶空间、风险收益逻辑（`2:36–2:44`） | 帕洛夫推举、单侧农夫行走、哥本哈根支撑；侧平板、鸟狗式、俯卧撑位单臂划船作为不同层级候选 | 动态负重稳定、静态控制、上肢复合动作应按目标分别评价；可设置清晰回归/进阶路径 | 侧平板不能作主力、固定 45 秒阈值、单侧农夫行走“最值得投入”、固定频次/组数/距离处方 |
| 下肢运动表现（下）：拉类 | 高等级看机械张力/力量、稳定易学、可渐进负荷、专项迁移（`0:27–0:51`） | 六角杠硬拉、罗马尼亚硬拉、传统硬拉、臀推、北欧腘绳肌训练、壶铃摆动；坐姿腿弯举和单腿 RDL 为辅助 | 髋铰链、后链补强、单腿控制和爆发辅助应有不同角色；区分杠铃、哑铃、绳索等变式 | 六角杠“最安全/迁移最好”、单腿杠铃 RDL 必然伤腰、臀推/北欧训练的专项或防伤承诺 |
| 下肢安全稳定训练 | 用低、中、高重复区间和“主力—单侧/弱项—辅助/核心”三段式排课（`0:33–3:38`） | 背蹲/前蹲、分腿蹲、哥本哈根支撑、侧桥、小腿训练；硬拉/六角杠/RDL/北欧/臀推 | 将**动作评价**与**计划编排**分开；一个动作可在计划中扮演主力、补强或辅助角色 | “最安全稳定”、必须同时覆盖三种重复区间、RPE/百分比/减量比例和经验年限等固定处方 |
| 下肢运动表现（上）：推类 | 高效安全地提升力量、易学、可超负荷、具有迁移性（`0:26–0:48`） | 高杠深蹲、前蹲、保加利亚分腿蹲；箭步蹲、哈克深蹲、反向北欧挺为条件性选择 | 双侧/单侧、膝主导/髋主导、稳定/不稳定支撑是特征，不是天然高低级；先明确目标 | “所有人必须掌握”高杠深蹲、BOSU 深蹲无任何价值、固定力量阈值后才能做爆发训练 |
| 爆发力动作 | 将功率表达、学习成本与受伤风险纳入排序（`0:25–1:09`） | 跳深、高翻、高拉、六角杠深蹲跳、中腿拉；敏捷梯、跳箱和单臂哑铃抓举被给出较低评价 | 爆发力应另看力—速度表达、反应式/落地要求、冲击暴露与技术条件；箱高不是爆发力的充分指标 | 所有三关节伸展动作的普适性、固定跳深高度/力量阈值、任一动作的伤病概率或绝对安全性 |
| 上肢推动作 | 专项迁移、稳定渐进、关节健康/风险收益、学习成本（`0:25–0:47`） | 杠铃卧推、窄距卧推、上斜卧推、严格推举、哑铃/器械推、站姿绳索前推、地雷杠推；俯卧撑和双杠臂屈伸按条件使用 | 输出应区分主项、容量补充、技术学习和居家替代；设备与支撑形式是使用条件 | 固定 S–F/B+/A+ 排名、某推举必然更护肩/更专项、视频中的战术体能和研究转述 |
| 上肢拉动作 | 运动拉转迁移、自然关节运动、可渐进与大训练量、腰椎/关节安全（`0:25–0:45`） | 正/反/对握引体、高位下拉、胸托/俯身/潘德雷/单臂哑铃/反向划船、面拉 | 按垂直拉、水平拉、肩胛/肩后侧辅助组织；记录支撑形式、单双侧、握法、躯干控制与疲劳成本 | 颈后动作绝对禁用、对握引体最护肩、面拉人人必练、特定握法或划船的防伤/专项承诺 |

### 2.2 通用提取与使用约束

- 预检源文件、时长、音轨和可解码性；转写必须保留时间对齐信息。
- 提取关键帧后核对帧数和时间覆盖；失败时在独立证据目录保守重跑，不覆盖原证据。
- 只有开场帧或视觉覆盖不足时，只能引用时间对齐口述，不能写成完整视觉示范。
- 具体视频对具体动作的观点迁入内容候选，并保持 `[视频观点]` 标签；视频排名不能直接成为 IronLog 评价结论。

### 2.3 外部证据的统一校正

- **[已验证]** 阻力训练处方应先匹配目标、能力和训练状态；渐进负荷是长期适应所需的实施原则，而不是某个动作天然更高价值的证据。[ACSM 2009 立场声明](https://pubmed.ncbi.nlm.nih.gov/19204579/)
- **[已验证]** 2026 ACSM 立场声明以健康成人的系统综述为基础，强调不同目标的处方变量并不相同；因此不能用一张动作总榜同时裁定力量、肥大、功率和体能。[ACSM 2026 立场声明](https://pubmed.ncbi.nlm.nih.gov/41843416/)
- **[已验证]** 全活动范围在该系统综述中对力量和下肢肥大总体更有利，但这不是忽略疼痛、结构限制或专项局部范围的硬规则。[活动范围系统综述](https://pubmed.ncbi.nlm.nih.gov/34170576/)
- **[已验证]** 自由重量与器械的直接比较没有显示一种器械类型在最大力量、肥大和跳跃表现上普遍胜出；目录不应给“自由重量”或“器械”固定加分。[自由重量与器械 Meta 分析](https://pmc.ncbi.nlm.nih.gov/articles/PMC10426227/)
- **[已验证]** 动态阻力训练的迁移具有任务特异性；“动作本身 1RM 更高”不能直接写成全面力量或专项表现更好。[任务特异性系统综述](https://pubmed.ncbi.nlm.nih.gov/40314751/)
- **[推断]** 因此，视频能提供动作候选、可能的技术观察点和待核验问题；外部证据与当前模型才决定目录结论。视频的排名、肌电、单项研究转述和安全宣传均不进入本指南的事实层。

### 2.4 事实与判断分离

逐动作记录应区分：

- **上游原始事实**：固定 revision 中可直接核对的字段与步骤。
- **IronLog 人工策展结论**：最终采用或拟采用的名称、记录语义和 description。
- **异常或损失**：两者的具体差异及原因。
- **当前评价**：按本指南得到的现行结论。
- **待人工判断**：证据不足或产品决策尚未完成的事项。

不得把策展判断写成上游事实，也不得以“来自上游”推导“内容正确”。

### 2.5 provenance 的证据能力

`provenance` 只表示“该动作引用固定 revision 中的某条上游记录”。它不表示最终名称、分类、器械、肌群或 description 忠实转写上游，也不能代替内容候选中保存的完整 `instruction_steps.zh`。

因此：

- 上游字段可以被纠错、压缩、重写或按产品语义扩展。
- 不要求最终运行时字段与上游严格一致。
- 所有不一致都必须在对应内容候选条目的“异常或损失”中说明具体差异和原因。
- 原始步骤只属于内容证据层，不新增运行时 `instructionStepsZh` 字段。

---

## 3. 统一评价方法论

### 3.1 先定目标，再谈动作

每次评价先从下列一个主目标出发；若一个动作有多个作用，选择本轮要解决的主目标，而不是把所有作用相加：

- **力量特异性**：目标动作/模式、负荷、活动范围、收缩方式和速度是否匹配。
- **肌肥大刺激**：能否在可控范围内累积足够训练量，并允许稳定接近目标用力程度。
- **功率/爆发**：是否能在已具备相应技术和耐受条件下快速表达力量；需要单列冲击、落地和技术要求。
- **核心/躯干控制**：目标是抗伸展、抗旋转、抗侧屈、屈曲、负重行走或其他明确任务，不能笼统称“练核心”。
- **心肺/体能**：优先看可持续记录的距离、时间、负荷和场地条件，而不是把高疲劳复合动作自动称为更好。
- **局部辅助/单侧控制**：说明它补的是哪个已知缺口，不把它伪装成所有人的主力动作。

### 3.2 再过四道准入门

1. **证据与语义门**：有可追溯来源或明确的自编契约；名称、步骤、器械和目标不自相矛盾。
2. **实现门**：当前 `ExerciseDoc`、器械枚举和完整记录配置可以准确表达；否则标为“阻塞”，而非低分入库。
3. **条件门**：动作可在使用者的可控范围、技术能力、设备、监督和症状状态下完成。疼痛、已知伤病或无法控制代偿时，结论应改为“需调整/转介”，而不是给动作添加一个虚假的通用风险分。
4. **目录身份门**：它须补足不同的主角色、目标条件、记录价值，或有清楚的替代路径；不能只因名称流行、变式较多或视频等级高而进入目录。

### 3.3 最终输出是角色，不是永久名次

通过准入门的动作，使用以下结论词：

- **主力候选**：在给定目标下可稳定渐进，并且目录价值清晰。
- **辅助候选**：目标明确，但更适合补强、容量或局部训练。
- **条件替代**：在特定器械、技术、可控范围或疲劳条件下可替代主力。
- **储备/暂缓**：当前覆盖增量低、证据不充分或实施收益不清晰。
- **阻塞**：当前模型、记录方式、来源契约或安全边界未满足；不是对动作训练价值的否定。

---

## 4. 与当前项目实现对齐

| 事项 | [已验证] 当前实现 | 本指南的处理 |
|---|---|---|
| 运行时评分/风险/训练目标字段 | `ExerciseDoc` 没有等级、评分、风险、禁忌、难度或结构化训练目标字段 | 所有评价仅留在 `docs/data_import`，不得声称已驱动 UI 或持久化 |
| 当前可筛选字段 | 动作库只按名称、`category`、`equipment` 筛选；模板激活后按动作 ID 真过滤 | “目录筛选”与“运行时筛选”分开表述 |
| 器械筛选展示阈值 | `ExerciseLibraryPage` 只单列使用次数大于 2 的器械，其余归入“其他” | 若正式目录仅有六角杠硬拉和六角杠农夫行走两条 `trap_bar`，它不会有独立筛选项；实施批次须决定是否接受“其他”归类或调整产品规则 |
| 训练目标表达 | 只有 `category` 与主/次肌群；`movementPattern` 尚未实现 | 细分目标、角色和限制是文档语义，不能伪装成模型字段 |
| 可记录性 | 七种 `RecordingMode` 与 `loadBasis/loadDirection/rateMetric` 是唯一权威 | 每项都要核验核心训练量是否能由当前组字段表达；不凭名称或器械猜测 |
| 器械 | 当前 `EquipmentId` **不含** `trap_bar`；用户已批准后续扩展该枚举 | 六角杠硬拉、六角杠农夫行走可在候选审查中标为“已批准前置”，但代码、字段契约和测试实际落地前不能实施 |
| 来源 | `DefaultExerciseSeed.provenance` 当前为必填，生成器/测试要求正式 63 条均有来源；用户已批准后续改为可选 | 自编动作的 G0 可由明确自编契约通过；当前生成链仍不能实施，待可选化实际落地后复审，绝不伪造近似上游来源 |
| 左右侧 | 当前不设左右侧字段；P1 不记录两侧不对称的重量、次数、距离、时间、PR 或休息 | `per_hand` 只表示每手重量；`per_side` 表示一次填写的是每侧值并按两侧聚合。单侧农夫/手提箱行走可用 `total + per_side` 表达，但不保存左右独立历史 |
| 个人成绩 | 是用户训练表现事件，不是策展评分 | 不得把 `higher_better`/`lower_better` 解释为动作质量或风险 |

实现依据见 [核心模型](../../ironlog/frontend/src/core/models.ts)、[记录方式注册表](../../ironlog/frontend/src/core/recordingModes.ts)、[默认目录生成器](../../ironlog/frontend/scripts/exerciseCatalog.mjs) 和 [动作库领域契约](../详细设计文档_P1_计划与动作库.md)。

---

## 5. 筛选与评分权威

本节是 IronLog 默认动作目录的唯一评分口径。它替代 v1 的“通用性/训练价值/可获得性/动作覆盖/可记录性/冗余惩罚/数据质量”单一总分。任何候选表、实施方案、代码注释或临时讨论不得另设可与本节竞争的评分模型。

### 5.1 评分前的准入门

候选必须依次记录以下四门的状态，且 `R` **不得**逆转任何一门的结论。G0、G1 是实施硬门；G2 可以在条件被明确写出时“条件通过”，G3 可以在角色或替代路径明确时“条件通过”。G2 的条件无法写清、或 G3 没有目录身份时，结论为“暂缓”，而不是靠高分入库。

| 门 | 必须满足的条件 | 未通过的处理 |
|---|---|---|
| G0：来源与语义 | 固定上游记录或明确自编契约；名称、步骤、器械和目标可解释 | 停止入库；不得借用近似动作的 provenance |
| G1：模型与记录 | 当前字段、器械枚举与完整记录配置能表达核心训练量 | 记录模型阻塞项；先更新字段契约矩阵和实现，再评价实施 |
| G2：使用条件 | 能写出可控 ROM、技术/监督、设备和不适信号等边界 | 条件写清即为条件通过；无法写清时停止入库或转介，不得写通用安全率、必然防伤或医疗结论 |
| G3：目录身份 | 有与现有动作不同的主角色、目标条件、记录价值或明确替代路径 | 角色/替代路径明确即为条件通过；否则标为储备/暂缓，不因名称流行、变式多或视频等级高而入库 |

`已批准前置` 不是 G1 的同义通过：它只表示用户已经明确授权在紧随其后的实施批次补齐缺失契约，例如 `trap_bar` 枚举或 `DefaultExerciseSeed.provenance` 可选化。候选可以继续完成来源、角色和评分审查；在代码、字段契约与测试真实落地并复审为“通过”前，仍不得写入正式目录。没有此类明确批准的缺口仍按“阻塞”处理。

### 5.2 四维目录优先级分

仅在 G0、G1 通过，且 G2、G3 已形成可执行的条件与角色结论后，为**已经声明的单一主目标**计算 `R`。多目标动作不累加多个目标分；改评价目标时必须重新打分。

```text
R = T（目标适配） + P（可渐进与可记录） + U（目录独特性） + Q（证据与数据置信度）
```

每项取 0–3：

| 维度 | 0 分 | 1 分 | 2 分 | 3 分 |
|---|---|---|---|---|
| T：目标适配 | 主目标不明确或不匹配 | 只能间接服务目标 | 可作为条件替代/辅助 | 直接、清晰地服务本轮主目标 |
| P：可渐进与可记录 | 当前组字段不能表达核心训练量 | 可记录但关键进阶变量缺失 | 可用负荷/次数/时间/距离中的一条稳定推进 | 进阶和核心训练量均可明确、稳定地记录 |
| U：目录独特性 | 仅是低价值微变体 | 与现有动作高度重叠 | 有设备、方向、单侧性或角色差异 | 补足尚无主角色或不可替代的记录价值 |
| Q：证据与数据置信度 | 来源/语义未核验或有关键矛盾 | 只能依赖间接推断、人工语义扩展或显著信息损失 | 来源、字段和一般训练证据充分，仍缺直接比较证据 | 有直接且适用的人群/目标证据，并且字段与实现无损匹配 |

`R` 的解释：

- **10–12**：可作为优先候选；仍需给出角色和条件，不能称“总体最佳”。
- **8–9**：可作为条件替代、辅助或储备候选；须写明限制与覆盖取舍。
- **0–7**：通常暂缓；只有经人工批准且有不可替代价值时才保留审计记录。
- **G0 或 G1 阻塞、待核或已批准前置**：`R` 仅用于说明潜在目录价值，不具有入库资格。
- **G2 条件未写清或 G3 暂缓**：即使列出 `R`，也不得把它解读为入库优先级；应先补足条件或目录身份。

### 5.3 评分输出格式与禁止项

每个候选必须写成：

```text
主目标与角色；G0/G1/G2/G3 状态（通过、待核、条件通过、已批准前置、阻塞或暂缓）；T/P/U/Q=R；结论；条件或替代路径；证据层级。
```

禁止：

- 用 `R` 宣称动作对所有人更安全、更有效或更适合专项。
- 因自由重量、器械、多关节、单侧、复杂或流行而自动加减分。
- 用视频作者的 S/A/B/F、肌电、相关性或未给出书目信息的研究转述填充 Q=3。
- 把目录评分写入 `ExerciseDoc`、生成产物、个人成绩、页面筛选或模板筛选逻辑。
- 在未通过来源或模型门时，通过降低标准、伪造来源、重命名近似记录或新增无契约字段绕过阻塞。

### 5.4 复审触发与审计

目录每次增删、字段模型/记录方式变更、上游版本变更、目标角色调整或出现直接适用证据时，必须重评受影响动作的 G0–G3 与 `T/P/U/Q=R`。内容候选只展示当前评价；变更原因和证据链接写入对应条目的异常或损失，不保留历史评分列或逐动作重评流水。
