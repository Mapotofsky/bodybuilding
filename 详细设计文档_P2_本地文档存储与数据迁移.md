# P2 详细设计文档：本地文档存储与数据迁移

> 对应概要设计：M3 本地存储、迁移与同步中的本地持久化部分；M5 主题设置；M6 AI 非敏感配置
> 状态：当前本地存储基线；主题 themeId 已进入 SettingsDoc；P4.1/P6/P7 新业务文档、AI 配置与批准计划批量导入为规划中
> 前置依赖：概要设计的数据不变量；P0/P1 的领域文档模型。

---

## 1. 设计目标与边界

本模块在无服务端环境中提供训练、动作、计划、模板、资料和设置的本地真源。它解决三个问题：

1. 同一业务代码在浏览器开发和 Android 上可读写相同 JSON 逻辑文件。
2. JSON 以静态文件与训练月分片保存，不退化为单一巨型文件。
3. 文档格式变化可通过 schemaVersion 归一化，而不是要求页面兼容所有旧字段。

本模块不负责：WebDAV 网络 IO、同步冲突合并、用户可操作的导入/导出 UI、Android 硬件级秘密存储。它只提供 `DocumentStore` 和 `LocalJsonRepository`。

---

## 2. 文件、快照和 manifest

### 2.1 逻辑文件

| 路径 | 内容 | 更新来源 |
|---|---|---|
| `manifest.json` | 应用标识、schema、设备 ID、分片列表 | 任意 repository mutation 后重建。 |
| `profile.json` | ProfileDoc | 更新本地资料。 |
| `settings.json` | SettingsDoc | 更新可同步偏好，不保存 WebDAV 端点配置。 |
| `exercises.json` | ExerciseDoc[] | 内置/自定义动作、tombstone 与替代重定向。 |
| `templates.json` | `{ plans, templates }` | 计划、模板变更。 |
| `body-metrics.json` | BodyMetricDoc[] | P6 身体指标稀疏测量记录，规划中。 |
| `timeline-notes.json` | TimelineNoteDoc[] | P6 日期/时间段背景备注，规划中。 |
| `workouts/YYYY-MM.json` | WorkoutDoc[] | 该月训练创建、编辑、删除、复制。 |
| `exercise-performance/YYYY-MM.json` | ExercisePerformanceRecordDoc[] | P7 动作成绩刷新事件，规划中。 |
| `assets/avatar/*` | 头像资源文件 | ProfilePage 选择、替换或清除头像。 |

`DataSnapshot.workouts` 是运行时聚合数组。训练的磁盘真源只允许是月分片；不得新增全量 `workouts.json` 或另一份训练索引文件。

### 2.2 IronLogManifest

```ts
interface IronLogManifest {
  app: "ironlog";
  schemaVersion: number;
  deviceId: string;
  updatedAt: string;
  shards: Array<{
    path: string;
    updatedAt: string;
    etag?: string | null;
  }>;
}
```

`buildShardList(snapshot)` 输出四个静态分片、实际存在的训练月分片以及头像资源文件。某月只有 tombstone 训练时，该分片仍必须列出并同步；否则另一设备可能恢复已删除的训练。

### 2.3 分片规则

```ts
workoutShardPath("2026-06-22") === "workouts/2026-06.json"
isWorkoutShardPath(path) === /^workouts\/\d{4}-\d{2}\.json$/
```

日期不合法不能用于构造分片路径。当前 service 依赖页面传入 `YYYY-MM-DD`；后续若开放导入，导入层必须在写入前校验日期。

---

## 3. DocumentStore 平台接口

定义位置：`frontend/src/platform/documentStore.ts`。

```ts
interface DocumentStore {
  load(): Promise<Partial<DataSnapshot> | null>;
  save(snapshot: DataSnapshot): Promise<void>;
  readSecret(key: string): Promise<string | null>;
  writeSecret(key: string, value: string): Promise<void>;
  removeSecret(key: string): Promise<void>;
  readSyncEndpoint(): Promise<SyncEndpointConfig>;
  writeSyncEndpoint(config: SyncEndpointConfig): Promise<void>;
  clearSyncEndpoint(): Promise<void>;
  exportFiles(snapshot: DataSnapshot): Record<string, unknown>;
  importFiles(files: Record<string, unknown>): Partial<DataSnapshot>;
}
```

### 3.1 浏览器实现：IndexedDbDocumentStore

- 使用一个 IndexedDB object store 保存逻辑路径到 JSON 对象的映射。
- manifest 是读取入口；先读 manifest，再读静态文件和其中列出的训练分片。
- 浏览器 secret 当前也保存在 IndexedDB 的独立 key 前缀下，只用于开发兼容，不代表安全凭据库。
- 清理站点数据或隐私模式结束会删除这些数据。

### 3.2 Android 实现：CapacitorDocumentStore

- 使用 Capacitor Filesystem `Directory.Data` 下的 `ironlog-data/`。
- 保存时先写所有非 manifest 分片，再写 manifest，最后删除旧 manifest 中已不存在的训练月分片。
- 读取 JSON 失败当前返回 `null`；这避免应用启动立即崩溃，但也会隐藏损坏原因。后续应增加校验和恢复提示。
- Preferences 用于 `readSecret/writeSecret/removeSecret`。它不写入同步 JSON，但当前不是硬件级加密层。

### 3.3 平台选择

`createDocumentStore()` 仅在 `window.Capacitor.isNativePlatform()` 为真时选择 Capacitor 实现，否则选 IndexedDB。业务模块不得自行分支判断平台。

---

### 3.4 主题与规划中的 AI 设置

当前 SettingsDoc 包含 weightUnit、themeId、lastSyncAt。WebDAV url、username、passwordRef 是本机-only 同步端点配置，通过 DocumentStore 本地配置接口保存；password 明文只能通过 DocumentStore secret 保存。它们不得进入 `settings.json`、manifest、远端 JSON 分片或 backup。themeId 使用稳定 string ID，默认 emerald-slate；未知值由 UI 回退但 migration 和同步不得改写原值。AI 仅保存非敏感 provider/model/能力配置、同意版本及 apiKeyRef；API key 明文只能通过 DocumentStore secret 保存。

新增持久化字段必须递增 schemaVersion，添加显式 migration、读取后写回、DocumentStore export/import 与 WebDAV 往返测试。远端 settings 的秘密引用清空规则由 P3 扩展，不能只依赖页面默认值。

### 3.5 动作详情元数据

当前 ExerciseDoc 保存名称、分类、记录类型、description、目标肌群、自定义标记和替代引用。目标肌群只使用下列稳定 ID；`description` 是唯一的动作要领字段。旧能耗估算字段和训练热量估算规划已从模型删除；旧 JSON 中多余字段在读取边界忽略，不再写回。

```ts
type MuscleGroupId =
  | "chest" | "back" | "shoulders" | "biceps" | "triceps" | "forearms"
  | "core" | "glutes" | "quadriceps" | "hamstrings" | "calves"
  | "adductors" | "abductors" | "full_body" | "other";

interface ExerciseDoc extends BaseDoc {
  primaryMuscleGroupIds: MuscleGroupId[];
  secondaryMuscleGroupIds: MuscleGroupId[];
}
```

`category` 不替代或推导目标肌群。数组的空值语义为“尚未提供”，不得使用 null。现有 `description: string | null` 是唯一的动作要领字段：它保存为纯文本并作为段落显示，不接受 HTML、外链媒体或富文本，也不得新增 `techniqueSteps` 或其他并列说明字段。P1 负责输入校验、标签文案和“暂无”呈现；P0 负责派生个人统计；本节只定义持久化字段和序列化边界。

本轮不设计旧快照 migration。开发机浏览器 IndexedDB 或 Android 虚拟机测试数据若与新字段不兼容，直接清空相应测试存储并用默认数据创建新快照；不得把默认值合并伪装成真实用户兼容迁移。内置动作的目标肌群与 description 由默认数据在新快照中提供；自定义动作在此测试阶段不承诺保留。字段随 `exercises.json` 经 DocumentStore export/import 和 P3 的既有分片同步往返，且不能派生或持久化个人统计。

该“清空测试数据”规则只适用于当前开发和虚拟机验证，不能作为面向已有真实用户的发布策略。若未来需要保留用户本地动作数据，必须先另行批准 migration、备份或导出方案。

### 3.6 头像资源

`ProfileDoc.avatarUrl` 保存头像资源路径引用，例如 `assets/avatar/profile-local.txt`，不保存 base64 正文。头像正文由 DocumentStore 作为资源文件保存；Web 端和 Android 端均通过同一 service 读取为 data URL 预览。ProfilePage 选择或替换头像时先写入资源文件，再把 profile 引用改为该路径；清除头像时将 `avatarUrl` 置为 null 并删除旧资源。

头像资源必须进入 manifest shards，并随 P3 同步流程 tmp/MOVE 发布。远端已有头像资源在覆盖前写入 `backups/`；清除头像后，下一次同步根据新 manifest 删除远端过期头像资源。profile 与资源文件的边界是：profile 只保存引用，资源正文不进入 `profile.json`、`settings.json` 或训练分片。当前工程先实现 Web 文件选择和本地资源接口；若 Android WebView 的文件选择能力受设备限制，后续再增加 Capacitor 原生选择器适配。

### 3.7 规划中的批准计划批量导入

当前 createPlan、createTemplate、createExercise 是独立 mutation，不能满足 AI 导入的原子性要求。P2 只定义并实现 repository 批量提交原语：在 snapshot 副本上应用已验证的变更、一次保存、成功后替换正式 snapshot，失败则保持正式 snapshot 不变。候选 JSON 校验、未知动作决议、预览、导入业务顺序和回滚规则由 P5 权威定义。

### 3.8 P4.1/P6/P7 字段契约矩阵（规划中）

本节是新增持久化字段的前置契约。实现前必须先更新本节，再修改 core model、service、mapper、repository、migration、DocumentStore 和 SyncService。P4.1 统计页、动作详情、分享图只读消费这些数据；字段写入权威分别归 P6 与 P7。

| 字段/文档 | 页面展示 | 页面编辑 | service/core | mapper/repository/migration | WebDAV 往返 | 状态 |
|---|---|---|---|---|---|---|
| `BodyMetricDoc.id` 与 BaseDoc 元字段 | 身体数据历史、趋势来源 | 新增、编辑、删除单条记录 | P6 service 统一创建与 tombstone | 新增 `DataSnapshot.bodyMetrics`，初始化空数组，静态分片 `body-metrics.json` | `body-metrics.json` | 规划中 |
| `BodyMetricDoc.recordedAt` | 历史排序、趋势归属、当前值来源时间 | 表单允许补录过去时间 | 校验 ISODateTime 且 `<= now`；派生默认过滤未来记录 | 固定字段；不得只保存日期 | `body-metrics.json` | 规划中 |
| `BodyMetricDoc.heightCm` | 当前身高、趋势 | 身体数据页编辑；ProfilePage 不再编辑 | 稀疏指标，最新非空派生；cm 固定单位 | 旧 `ProfileDoc.height` 不迁移、不兼容读取 | `body-metrics.json` | 规划中 |
| `BodyMetricDoc.weightKg` | 当前体重、趋势、统计只读摘要 | 身体数据页按 `SettingsDoc.weightUnit` 输入 | kg 规范持久化；输入/展示 kg/lb 转换 | 旧 `ProfileDoc.weight` 不迁移、不兼容读取 | `body-metrics.json` | 规划中 |
| `BodyMetricDoc.bodyFatPercent` | 当前体脂、趋势、统计只读摘要 | 身体数据页编辑 | 百分比数值；最新非空派生 | 固定字段，未测量为 null | `body-metrics.json` | 规划中 |
| `BodyMetricDoc.measurementsCm` | 围度当前值、左右差值、趋势 | 固定围度表单 | 固定键校验；左右成对围度双序列派生 | 所有固定键写出，未测量为 null；拒绝自由键 | `body-metrics.json` | 规划中 |
| `BodyMetricDoc.note` | 单条测量备注 | 身体数据表单 | trim 后空为 null；不得创建 note-only 记录 | 固定字段 | `body-metrics.json` | 规划中 |
| `TimelineNoteDoc.id` 与 BaseDoc 元字段 | 日历/趋势标记、备注列表 | 新增、编辑、删除备注 | P6 service 统一创建与 tombstone | 新增 `DataSnapshot.timelineNotes`，初始化空数组，静态分片 `timeline-notes.json` | `timeline-notes.json` | 规划中 |
| `TimelineNoteDoc.content` | 备注详情，短摘要由截断生成 | 备注表单 | trim 与长度校验；不新增 title/category/tag | 固定字段 | `timeline-notes.json` | 规划中 |
| `TimelineNoteDoc.rangeType/startDate/endDate` | 日历和趋势标记 | 备注表单 | single_day/date_range/open_ended 规则；`startDate <= today`；查询裁剪到 today 后闭区间重叠 | 使用 ISODate，不升级为时间戳 | `timeline-notes.json` | 规划中 |
| `TimelineNoteDoc.workoutId` | 可选来源训练跳转 | 可选关联训练 | 为空或解析有效训练；不支持 exerciseId/templateId | 保留引用，不自动写入 WorkoutDoc | `timeline-notes.json` | 规划中 |
| `ExercisePerformanceRecordDoc.id` 与 BaseDoc 元字段 | 动作详情、统计摘要、分享图摘要 | 用户不手动编辑；仅可重算/清空重建 | P7 service/core 确定性生成刷新事件 | 新增 `DataSnapshot.exercisePerformanceRecords`，按月分片 `exercise-performance/YYYY-MM.json` | 月分片 | 规划中 |
| `ExercisePerformanceRecordDoc.exerciseId` | 动作详情归属、统计 Top 提升 | 不编辑 | P1 替代链解析归并，保留来源引用 | 固定引用 | 月分片 | 规划中 |
| `kind/metricType/value/unit` | 指标名称、当前最佳、周期摘要 | 不编辑 | 真实 PR 与基于 RPE 修正 RM 比较；规范单位 kg/kg_reps/m/sec/m_per_sec/reps | 枚举校验；展示单位转换不改写规范值 | 月分片 | 规划中 |
| `achievedAt` | 周期归属、排序、分片归属 | 不编辑 | 来源训练/组达成时间；不得未来 | 决定 `exercise-performance/YYYY-MM.json` | 月分片 | 规划中 |
| `sourceWorkoutId/sourceWorkoutExerciseId/sourceSetId` | 来源跳转 | 不编辑 | 训练编辑/删除后重算或清理 | 保留来源引用；不保存名称快照 | 月分片 | 规划中 |
| `input` 与 `rm` | RM 摘要、公式展开、分享摘要 | 不编辑 | 保存必要输入摘要和四公式结果；RIR 不持久化 | 真实 PR 的 `rm=null`，RM 事件必填公式明细 | 月分片 | 规划中 |
| 分享图 PNG | 训练详情预览、保存、系统分享 | 运行时“显示明细”选项 | P4.1 生成本地输出物 | 不进入 DataSnapshot、manifest 或 Settings；不新增默认偏好 | 不同步 | 规划中 |

## 4. LocalJsonRepository

文件：`frontend/src/repositories/localJsonRepository.ts`。

### 4.1 初始化

```text
getSnapshot()
  -> DocumentStore.load()
  -> makeEmptySnapshot(deviceId)（空存储时）
  -> migrateSnapshot(raw, deviceId)
  -> DocumentStore.save(snapshot)
```

设备 ID 用 `localStorage` 中的 `ironlog_device_id` 保存；在 Android WebView 中它仅用于 manifest 标识，不是账户标识。

### 4.2 CRUD 规则

| 操作 | Repository 行为 |
|---|---|
| create | `withDoc` 生成 string ID、createdAt、updatedAt、deletedAt=null、schemaVersion=1。 |
| update | `withoutUndefined` 后合并；`undefined` 不覆盖旧字段；更新 `updatedAt`。 |
| delete | `tombstone` 写 `deletedAt` 和 `updatedAt`。 |
| list/get | 默认过滤 tombstone；需要历史/同步时显式 `includeDeleted`。 |
| mutate | 更新 manifest.updatedAt，重建 manifest.shards，调用 DocumentStore.save。 |

训练创建/更新还有额外规则：若嵌套动作或组 ID 为 `""`，repository 生成新 ID。复制训练必须显式把来源嵌套 ID 置空，不能只复制数组引用。

### 4.3 原子性限制

当前 repository 是内存快照 + 全量相关分片写入模型；它没有跨文件事务或本地临时文件发布。一次写入中断时可能留下部分新分片和旧 manifest。当前恢复策略是以 manifest 为读取入口；后续要提高可靠性时，应引入本地临时文件、校验和或写入日志，而不是在页面中补偿。

---

## 5. schema migration

文件：`frontend/src/core/migrations.ts`。

### 5.1 当前行为

`CURRENT_SCHEMA_VERSION = 1`。`migrateSnapshot` 的职责是：

1. 创建完整默认快照。
2. 合并可读取的 raw snapshot。
3. 对 profile/settings/数组文档补齐 BaseDoc 元字段。
4. 按内置动作固定 ID 增量合并 `DEFAULT_EXERCISES`：空库写入完整默认库；非空库补齐缺失内置动作，并以代码定义修正同 ID 内置动作的受控分类和 type。
5. 为历史 `WorkoutExerciseDoc` 补齐 `exerciseType`：优先保留已有合法快照，其次读取关联动作的当前 type，最后依据已有组中的距离/时长字段推断；归一化后写回本地和同步分片。
6. 重新计算 manifest.shards。

自定义动作不参与默认数据覆盖。历史数据中的 `ExerciseDoc.replacedByExerciseId` 缺失时归一化为 `null`；该字段与 tombstone 一起保留在 exercises.json，供其他设备和读取侧解析动作替代链。

这是一种“归一化 v1”而非可追溯的多版本迁移链。当前分支明确不迁移历史 FastAPI/PostgreSQL 数据或旧的大训练索引数据；已有 IndexedDB/模拟器数据必须经归一化后增量保留，不能因默认动作或字段演进而要求清空。

### 5.2 新版本迁移规范

当 `schemaVersion` 从 1 增加时，必须：

1. 在 `models.ts` 定义新字段和默认/空值语义。
2. 新增显式 migration 分支，例如 `v1 -> v2`，不能仅靠对象展开。
3. 同时迁移静态文件和 workout 月分片。
4. 保留 tombstone 与未知字段的兼容策略。
5. 添加旧快照升级测试、无数据初始化测试和同步后加载测试。
6. 更新 manifest schemaVersion，并在发布说明描述不可逆风险。

第 3.5 节的动作详情元数据不采用这条 migration 路径：本轮仅重置不兼容的开发/虚拟机测试快照，并从新默认数据初始化。

动作详情元数据在本轮开发不进入 v1 -> v2 migration。验证时应重置不兼容的 IndexedDB/Android 虚拟机测试快照，再确认新快照中的目标肌群字段和 description 能随 `exercises.json` 往返。真实用户数据保留方案属于未来独立决策，不能在本轮以静默清空替代。

P6 实施时直接用 `BodyMetricDoc` 替换旧 `ProfileDoc.height` / `ProfileDoc.weight` 身体数据模型。当前阶段不迁移旧开发/虚拟机测试数据，不在读取层兼容旧字段，也不把旧 Profile 单值生成初始身体记录；验证时按运行指南清空 IndexedDB 或 Android 模拟器旧测试数据后重建。这个规则只适用于当前开发测试阶段，不能作为面向真实用户发布的数据保留策略。

### 5.3 导入/导出边界

DocumentStore 已提供 `exportFiles` / `importFiles` 辅助函数，用于把 snapshot 转为逻辑文件集合或反向解析。当前没有用户 UI、文件选择器、完整格式校验或覆盖确认流程；不得在文档中宣称“已提供数据导入导出”。

后续导入流程至少要：校验 manifest、校验路径白名单、执行 migration、显示将覆盖/合并的文档数量、创建本地备份，再调用 `replaceSnapshot`。

---

## 6. 关键测试与验收

现有测试文件包括：

| 文件 | 覆盖方向 |
|---|---|
| `src/core/migrations.test.ts` | 默认快照、元字段和分片列表。 |
| `src/platform/documentStore.test.ts` | 文件/快照转换和分片持久化约束。 |
| `src/services/plan.test.ts` | 计划规则的服务层约束。 |

修改本模块后至少增加/执行：

```powershell
cd D:\workspaces\vscodeWorkspace\project\bodybuilding\ironlog\frontend
npm test
npm run build
```

人工验收：

- Web 开发刷新后训练仍可读取。
- 训练写入正确月分片，不生成全量 `workouts.json`。
- 删除训练后常规列表隐藏、分片保留 tombstone。
- 更新设置不把 `undefined` 写成字段值。
- Android 环境下由 Capacitor Filesystem 读取同一逻辑文件结构。
- 动作详情元数据在清空不兼容的 IndexedDB/Android 虚拟机测试数据后，从新默认快照初始化；`exercises.json` 往返后目标肌群数组顺序不变，description 保持纯文本。
- P6/P7 实现后，`body-metrics.json`、`timeline-notes.json`、`exercise-performance/YYYY-MM.json` 必须进入 manifest、DocumentStore export/import、WebDAV pull/merge/push、backup 与 tmp/MOVE 往返；分享图 PNG 不进入 manifest。
