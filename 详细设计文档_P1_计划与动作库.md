# 详细设计文档 P1：计划、模板与动作库

> 当前版本：本地 JSON 文档模型  
> 最后更新：2026-06-11  
> 文档用途：指导动作库、训练计划、模板、日历生成和模板过滤相关开发。

---

## 1. 目标

P1 提供个人训练编排能力：

- 默认动作库。
- 自定义动作。
- 动作历史。
- 训练计划。
- 训练模板。
- weekly/cyclic/flexible 编排。
- 今日计划。
- 日历条目。
- 模板驱动的训练动作过滤。

所有数据均来自本地 JSON，不依赖用户账号或服务端。

---

## 2. 相关文件

页面：

```text
src/pages/PlansPage.tsx
src/pages/PlanCreatePage.tsx
src/pages/PlanDetailPage.tsx
src/pages/PlanEditPage.tsx
src/pages/TemplateEditPage.tsx
src/pages/CalendarPage.tsx
src/pages/HomePage.tsx
src/pages/ExerciseDetailPage.tsx
```

服务：

```text
src/services/plan.ts
src/services/exercise.ts
src/services/localMappers.ts
```

数据：

```text
src/core/models.ts
src/core/defaultData.ts
src/repositories/localJsonRepository.ts
```

---

## 3. 动作库设计

### 3.1 ExerciseDoc

```ts
interface ExerciseDoc {
  id: string;
  name: string;
  category: string;
  type: string;
  description: string | null;
  metValue: number | null;
  isCustom: boolean;
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null;
  schemaVersion: number;
}
```

### 3.2 默认动作

默认动作由 `src/core/defaultData.ts` 初始化。

要求：

- 默认动作 id 必须稳定。
- 不要每次启动生成新 id。
- 默认动作可用于模板和历史引用。

### 3.3 自定义动作

创建自定义动作：

```ts
createExercise({
  name,
  category,
  type,
  description
})
```

规则：

- `isCustom = true`。
- `metValue` 可为空。
- 删除使用 tombstone。

### 3.4 动作分类

当前分类：

```text
chest
back
legs
shoulders
arms
core
cardio
compound
```

新增分类时必须同步：

- `CATEGORY_LABELS`
- 默认动作数据。
- 筛选 UI。

---

## 4. 计划设计

### 4.1 TrainingPlanDoc

```ts
interface TrainingPlanDoc {
  id: string;
  name: string;
  description: string | null;
  color: string;
  mode: "weekly" | "cyclic" | "flexible";
  cycleLength: number | null;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null;
  schemaVersion: number;
}
```

### 4.2 PlanSummary

列表页需要：

```ts
{
  id,
  name,
  description,
  mode,
  cycle_length,
  color,
  is_active,
  template_count,
  created_at
}
```

`template_count` 由本地 templates 聚合，不存入 plan doc。

### 4.3 启停计划

`updatePlan(id, { is_active })` 必须是 partial update。

关键规则：

- 未传字段不得覆盖为 undefined。
- 停用计划不参与日历自动生成。
- 停用计划下既有 workout 仍保留历史展示。

---

## 5. 模板设计

### 5.1 TemplateDoc

```ts
interface TemplateDoc {
  id: string;
  planId: string;
  name: string;
  sortOrder: number;
  color: string | null;
  scheduleRule: Record<string, unknown> | null;
  exercises: TemplateExerciseDoc[];
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null;
  schemaVersion: number;
}
```

### 5.2 TemplateExerciseDoc

```ts
interface TemplateExerciseDoc {
  id: string;
  exerciseId: string;
  sortOrder: number;
  note: string | null;
}
```

要求：

- 模板动作引用 ExerciseDoc。
- 模板动作删除不影响 ExerciseDoc。
- 模板删除不影响历史 WorkoutDoc。

### 5.3 模板编辑保存

保存时：

```ts
updateTemplate(planId, templateId, {
  name,
  color,
  schedule_rule,
  exercises: [...]
})
```

规则：

- exercises 使用完整替换。
- sort_order 按当前列表顺序重算。
- note 空字符串转 null。
- exercise_id 必须是 string。

---

## 6. 编排模式

### 6.1 weekly

规则：

```json
{ "day_of_week": [1, 3, 5] }
```

生成逻辑：

- 周一 = 1。
- 周日 = 7。
- 仅 active plan 生效。
- 不生成早于 plan.createdAt 的条目。

### 6.2 cyclic

规则：

```json
{ "day_in_cycle": 1 }
```

参数：

- `cycleLength` 表示周期天数。
- 模板按 sortOrder 排序。

当前本地第一版：

- 从今天或计划创建日开始推导。
- 根据 day_in_cycle gap 生成下一模板。
- 已完成 workout 会额外注入日历，避免历史丢失。

后续必须改进：

- 抽到 `core/calendar.ts`。
- 用最近完成模板驱动下一次生成。
- 增加 cyclic 单元测试。

### 6.3 flexible

规则：

- 不自动生成日历条目。
- 可通过手动安排或直接训练产生历史。

---

## 7. 日历设计

`getCalendar(from, to)` 输入为闭区间日期字符串。

数据来源：

1. active plans。
2. plan templates。
3. workouts。
4. manual schedule entries。

输出：

```ts
interface CalendarDay {
  date: string;
  entries: CalendarEntry[];
}
```

### 7.1 CalendarEntry

```ts
interface CalendarEntry {
  id: string;
  plan_id: string;
  plan_name: string;
  plan_color: string;
  plan_mode: string;
  template_id: string;
  template_name: string;
  template_color: string | null;
  template_exercise_ids: string[];
  scheduled_date: string;
  is_completed: boolean;
  workout_id: string | null;
}
```

### 7.2 完成状态

完成判断：

```text
workout.planTemplateId === template.id && workout.date === scheduled_date
```

注意：

- workout 可能来自自动生成条目。
- workout 也可能来自手动训练后注入。
- 停用计划不再自动生成，但历史 workout 不删除。

### 7.3 月统计口径

CalendarPage 可加载：

```text
calStart~calEnd
```

本月训练次数必须过滤：

```text
monthStart~monthEnd
```

禁止把日历补齐日期算入本月统计。

---

## 8. 模板过滤

WorkoutCreatePage 有两个模板入口：

1. URL 参数 `template_id`。
2. 页面内计划/模板选择器。

激活模板后：

```ts
templateExerciseIds = new Set(activeTemplate.exercises.map((te) => te.exercise_id));
```

动作列表：

```ts
allExercises.filter((exercise) => templateExerciseIds.has(exercise.id))
```

验收：

- 非模板动作不渲染。
- 搜索和分类筛选在模板过滤之后继续生效。
- 清除模板后恢复全部动作。

---

## 9. 手动排程

`scheduleTemplate(templateId, date)` 创建 `ManualScheduleEntryDoc`：

```ts
{
  planId,
  templateId,
  scheduledDate,
  isCompleted: false,
  workoutId: null
}
```

日历合并时：

- 手动条目覆盖同模板自动条目。
- 手动条目不影响模板定义。

---

## 10. 边界 case

- 无计划时首页不显示今日计划，但开始训练可用。
- 计划无模板时详情可打开。
- 模板无动作时可保存，但训练入口过滤后显示空列表。
- 删除模板后，历史 workout 仍通过 planTemplateId 尽力显示。
- 停用计划后，不再生成未来日历条目。
- flexible 计划不自动生成条目。
- weekly 多天排程可生成多个日期。
- cyclic 无完成记录时从第一模板开始。

---

## 11. 禁止事项

- 不得恢复 `/plans` `/exercises` HTTP API。
- 不得新增 number id。
- 不得把 plan/template/exercise 拆成 SQL 表思维。
- 不得用 sort 替代模板过滤。
- 不得物理删除计划/模板/动作。
- 不得让停用计划影响历史 workout。

---

## 12. 开发验收清单

- [ ] 创建计划成功。
- [ ] 编辑计划 partial update 不破坏未传字段。
- [ ] 启停计划影响日历生成。
- [ ] 添加模板成功。
- [ ] 编辑模板动作和备注成功。
- [ ] 删除模板使用 tombstone。
- [ ] weekly 日历生成正确。
- [ ] flexible 不自动生成。
- [ ] 模板过滤两入口一致。
- [ ] 动作历史来自本地 workouts。
