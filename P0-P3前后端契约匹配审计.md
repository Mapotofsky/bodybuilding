# P0-P3 前后端契约匹配审计

> 审计日期：2026-06-26
>
> 范围：P0 核心训练、P1 计划/模板/动作库/日历、P2 本地文档存储与迁移、P3 WebDAV 同步与 Android 平台。
>
> 本版更新：吸收产品人工确认结论，复核 A5，新增“模板训练中新建动作未写回模板”的问题；2026-06-27 已完成第一阶段 A1/A2/A3/A6/A12 修复并补齐防再犯措施。

---

## 1. 审计口径

证据标记：

- [已验证]：已直接核对当前代码、当前文档或用户提供截图。
- [推断]：由已验证证据推导出的用户影响或工程风险。
- [产品确认]：来自 2026-06-26 人工确认的产品决策。

严重程度：

| 等级 | 含义 |
|---|---|
| 高 | 会造成统计错误、用户明确操作无效、数据解释错误，或常规路径无法完成产品确认的核心行为。 |
| 中 | 功能已在一端实现但另一端缺口明显，影响一致性、同步稳定性或后续扩展。 |
| 低 | 预留字段、文档标注项或短期可接受的体验缺口。 |

修订紧迫性：

| 等级 | 含义 |
|---|---|
| 立即 | 应优先于新的 P0-P3 功能开发修复。 |
| 近期 | 应进入下一轮 P0-P3 修订，避免继续扩大不一致。 |
| 排期 | 可进入后续迭代，但需要文档标注或测试护栏。 |
| 撤销 | 复核后不再作为当前缺陷追踪。 |

---

## 2. 产品确认结论

[产品确认] 体重字段固定为 kg；`SettingsDoc.weightUnit` 只表示训练重量单位，不影响体重展示。

[产品确认] 训练组录入区增加“热身”、“力竭”和 RPE 的紧凑控件；删除 `isDropset` 字段。递减组可以从训练记录本身体现，不再作为独立布尔字段。训练容量不排除热身组。

[产品确认] `supersetGroup` 暂不做前端设计，仅作为预留字段保留；当前目标是修复短期缺陷。

[产品确认] 自定义动作编辑/创建应抽出共享组件。

[产品确认] CalendarPage 不体现计划条目；文档需明确“日历当前仅显示训练记录”。

[产品确认] SyncPage 增加“清除同步配置”。

[产品确认] `metValue` 属于早期设计漏洞。MET 用于力量训练能量消耗估算过于粗糙，删除该字段，不再考虑能量消耗估算功能。

[产品确认] 增加头像管理功能；头像和训练记录一样通过 WebDAV 同步。

---

## 3. 总体现状

[已验证] P0-P3 主链路仍成立：页面经 `services/*` 到 `LocalJsonRepository`，持久化模型集中在 `frontend/src/core/models.ts`，WebDAV 同步通过 `settings.json`、manifest 和 JSON 分片工作。

[已验证] 当前最需要优先修正的是：

1. 训练重量单位设置无 UI 闭环，且容量统计未按组单位换算。
2. 训练组模型字段与前端录入控件不一致，尤其是热身、力竭、RPE 和待删除的 `isDropset`。
3. 模板训练中新建动作没有写回模板，导致“模板过滤动作”和“训练中新建动作”语义冲突。
4. 模板动作嵌套 ID 保存不稳定，影响后续 WebDAV 合并和数据演进。

[推断] A5 原始表述中“已持久化 workout 后清除模板导致保留旧模板”的场景在当前 UI 不成立，应撤销为活跃缺陷；但 `undefined` 与 `null` 的清空语义仍应保留为测试护栏。

---

## 4. 问题清单

| 编号 | 范围 | 当前结论 | 严重程度 | 修订紧迫性 |
|---|---|---|---|---|
| A1 | P0/P2/P3 | `SettingsDoc.weightUnit` 已定义并参与个人动作统计与同步合并，但没有训练重量单位设置入口；训练创建/编辑页默认 `kg`，不读取用户偏好。体重字段固定 kg。 | 高 | 立即 |
| A2 | P0 | 训练容量汇总直接累加 `weight * reps` 并固定显示 kg，未按每组 `unit` 换算；列表、首页、日历、详情、分享均受影响。容量按产品确认包含热身组。 | 高 | 立即 |
| A3 | P0/P2/P3 | 训练组应支持热身、力竭和 RPE 输入；当前页面没有输入控件。`isDropset` 已确认删除，但当前模型、类型、mapper 和 service 仍保留。 | 高 | 立即 |
| A4 | P0 | `supersetGroup` 作为预留字段暂不做前端设计。当前短期要求是不新增 UI，但读后保存不应意外破坏未来或同步导入数据。 | 低 | 排期 |
| A5 | P0/P1 | 原问题“已持久化 workout 后清除模板导致旧模板保留”经复核不成立。当前 UI 只有 `isFirstSelect=true` 的准备页显示“清除”，完成组并进入换动作页后没有该按钮。 | 无 | 撤销 |
| A6 | P1/P2 | `TemplateExerciseDoc.id` 已定义，但 `TemplateEditPage` 本地状态不保留，保存时所有模板动作条目重新生成 ID。 | 中 | 近期 |
| A7 | P1/P2 | 自定义动作创建入口能力不一致；应抽出共享组件统一动作库、模板编辑、训练中新建动作的创建/编辑体验。 | 中 | 近期 |
| A8 | P1 | CalendarPage 当前只显示训练记录，不显示计划条目。产品确认 UI 不体现计划条目，需在文档中明确限制。 | 低 | 近期 |
| A9 | P3 | `removeSecret` 能力存在，但 SyncPage 没有“清除同步配置”入口。产品确认需要增加。 | 低 | 近期 |
| A10 | P1/P2 | `ExerciseDoc.metValue` 属于早期设计漏洞，需删除字段和相关类型、默认数据、mapper、测试，不再做能量消耗估算。 | 中 | 近期 |
| A11 | P2/P3 | `ProfileDoc.avatarUrl` 存在但页面无头像管理。产品确认需要新增头像管理，并与训练记录一样走 WebDAV 同步。 | 中 | 近期 |
| A12 | P1/P0 | 模板训练中通过动作选择器“新建”动作时，新动作应同时加入当前模板；当前代码只创建 ExerciseDoc 并更新本地动作列表，不调用模板更新。初次选择模板和训练中“换动作”两条路径都受影响。 | 高 | 立即 |

---

## 5. 逐项说明

### A1. 训练重量单位设置没有 UI 闭环

[已验证] `SettingsDoc` 定义了 `weightUnit: "kg" | "lb"`，默认值为 `kg`，并会随 settings 同步合并。`ExerciseDetailPage` 的个人统计通过 `buildExercisePersonalStats` 读取该设置后换算展示。

[已验证] `ProfilePage` 只调用 `getProfile/updateProfile`，没有读取或更新 settings。`WorkoutCreatePage` 与 `WorkoutEditPage` 各自维护本地 `weightUnit` state，初始值为 `kg`。

[产品确认] 体重字段固定 kg，训练重量单位设置不影响体重。

建议：

- 新增 `services/settings.ts`，页面通过 service 读取/更新 settings。
- 在个人页或设置页增加“训练重量单位”。
- WorkoutCreatePage、WorkoutEditPage 默认读取 `SettingsDoc.weightUnit`；单次训练内仍允许临时切换每组单位。
- ProfilePage 保持体重固定 kg，并在文案中避免暗示它受训练重量单位影响。

### A2. 容量统计没有按组单位换算

[已验证] `WorkoutSummary.total_volume`、`shareWorkout.total_volume`、WorkoutDetailPage、WorkoutCreatePage finish 统计均直接使用 `(set.weight || 0) * (set.reps || 0)`。多个页面固定显示 `kg` 或 `容量(kg)`。

[已验证] `WorkoutSetDoc.unit` 已保存到每组，且 P0 个人动作统计中已有固定换算常量 `1 lb = 0.45359237 kg`。

[产品确认] 容量不排除热身组。

[推断] 一组 `100 lb x 5` 当前会被汇总为 `500 kg` 容量展示，而正确 kg 容量约为 `226.8 kg`。首页、训练列表、日历、详情和分享文案都会受影响。

建议：

- 将重量换算和训练容量计算下沉到 `core`，例如 `core/workoutMetrics.ts`。
- 统一定义训练总容量：包含热身组，按每组 unit 换算到展示单位。
- 所有 summary、detail、share 和 finish 页使用同一计算函数。
- 输出值必须携带 `displayUnit`，不得再用裸 number 加固定 `kg`。

### A3. 训练组高级字段与产品确认不一致

[已验证] `WorkoutSetDoc` 当前包含 `rpe`、`isWarmup`、`isDropset`、`isFailure`、`restSeconds`。service 校验 RPE 与休息秒数，mapper 能保留这些字段。

[已验证] WorkoutCreatePage 新增组时把 `rpe` 写为 `null`，`isWarmup/isDropset/isFailure` 写为 `false`，没有控件让用户修改。WorkoutEditPage 会保留读到的字段，但也没有控件修改。WorkoutDetailPage 只显示 `isWarmup` 的 W 标识、`rpe` 和休息秒数。

[产品确认] 增加“热身”、“力竭”和 RPE 的紧凑控件；删除 `isDropset`。递减组从训练记录中体现，不独立建模。容量不排除热身组。

建议：

- 从 `WorkoutSetDoc`、页面兼容类型、service payload、mapper、默认 set、测试 fixture 中删除 `isDropset`。
- 如存在旧数据，migration/读取归一化应忽略该字段，不再写回。
- 在组录入区增加“热身”、“力竭”和 RPE 的紧凑控件。
- 编辑页必须能修改这些字段，详情页和动作历史应显示热身、力竭、RPE。
- 更新个人动作统计口径：热身组是否排除表现指标可独立讨论；训练总容量按产品确认包含热身组。

### A4. supersetGroup 仅作为预留字段

[已验证] `WorkoutExerciseDoc.supersetGroup`、页面兼容类型 `superset_group`、`toWorkoutExercise` 和 `workoutToPayload` 都有该字段。

[已验证] `WorkoutEditPage` 的 `LocalExercise` 没有 `superset_group`，读取 workout 时不保存该字段，保存 payload 时也不传。`normalizeWorkoutPayload` 对缺失值使用 `exercise.superset_group ?? null`，因此编辑会把已存在的 superset 分组归一为 `null`。

[产品确认] 暂时不做 superset 前端设计，字段预留。

建议：

- 本轮不新增 superset UI。
- 在文档中明确它是预留字段，不进入当前验收。
- 若保留字段，编辑保存应原样保留现有 `superset_group`，避免同步导入或未来数据被当前页面清空。

### A5. 复核后撤销：清除模板场景当前不成立

[已验证] `handleClearTemplate()` 只清空 `activeTemplate/activeTemplateId/expandedPlanId`。

[已验证] “清除”按钮位于 WorkoutCreatePage 的准备页计划区块中，该区块受 `isFirstSelect && activePlans.length > 0` 约束。第一次开始训练后 `setIsFirstSelect(false)`；完成一组后点击“换动作”只 `setPhase("select")`，不会恢复 `isFirstSelect=true`。

[已验证] 用户提供的图 1 显示准备页有“清除”，图 2 显示换动作页无“清除”。这与代码路径一致。

结论：

- 原 A5 中“已持久化 workout 后清除模板导致 update 传 undefined 保留旧模板”的具体场景不成立，撤销为当前缺陷。
- `note: note || undefined`、`mood: extra?.mood ?? undefined` 的清空语义仍建议作为 service/page 测试护栏保留，但当前没有证据表明它构成同等级短期缺陷。

### A6. TemplateExerciseDoc.id 保存时不稳定

[已验证] `TemplateExerciseDoc` 有 `id`，repository 在缺少 ID 时会分配 ID。

[已验证] `TemplateEditPage` 的 `LocalTemplateExercise` 不包含模板动作条目自身 `id`，从 `tmpl.exercises` 映射时丢弃 `te.id`，保存时只传 `exercise_id/sort_order/note`。`services/plan.ts` 的 `updateTemplate` 会把缺少 ID 的条目写成 `id: ""`，repository 再生成新 ID。

[推断] 每次保存模板都会让模板动作嵌套 ID 全量变化，造成无意义的分片更新时间、同步 LWW 噪音，也违背“嵌套 ID 稳定”的数据演进要求。

建议：

- `TemplateExerciseCreate` 支持可选 `id`。
- `LocalTemplateExercise` 增加 `id`。
- 读取时保留 `te.id`，新增动作使用空 ID，保存时把已有 ID 传回 service。
- 增加测试：仅修改模板名称或备注时，未变更动作条目的 ID 不变。

### A7. 自定义动作创建入口应抽出共享组件

[已验证] 动作库入口创建自定义动作时可填写 `name/category/type/description`。共享 `ExercisePicker` 创建入口只填写 `name/category/type`。

[已验证] 动作详情编辑页能编辑目标肌群和 description；service 也能校验主/次目标肌群。

[产品确认] 抽出共享组件。

建议：

- 抽出共享的自定义动作创建/编辑组件，统一动作库、模板编辑、训练创建、训练编辑中的动作创建能力。
- 该组件至少覆盖名称、分类、记录类型、description；目标肌群可在快速创建模式中折叠展示，但字段路径应一致。
- 与 A12 联动：训练中 active template 下创建动作成功后，应由页面或 service 负责同步加入当前模板。

### A8. CalendarPage 只显示训练记录

[已验证] `getCalendar(from, to)` 已能组合 weekly/cyclic 虚拟条目和实际训练完成状态，HomePage 使用它展示今日计划和补训建议。

[已验证] CalendarPage 仍只调用 `getWorkouts({ from, to })`，只渲染实际训练记录。

[产品确认] UI 不体现计划条目，文档明确“日历当前仅显示训练记录”。

建议：

- 不把 CalendarPage 未接入计划条目作为代码缺陷。
- 更新概要/P1/运行说明中涉及日历的表述，避免宣称日历可查看计划条目或未来计划。
- 保留月统计边界规则：日历网格可查询相邻月训练，但“本月训练次数”只统计当月。

### A9. SyncPage 需要清除同步配置

[已验证] `DocumentStore` 与 repository 提供 `removeSecret`，SyncPage 保存新密码时可写 secret。

[已验证] SyncPage 没有“清除 WebDAV 密码/删除配置”按钮。

[产品确认] 增加“清除同步配置”。

建议：

- 增加“清除同步配置”按钮，并使用 ConfirmDialog 确认。
- 若存在 `passwordRef`，调用 `localRepository.removeSecret(passwordRef)`。
- 写入 settings：`webdav.url=""`、`webdav.username=""`、`webdav.passwordRef=null`；`lastSyncAt` 是否清空需产品决定。
- 远端 `settings.json` 继续清空 `passwordRef` 与 `lastSyncAt`。

### A10. 删除 metValue

[已验证] `ExerciseDoc.metValue` 存在，默认数据和自定义创建均写为 `null`。当前页面不展示、不编辑，也没有统计使用。

[产品确认] MET 用于力量训练能量消耗估算过于粗糙，属于早期设计漏洞；删除 `metValue`，不再考虑能量消耗估算功能。

建议：

- 删除 `core/models.ts`、`types/index.ts`、`defaultData.ts`、`localMappers.ts`、`services/exercise.ts`、测试 fixture 中的 `metValue/met_value`。
- 删除文档中任何能量消耗估算、MET 扩展或卡路里规划表述。
- 删除前按 M2 全文搜索所有引用。

### A11. 增加头像管理并走 WebDAV 同步

[已验证] `ProfileDoc.avatarUrl` 和页面兼容类型 `avatar_url` 存在。ProfilePage 固定显示本地图标，不提供头像选择、清除或本地文件保存。

[产品确认] 增加头像管理功能，和训练记录一样采用 WebDAV 同步。

建议：

- 明确头像是二进制资源还是 base64/逻辑文件引用。优先建议二进制资源分片，避免把大 base64 写入 `profile.json`。
- 设计本地路径，例如 `assets/avatar/<profile-id>.<ext>`，并在 `profile.json` 中保存稳定引用。
- DocumentStore 需要支持资源文件导出/导入或新增头像专用逻辑文件。
- WebDAV 同步需要纳入 manifest、tmp/MOVE、backup 和删除策略。
- ProfilePage 提供选择、预览、替换、清除入口；Android 需确认文件选择和权限路径。

### A12. 模板训练中新建动作未写回模板

[已验证] 用户截图显示 active template 下的动作选择页存在“新建”入口；图 1 是准备训练页，图 2 是训练中“换动作”返回的选择页。

[已验证] `WorkoutCreatePage` 在 active template 下使用：

```ts
const selectableExercises = activeTemplate
  ? allExercises.filter((exercise) => templateExerciseIds.has(exercise.id))
  : allExercises;
```

[已验证] `ExercisePicker` 创建动作后只调用 `createExercise`、`onCreated(exercise)`、`onSelect(exercise)`。WorkoutCreatePage 的 `onCreated` 只执行：

```ts
setAllExercises((previous) => [...previous, exercise]);
setSelectedExercise(exercise);
```

[已验证] WorkoutCreatePage 没有调用 `updateTemplate`，也没有刷新 `activeTemplate` 或 `planTemplatesCache`。因此新建动作不会写入当前模板。

[推断] 如果 active template 仍是真过滤，那么新建动作不写回模板会造成语义冲突：用户在“已过滤动作”环境下创建动作，却不会进入模板动作集合；下一次打开同一模板训练时，该动作不会出现。当前代码还存在 `onCreated` 与 `onSelect` 同时设置选中状态的潜在交互风险，应在实现时一并验证。

建议：

- 在 `services/plan.ts` 增加专用函数，例如 `appendExerciseToTemplate(templateId, exerciseId)`，避免页面手动拼完整模板。
- active template 下创建动作成功后，同时把动作追加到当前模板，`sort_order` 为当前模板动作数量。
- 更新 `activeTemplate`、`planTemplatesCache` 和 `allExercises`，保证当前页立即可选。
- 两条入口必须覆盖：
  - 准备训练页选择模板后点击“新建”。
  - 完成至少一组后点击“换动作”，在选择页点击“新建”。
- 与 A6 联动：追加模板动作时必须保留既有模板动作 ID，只给新增项分配新 ID。

---

## 6. 修订计划

### 第一阶段：立即修复短期缺陷

阶段入口条件：

- 先完成 A1/A2/A3/A6/A12 涉及字段的字段契约矩阵。
- 先在 P0/P1 详细设计中确认容量计算口径、训练重量单位口径、模板训练中新建动作写回模板口径。
- 先提出项目 AGENTS.md 最小规则补丁，至少覆盖字段契约矩阵、读后原样保存、null/undefined、计算单一权威、删除字段零残留。

1. 重量单位与容量统计。
   - 新增 settings service 和训练重量单位 UI。
   - 训练创建/编辑默认读取 `SettingsDoc.weightUnit`。
   - 抽出统一容量计算，按每组 unit 换算，训练总容量包含热身组。
   - 同步落地防再犯措施：先写 core 容量计算单测和 kg/lb fixture，再替换页面散落计算。

2. 训练组字段重整。
   - 删除 `isDropset` 字段及所有映射、默认值和测试引用。
   - 创建/编辑页增加热身、力竭、RPE 控件。
   - 详情页和历史记录显示热身、力竭、RPE。
   - 同步落地防再犯措施：补含 warmup、failure、rpe、restSeconds 的非默认 fixture；删除后执行 `isDropset/is_dropset` 零残留搜索。

3. 模板训练中新建动作写回模板。
   - 增加追加模板动作的 service。
   - 覆盖准备页和换动作页两条新建路径。
   - 当前 active template 和缓存同步刷新。
   - 同步落地防再犯措施：补 active template 下新建动作追加模板的 service 测试和页面走查清单。

4. 模板动作 ID 稳定。
   - `TemplateExerciseCreate` 支持可选 id。
   - TemplateEditPage 保存时保留既有 id。
   - 同步落地防再犯措施：补 TemplateEditPage 读后原样保存测试，断言未变更模板动作 id 不变。

阶段出口条件：

- 第一阶段相关 fixture、单测、grep 和人工走查全部通过。
- 防再犯规则已进入项目 AGENTS.md 或形成待确认补丁；若未进入，必须在阶段验收记录中说明原因。

### 第二阶段：按产品确认补齐体验

1. 抽出共享自定义动作创建/编辑组件。
2. SyncPage 增加“清除同步配置”。
3. 删除 `metValue/met_value` 并清理文档。
4. 为头像管理设计本地资源文件和 WebDAV 同步方案，再实现 ProfilePage 管理入口。

防再犯落地项：

- 抽共享组件前，补动作创建字段契约，避免动作库、模板、训练入口再次分叉。
- 修复 A9 时补 `passwordRef` 的 null/undefined 清空语义测试。
- 删除 A10 后执行 `rg -n "metValue|met_value" src`，并同步清理默认数据、mapper、service payload、测试 fixture 和文档。
- 实现 A11 前先写头像资源同步设计；实现中补头像引用的 WebDAV 往返 fixture 和 ProfilePage 读后原样保存测试。

### 第三阶段：预留项与文档约束

1. `supersetGroup` 保持预留，不做前端设计；编辑保存应尽量原样保留。
2. CalendarPage 文档明确只显示训练记录，不显示计划条目。
3. 将 A5 从活跃问题移除，仅保留 `undefined/null` 清空语义测试。

防再犯落地项：

- 对 `supersetGroup` 补读后原样保存测试，确保预留字段不会被当前编辑页清空。
- 将“日历当前仅显示训练记录”迁入概要设计或 P1 详细设计，审计文档只保留追踪记录。
- 将 A5 对应的清空语义沉淀为测试规则，不再作为缺陷编号继续追踪。

---

## 7. 防止再犯的措施

### 7.1 落地分层

防止再犯措施分三层落实：

| 层级 | 作用 | 放置位置 | 本轮处理 |
|---|---|---|---|
| 开发文档 | 记录本轮 P0-P3 的字段契约、修订计划、验收矩阵和已知限制。 | 本审计文档、概要设计、P0-P3 详细设计。 | 立即补齐，先于对应代码修复。 |
| 项目 AGENTS.md | 约束本仓库未来所有同类开发行为。只写稳定、可复用、会改变未来动作的规则。 | `AGENTS.md` 的数据模型、前端实现、验证规则或文档规则。 | 本轮修复前先提出最小规则补丁；规则确认后再编辑。 |
| 可执行护栏 | 让规则在测试、grep、构建和人工走查中失败可见。 | `frontend/src/**/__tests__`、测试 fixture、脚本或验收命令。 | 随对应缺陷修复同步加入，作为合并前门禁。 |

不建议写入全局 AGENTS.md。A1-A12 都是 IronLog 的领域字段、同步链路和页面行为问题，属于项目级规则；只有“新增字段必须端到端追踪”“删除符号前全文搜索”这类跨项目通用原则才适合全局规则，而当前全局/项目规则中已经存在相近要求。

### 7.2 执行时机

| 措施 | 如何落实 | 何时落实 | 权威位置 |
|---|---|---|---|
| 字段契约矩阵 | 为本轮涉及字段补表，列出页面展示、页面编辑、service 输入、core doc、mapper、repository、migration、WebDAV 往返和当前状态。 | 修复 A1/A3/A4/A10/A11/A12 前先完成。字段未入矩阵不得开始实现。 | 本审计文档先记录；随后迁入对应 P0-P3 详细设计，作为长期权威。 |
| 新增/删除字段 M1 链路检查 | 每个字段改动前列出完整链路，改动后按链路逐项勾验。 | 每个字段 PR 或修复提交内同步完成。 | 项目 AGENTS.md 保留规则；具体字段链路写入设计文档或修复记录。 |
| 非默认字段回归 fixture | 建立包含 lb、warmup、failure、rpe、supersetGroup、模板动作稳定 id、active template 新建动作、settings、头像引用的样本。 | 修复对应缺陷时同步补。A1/A2 先补 kg/lb；A3 补组字段；A6/A12 补模板；A11 补头像。 | 测试 fixture 与单测是权威；审计文档只列覆盖项。 |
| null/undefined 语义测试 | 对可清空字段写测试：`undefined` 不修改，`null` 显式清空，页面清除动作必须传 `null`。 | 任何“清除/取消/未设置”功能修复时同步加入；A9 和 A11 必须覆盖。 | 单测是权威；项目 AGENTS.md 可增加一条通用规则。 |
| 读后原样保存测试 | 打开含非默认字段记录，不改该字段后保存，断言不相关字段不变。 | 修复 A4、A6、训练编辑、模板编辑、头像管理时同步加入。 | 单测或集成测试是权威；项目 AGENTS.md 可增加一条编辑页保存规则。 |
| 计算逻辑单一权威 | 把容量、单位换算、展示单位下沉到 core/service 纯函数，页面禁止重复手写。 | 修复 A2 前先定义函数与测试，再替换页面调用。 | core 函数和测试是权威；P0 详细设计记录口径。 |
| grep 零残留 | 删除字段后执行残留搜索，例如 `rg -n "metValue|met_value|isDropset|is_dropset" src`。 | 删除 A3/A10 字段后、提交前执行。 | 验收命令；项目 AGENTS.md 可补充删除字段残留检查规则。 |
| 人工走查清单 | 按“准备训练/换动作/保存/编辑/详情/WebDAV 往返”走查关键路径。 | 每个阶段修复完成后执行，不替代单测。 | 审计文档和对应阶段验收记录。 |

### 7.3 建议写入项目 AGENTS.md 的最小规则

以下规则稳定且会影响未来开发行为，建议进入项目 AGENTS.md，而不是只停留在本审计文档：

1. 新增、删除或重命名持久化字段前，必须先更新字段契约矩阵；未标明页面、service、core、mapper、repository、migration、同步状态的字段不得实现。
2. 页面编辑聚合文档时，必须通过读后原样保存测试保护未展示字段，特别是模板动作 ID、预留字段和同步导入字段。
3. 可清空字段必须区分 `undefined` 与 `null`：`undefined` 表示不修改，`null` 表示显式清空；页面“清除”操作不得用 `undefined` 代替。
4. 统计与单位换算必须有 core/service 单一权威实现，页面不得自行重复计算训练容量或拼接固定单位。
5. 删除持久化字段后必须全文搜索零残留，并覆盖默认数据、mapper、service payload、测试 fixture 和文档。

不建议写入项目 AGENTS.md 的内容：

- A1-A12 的具体问题编号和本轮修复顺序。这些属于审计文档和阶段计划。
- “体重固定 kg”“日历当前仅显示训练记录”等产品现状。这些应写入概要设计或对应 P0/P1 详细设计。
- 头像资源路径、模板追加 service 名称等实现细节。这些应写入设计文档和代码注释/测试。

### 7.4 字段契约矩阵

新增或删除持久化字段时，必须填写字段矩阵：

| 字段 | 权威文档 | 页面展示 | 页面编辑 | service 输入 | mapper | repository | migration | WebDAV 往返 | 当前状态 |
|---|---|---|---|---|---|---|---|---|---|
| `SettingsDoc.weightUnit` | P0/P2/P3 | 首页、列表、日历、详情、分享、完成页容量展示；个人动作统计 | ProfilePage 训练重量单位选择；体重仍固定 kg | `services/settings.ts` 读取/更新；训练创建/编辑默认读取 | settings 直接由 repository 读写；统计通过 core 换算 | `LocalJsonRepository.getSettings/updateSettings` | v1 默认 `kg` | `settings.json` 往返，远端不含密码引用 | 已实现 |
| `WorkoutSetDoc.unit` | P0/P2/P3 | 每组重量单位、容量统一展示单位 | 创建/编辑每组随当前训练单位写入，可切换 | `WorkoutSetPayload.unit` | `toWorkout`/summary/share 使用 core 换算 | WorkoutDoc 月分片 | 历史缺省按 `kg` | `workouts/YYYY-MM.json` 往返 | 已实现 |
| `WorkoutSetDoc.isWarmup` | P0/P2/P3 | 详情、历史、编辑显示热身标记；容量包含热身组 | 创建/编辑组录入区紧凑控件 | `WorkoutSetPayload.is_warmup` | snake/camel 成对映射 | WorkoutDoc 月分片 | 历史缺省 `false` | `workouts/YYYY-MM.json` 往返 | 已实现 |
| `WorkoutSetDoc.isFailure` | P0/P2/P3 | 详情、历史、编辑显示力竭标记 | 创建/编辑组录入区紧凑控件 | `WorkoutSetPayload.is_failure` | snake/camel 成对映射 | WorkoutDoc 月分片 | 历史缺省 `false` | `workouts/YYYY-MM.json` 往返 | 已实现 |
| `WorkoutSetDoc.rpe` | P0/P2/P3 | 详情、历史、编辑显示 RPE | 创建/编辑组录入区可清空输入，`null` 表示未设置 | `WorkoutSetPayload.rpe` | snake/camel 成对映射 | WorkoutDoc 月分片 | 历史缺省 `null` | `workouts/YYYY-MM.json` 往返 | 已实现 |
| `WorkoutSetDoc.restSeconds` | P0/P2/P3 | 详情、历史、编辑显示休息秒数 | 创建页休息计时回写；编辑页读后原样保存 | `WorkoutSetPayload.rest_seconds` | snake/camel 成对映射 | WorkoutDoc 月分片 | 历史缺省 `null` | `workouts/YYYY-MM.json` 往返 | 已实现 |
| `WorkoutSetDoc.isDropset` | P0/P2/P3 | 不展示 | 不编辑 | 不再接收/写出 | 读取旧数据时忽略，不再映射写回 | 不再写入新分片 | 旧数据归一时移除 | 删除后不再往返 | 待删除 |
| `TemplateExerciseDoc.id` | P1/P2/P3 | TemplateEditPage 不直接展示 | 读取模板时保留；新增动作才生成新 ID | `TemplateExerciseCreate.id?` | `toTemplate` 保留 id | `LocalJsonRepository.createTemplate/updateTemplate` 缺失时补 ID | 历史缺省由 repository 补 ID | `templates.json` 往返 | 已实现 |
| active template 新建动作追加 | P1/P0 | 当前选择页立即可选 | ExercisePicker 新建成功后追加到当前模板 | `appendExerciseToTemplate(templateId, exerciseId)` | `toTemplate` 刷新 activeTemplate/cache | `LocalJsonRepository.updateTemplate` | 不新增字段 | `templates.json` 往返 | 已实现 |

状态只允许：已实现、已知限制、预留、待删除。`metValue` 这类早期设计漏洞必须标为待删除，不能继续以“预留”名义保留。

### 7.5 非默认字段回归 fixture

建立包含非默认值的 snapshot：

- lb 训练组和 kg/lb 混合训练。
- warmup、failure、rpe、restSeconds。
- supersetGroup 非空。
- 模板动作条目有稳定 id。
- active template 下新建自定义动作并追加到模板。
- settings.weightUnit 为 lb。
- WebDAV passwordRef 本地存在、远端清空。
- profile.avatarUrl 或头像资源引用。

这些 fixture 至少覆盖 mapper、service update、DocumentStore export/import 和 sync snapshotToFiles。

### 7.6 null/undefined 语义测试

所有可清空字段都必须有测试：

- `undefined` 表示不修改。
- `null` 表示显式清空。
- 页面上的“清除/未设置/取消选择”必须传到 `null`。

当前 A5 已撤销为活跃缺陷，但该规则仍应覆盖 `note`、`mood`、`plan_template_id`、`rest_seconds`、WebDAV `passwordRef`、头像引用。

### 7.7 页面保存的读后原样保存测试

对编辑页类功能增加测试或脚本检查：

- 打开包含非默认字段的记录。
- 不改该字段。
- 保存。
- 比较不相关字段保持不变。

优先覆盖 WorkoutEditPage、TemplateEditPage、ExerciseDetailPage 的编辑弹层、SyncPage、ProfilePage 头像管理。

### 7.8 计算逻辑只有一个权威实现

容量、总组、展示单位、距离和时长不应在页面重复手写。页面只消费 core/service 给出的结果，或调用同一个纯函数。

需要禁止的模式：

```ts
set.weight * set.reps
`${value}kg`
```

除非代码附近明确已经完成单位换算并带有 displayUnit。

### 7.9 新增字段按 M1 链路验收

任何训练、动作、计划、模板、资料、设置、同步字段新增、删除或调整时，必须逐项核对：

```text
页面 state
-> service payload
-> core doc type
-> repository
-> DocumentStore
-> IndexedDB/Capacitor Filesystem
-> 读取/响应映射
```

同步字段额外核对：

```text
settings/profile/doc
-> SyncService
-> WebDavClient
-> JSON shard/resource
-> remote backup/tmp/MOVE
```

---

## 8. 建议验收顺序

1. 修复 A1/A2 后，增加 kg/lb 混合训练测试，人工核对首页、列表、详情、日历、分享。
2. 修复 A3 后，建立一条含热身、力竭、RPE 的训练，执行创建、编辑、详情、动作历史、WebDAV 往返。
3. 修复 A12/A6 后，分别走查：
   - 准备训练页 active template 下新建动作，确认模板持久化包含新动作。
   - 完成一组后“换动作”页新建动作，确认同一模板也被更新。
   - 保存模板前后既有模板动作 id 不变。
4. 修复 A9 后，保存 WebDAV 配置、清除配置、再次同步，确认旧 secret 不再引用。
5. 删除 A10 后，执行 `rg -n "metValue|met_value" src` 应为零残留。
6. 实现 A11 前先完成头像资源同步设计；实现后验证头像在本地重启、WebDAV 往返、另一设备拉取后可显示。
7. 对 A8 更新文档后，确认 README、概要、P1 不再把 CalendarPage 描述为计划条目页面。

---

## 9. 第一阶段修复记录

> 修复日期：2026-06-27
>
> 范围：第 6 节“第一阶段：立即修复短期缺陷”，包括 A1/A2/A3/A6/A12 与对应防再犯措施。

### 9.1 分项状态

| 编号 | 修复状态 | 代码与文档落点 | 验收证据 |
|---|---|---|---|
| A1 | 已修复 | 新增 `services/settings.ts`；ProfilePage 增加“训练重量单位”；WorkoutCreatePage、WorkoutEditPage 默认读取 `SettingsDoc.weightUnit`。体重仍固定 kg。 | `npm run build`、`npm test`、`npm run android:sync` 通过。 |
| A2 | 已修复 | 新增 `core/workoutMetrics.ts` 作为训练容量与重量换算单一权威；首页、列表、日历、详情、完成页、分享改为使用统一口径；`WorkoutSummary` 与 `WorkoutShareData` 携带展示单位。容量包含热身组。 | 新增 kg/lb 混合容量测试；散落 `set.weight * set.reps` 与固定 `容量(kg)` 搜索已清理。 |
| A3 | 已修复 | 删除 `WorkoutSetDoc.isDropset` 与页面兼容字段 `is_dropset`；迁移读取旧数据时剥离旧字段；创建/编辑页增加热身、力竭、RPE 控件；详情和动作历史展示热身、力竭、RPE、休息秒数。 | `rg -n "isDropset|is_dropset" ironlog/frontend/src` 零结果；新增非默认组字段测试。 |
| A6 | 已修复 | `TemplateExerciseCreate` 支持可选 `id`；TemplateEditPage 读取并保存既有模板动作 ID；repository 继续只给缺失 ID 的新增条目分配 ID。 | 新增模板读后原样保存测试，断言未变更模板动作 ID 不变。 |
| A12 | 已修复 | 新增 `appendExerciseToTemplate(templateId, exerciseId)`；active template 下新建动作后追加当前模板，刷新 `activeTemplate`、`planTemplatesCache` 与 `allExercises`；ExercisePicker 等待 `onCreated` 完成后再 `onSelect`。 | 新增模板追加 service 测试，覆盖追加保留既有 ID 与已存在动作幂等。 |

### 9.2 防再犯措施落地

- 字段契约矩阵已补入本审计文档第 7.4 节，并迁入 P0/P1 详细设计作为长期权威。
- 项目 `AGENTS.md` 已新增稳定规则：持久化字段变更先更新契约矩阵、聚合编辑读后原样保存、`undefined/null` 语义、统计与单位换算单一权威、删除持久化字段零残留。
- 可执行护栏已补：`workoutMetrics` 单测、组字段校验测试、旧字段迁移剥离测试、模板追加测试、模板动作 ID 稳定测试。
- A10 `metValue/met_value` 不属于第一阶段代码修复；本轮仅执行残留检查并确认仍为第二阶段待删除项。

### 9.3 第一阶段出口验证

已执行并通过：

```powershell
cd ironlog/frontend
npm run build
npm test
npm run android:sync
```

已执行残留检查：

```powershell
rg -n "isDropset|is_dropset" ironlog/frontend/src
rg -n "metValue|met_value" ironlog/frontend/src
rg -n "window\.(confirm|alert|prompt)" ironlog/frontend/src
rg -n "blue-|gray-" ironlog/frontend/src
git diff --check
```

结果说明：

- `isDropset|is_dropset`：零结果。
- `window.confirm|alert|prompt`：零结果。
- `blue-|gray-`：零结果。
- `metValue|met_value`：仍有既有结果，符合 A10 第二阶段待删除范围，本阶段未新增 MET 路径。
- `git diff --check`：通过，仅有 Windows 行尾转换 warning。
- 中文文档已用 `Get-Content` 抽查，无乱码。

未完成风险：

- 尚未在真实 Android WebView 360px/412px 上人工点按走查新增组控件与模板新建动作路径；当前已完成代码路径核对、构建、单测和 android sync。

---

## 10. 会改变结论的新证据

以下新证据会改变本审计结论：

- 已存在但未被本次搜索覆盖的 settings 页面或模板追加 service。
- 已存在未提交分支已经统一容量换算和 active template 下新建动作追加模板。
- 产品改变 CalendarPage 决策，要求日历显示未来计划条目。
- 产品改变头像同步方案，决定头像不进入 WebDAV 同步。
