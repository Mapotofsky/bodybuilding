# P0-P3 前后端契约匹配审计

> 审计日期：2026-06-25  
> 范围：P0 核心训练、P1 计划/模板/动作库/日历、P2 本地文档存储与迁移、P3 WebDAV 同步与 Android 平台。  
> 目标：找出数据模型、service/repository、同步链路已经定义，但页面未体现、未可操作、语义不一致或会丢失字段的现象，并给出修订计划和防复发措施。

---

## 1. 审计口径

证据标记：

- [已验证]：已直接核对当前代码或 P0-P3 文档。
- [推断]：由已验证证据推导出的用户影响或工程风险。
- [假设]：需要产品决策确认的前提。

严重程度：

| 等级 | 含义 |
|---|---|
| 高 | 会造成已记录数据解释错误、用户明确操作无效、统计错误，或会在常规编辑中丢失已存在字段。 |
| 中 | 功能已在一端实现但另一端缺口明显，影响一致性、同步稳定性或后续扩展，但当前默认路径不一定立刻丢数据。 |
| 低 | 底层预留字段、当前未承诺的能力或体验不完整，短期可接受但应明确状态。 |

修订紧迫性：

| 等级 | 含义 |
|---|---|
| 立即 | 应优先于新的 P0-P3 功能开发修复。 |
| 近期 | 应进入下一轮 P0-P3 修订，避免继续扩大不一致。 |
| 排期 | 可以排入后续迭代，但需要文档标注或测试护栏。 |

---

## 2. 总体现状

[已验证] 当前 P0-P3 的分层主体已经成立：页面经 `services/*` 到 `LocalJsonRepository`，持久化模型集中在 `frontend/src/core/models.ts`，WebDAV 同步通过 `settings.json`、manifest 和 JSON 分片工作。

[已验证] 主要不匹配集中在三类：

1. 训练与统计字段已经进入 `WorkoutSetDoc` 或 `SettingsDoc`，但页面没有完整输入路径，或展示时没有按字段语义解释。
2. 页面兼容类型在保存时丢弃嵌套 ID、`supersetGroup`、显式清空意图等信息，和 service/repository 的“保留字段、稳定 ID、undefined 不覆盖”语义不一致。
3. 部分 service 能力已经存在，但关键页面未接入，导致同一数据在不同入口可见性不一致。

[推断] 最需要优先处理的是重量单位与容量统计、训练组高级字段、模板动作嵌套 ID、清空模板/备注语义。这些会直接影响用户看到的数据是否正确，或影响后续同步与数据演进的稳定性。

---

## 3. 问题清单

| 编号 | 范围 | 现象 | 严重程度 | 修订紧迫性 |
|---|---|---|---|---|
| A1 | P0/P2/P3 | `SettingsDoc.weightUnit` 已定义并参与个人动作统计与同步合并，但没有个人设置入口；训练创建/编辑页默认 `kg`，不读取用户偏好。 | 高 | 立即 |
| A2 | P0 | 训练容量汇总直接累加 `weight * reps` 并固定显示 kg，未按每组 `unit` 换算；列表、首页、日历、详情、分享均受影响。 | 高 | 立即 |
| A3 | P0 | `isWarmup`、`isDropset`、`isFailure`、`rpe` 在模型和 service 中存在，但创建/编辑页无输入控件；详情只显示 `isWarmup` 与 `rpe`，不显示递减组/力竭组。 | 高 | 立即 |
| A4 | P0 | `WorkoutExerciseDoc.supersetGroup` 只在 mapper/service 中存在，编辑页本地状态不保留该字段，保存后会归一为 `null`。 | 中 | 近期 |
| A5 | P0/P1 | 训练创建页“清除模板”在已持久化草稿上会传 `plan_template_id: undefined`，service 合并后保留旧模板；清空备注和取消感受也有类似 `undefined` 保留风险。 | 中 | 近期 |
| A6 | P1/P2 | `TemplateExerciseDoc.id` 已定义，但 `TemplateEditPage` 本地状态不保留，保存时所有模板动作条目重新生成 ID。 | 中 | 近期 |
| A7 | P1/P2 | 自定义动作创建入口能力不一致：动作库入口可填写 description，共享 `ExercisePicker` 入口只能填写名称、分类、记录类型，不能填写动作说明和目标肌群。 | 中 | 排期 |
| A8 | P1 | `getCalendar` service 可生成计划条目，HomePage 已使用；CalendarPage 仍只用 `getWorkouts` 展示实际训练。P1 文档已标注这是当前限制。 | 中 | 排期 |
| A9 | P3 | `DocumentStore.removeSecret` 和 repository secret 删除能力存在，但 SyncPage 没有清除 WebDAV 凭据/删除旧 secret 的用户入口。 | 低 | 排期 |
| A10 | P1/P2 | `ExerciseDoc.metValue` 存在于模型和默认数据结构，但当前无 UI、无统计或日历使用路径。 | 低 | 排期 |
| A11 | P2 | `ProfileDoc.avatarUrl` 存在，但 ProfilePage 仅显示默认头像图标，没有头像选择、保存或清除入口。 | 低 | 排期 |

---

## 4. 逐项说明

### A1. 重量单位设置没有 UI 闭环

[已验证] `SettingsDoc` 定义了 `weightUnit: "kg" | "lb"`，默认值为 `kg`，并会随 settings 同步合并。`ExerciseDetailPage` 的个人统计通过 `buildExercisePersonalStats` 读取该设置后换算展示。

[已验证] `ProfilePage` 只调用 `getProfile/updateProfile`，没有读取或更新 `localRepository.getSettings()/updateSettings()`。页面体重展示固定为 `kg`。`WorkoutCreatePage` 与 `WorkoutEditPage` 各自维护本地 `weightUnit` state，初始值为 `kg`，不读取 `SettingsDoc.weightUnit`。

[推断] lb 用户即使数据模型支持偏好，也无法在设置页修改偏好；动作详情统计和训练录入默认值会出现割裂。

建议：

- 新增 `services/settings.ts`，让页面不直接访问 repository。
- 在 ProfilePage 或独立 SettingsPage 增加“训练重量单位”设置。
- WorkoutCreatePage、WorkoutEditPage 默认读取 `SettingsDoc.weightUnit`，单次训练内仍允许临时切换每组单位。
- 体重字段是否跟随同一单位需要产品确认；若不跟随，应明确命名为“训练重量单位”。

### A2. 容量统计没有按组单位换算

[已验证] `WorkoutSummary.total_volume`、`shareWorkout.total_volume`、WorkoutDetailPage、WorkoutCreatePage finish 统计均直接使用 `(set.weight || 0) * (set.reps || 0)`。多个页面固定显示 `kg` 或 `容量(kg)`。

[已验证] `WorkoutSetDoc.unit` 已保存到每组，且 P0 个人动作统计中已有固定换算常量 `1 lb = 0.45359237 kg`。

[推断] 例如一组 `100 lb x 5` 当前会被汇总为 `500 kg` 容量展示，而正确 kg 容量约为 `226.8 kg`。这会污染首页、训练列表、日历、详情和分享文案。

建议：

- 将重量换算和训练容量计算下沉到 `core`，例如 `core/workoutMetrics.ts`。
- 所有 summary、detail、share 和 finish 页使用同一计算函数。
- 输出值必须带 `displayUnit`，不得再用裸 number 加固定 `kg`。
- 增加 kg、lb、混合单位、热身组参与或排除策略的测试。

### A3. 训练组高级字段没有输入闭环

[已验证] `WorkoutSetDoc` 包含 `rpe`、`isWarmup`、`isDropset`、`isFailure`、`restSeconds`。service 校验 RPE 与休息秒数，mapper 能保留这些字段。

[已验证] WorkoutCreatePage 新增组时把 `rpe` 写为 `null`，`isWarmup/isDropset/isFailure` 写为 `false`，没有控件让用户修改。WorkoutEditPage 会保留读到的字段，但也没有控件修改。WorkoutDetailPage 只显示 `isWarmup` 的 W 标识、`rpe` 和休息秒数，不显示 dropset/failure。

[推断] P0 的个人动作统计已用 `isWarmup` 区分总组与工作组，但用户无法标记热身组，导致 `workingSetCount` 和表现指标在真实训练场景中失真。递减组和力竭组则完全无法进入用户记录。

建议：

- 在组录入区增加“热身”、“力竭”、“递减组”和 RPE 的紧凑控件，可放入高级展开区。
- 编辑页必须能修改这些字段，详情页和历史记录应显示标识。
- 先定义容量/表现指标是否排除热身组；当前 P0 个人统计已经排除热身组，训练总容量也应明确策略。

### A4. supersetGroup 会在编辑时丢失

[已验证] `WorkoutExerciseDoc.supersetGroup`、页面兼容类型 `superset_group`、`toWorkoutExercise` 和 `workoutToPayload` 都有该字段。

[已验证] `WorkoutEditPage` 的 `LocalExercise` 没有 `superset_group`，读取 workout 时不保存该字段，保存 payload 时也不传。`normalizeWorkoutPayload` 对缺失值使用 `exercise.superset_group ?? null`，因此编辑会把已存在的 superset 分组归一为 `null`。

[推断] 当前 UI 没有创建 superset 的入口，所以默认用户路径暂时不触发。但一旦后续导入、同步或手工数据出现 superset，编辑训练会造成字段丢失。

建议：

- 短期：即使 UI 不支持 superset，也在 `WorkoutEditPage` 状态和 payload 中原样保留 `superset_group`。
- 中期：若 superset 属于当前 P0-P3 范围，补充显示和编辑控件；否则在模型文档中标为预留字段。

### A5. 显式清空与 service 合并语义不一致

[已验证] `updateWorkout` 中 `undefined` 表示不覆盖，`null` 表示显式清空。WorkoutCreatePage 的 `buildPayload` 使用 `plan_template_id: activeTemplateId ?? undefined`，finish 保存使用 `note: note || undefined` 和 `mood: extra?.mood ?? undefined`。

[已验证] 创建页提供“清除”模板按钮，会把 `activeTemplateId` 设为 `null`，但已存在草稿再次保存时会传 `undefined`，service 合并后保留旧 `planTemplateId`。

[推断] 用户看到“已清除模板”或清空备注，但保存后的 WorkoutDoc 仍可能保留旧模板或旧备注。这违反了“用户明确措辞优先”的规则。

建议：

- 在创建页区分 create 与 update：已有 `workoutIdRef.current` 时，清空模板必须传 `plan_template_id: null`。
- note、mood 也按用户意图传 `null`，只在字段未触达时传 `undefined`。
- 增加单测覆盖 `undefined` 保留、`null` 清空、草稿恢复后清空模板。

### A6. TemplateExerciseDoc.id 保存时不稳定

[已验证] `TemplateExerciseDoc` 有 `id`，repository 在缺少 ID 时会分配 ID。

[已验证] `TemplateEditPage` 的 `LocalTemplateExercise` 不包含模板动作条目自身 `id`，从 `tmpl.exercises` 映射时丢弃 `te.id`，保存时只传 `exercise_id/sort_order/note`。`services/plan.ts` 的 `updateTemplate` 会把缺少 ID 的条目写成 `id: ""`，repository 再生成新 ID。

[推断] 每次保存模板都会让模板动作嵌套 ID 全量变化，造成无意义的分片更新时间、同步 LWW 噪音，也违背“嵌套 ID 稳定”的数据演进要求。

建议：

- `LocalTemplateExercise` 增加 `id`。
- 读取时保留 `te.id`，新增动作使用空 ID，保存时把已有 ID 传回 service。
- 增加测试：仅修改模板名称或备注时，未变更动作条目的 ID 不变。

### A7. 自定义动作创建入口能力不一致

[已验证] 动作库入口创建自定义动作时可填写 `name/category/type/description`。共享 `ExercisePicker` 创建入口只填写 `name/category/type`。

[已验证] 动作详情编辑页能编辑目标肌群和 description；service 也能校验主/次目标肌群。

[推断] 用户在训练中临时新建动作时，容易产生缺失说明和目标肌群的动作。后续需要再进入详情补齐，且入口能力不一致。

建议：

- 抽出共享的 `ExerciseEditor` 或最小创建表单，三个入口统一字段。
- 若训练中创建需要保持快速，至少创建后提示“补充详情”，并保证后续补齐路径明显。

### A8. CalendarPage 未渲染计划条目

[已验证] `getCalendar(from, to)` 已能组合 weekly/cyclic 虚拟条目和实际训练完成状态，HomePage 使用它展示今日计划和补训建议。

[已验证] CalendarPage 仍只调用 `getWorkouts({ from, to })`，只渲染实际训练记录。P1 文档已明确“计划条目渲染尚未接入本页”。

[推断] 用户在首页可以看到今日计划，但进入日历后看不到未来计划或未完成条目，形成可见性不一致。

建议：

- 若日历作为计划入口，改用 `getCalendar` 渲染计划条目，并保留实际训练记录。
- 未完成计划条目点击进入 `/workouts/new?template_id=X&date=YYYY-MM-DD` 或现有模板入口。
- 若暂不实现，应在 UI 文案或文档中继续明确“日历当前仅显示训练记录”。

### A9. WebDAV secret 缺少清除入口

[已验证] `DocumentStore` 与 repository 提供 `removeSecret`，SyncPage 保存新密码时可写 secret。

[已验证] SyncPage 没有“清除 WebDAV 密码/删除配置”按钮；用户清空输入保存时，旧 `passwordRef` 仍可能保留。

[推断] 密码不会进入 JSON，P3 的最低秘密边界仍成立。但旧 secret 生命周期不可见，影响本地安全卫生和用户控制感。

建议：

- 增加“清除同步配置”或“删除已保存密码”操作。
- 调用 `removeSecret(passwordRef)` 后把 settings.webdav.passwordRef 写为 `null`。
- 同步前继续保持远端 settings 清空 passwordRef。

### A10. metValue 是底层占位，当前无用户路径

[已验证] `ExerciseDoc.metValue` 存在，默认数据和自定义创建均写为 `null`。当前页面不展示、不编辑，也没有统计使用。

[推断] 这不是当前 P0-P3 的用户承诺，更像未来能量消耗或有氧估算的预留字段。

建议：

- 短期在设计文档中标注为预留字段，避免被误认为已实现能力。
- 实现前补齐来源、单位、校验、UI、统计和同步回归测试。

### A11. avatarUrl 是底层占位，当前无头像管理

[已验证] `ProfileDoc.avatarUrl` 和页面兼容类型 `avatar_url` 存在。ProfilePage 固定显示本地图标，不提供头像选择、清除或本地文件保存。

[推断] 当前本地单人版不依赖头像能力，风险低。但如果文档或产品宣称“个人资料头像”，当前前端未匹配。

建议：

- 若短期不做头像，标注为预留字段。
- 若实现，应定义本地文件存储位置、Android 权限、导出/同步策略和隐私边界。

---

## 5. 修订计划

### 第一阶段：先修正会造成错误数据解释的问题

1. 建立设置 service 与重量单位 UI。
   - 新增 `services/settings.ts`。
   - ProfilePage 或 SettingsPage 读取/保存 `weightUnit`。
   - WorkoutCreatePage、WorkoutEditPage 初始化时读取用户偏好。

2. 抽出统一训练指标计算。
   - 在 `core` 新增训练汇总/容量计算函数。
   - 支持 kg/lb 换算和混合单位。
   - 替换 `WorkoutSummary`、HomePage、WorkoutsPage、CalendarPage、WorkoutDetailPage、WorkoutCreatePage finish、`shareWorkout` 中的重复裸计算。

3. 补齐训练组高级字段。
   - 创建页和编辑页提供热身、RPE、递减组、力竭控件。
   - 详情页和动作历史显示这些标识。
   - 单测覆盖 warmup 不进入个人表现指标。

4. 修复显式清空语义。
   - 创建页清除模板、清空备注、取消感受时对已存在 workout 传 `null`。
   - 增加 service 层回归测试，固定 `undefined` 与 `null` 的语义。

### 第二阶段：修正嵌套 ID 与字段保留

1. TemplateEditPage 保留 `TemplateExerciseDoc.id`。
2. WorkoutEditPage 保留 `superset_group`。
3. 对模板动作、训练动作、训练组增加“读取后保存不改变未编辑字段”的 fixture 测试。
4. 对 WebDAV 往返增加非默认字段 fixture：lb、warmup、rpe、dropset、failure、superset、模板动作 ID。

### 第三阶段：统一 P1/P3 体验缺口

1. 统一自定义动作创建表单，至少保证说明字段一致；目标肌群可选择快速创建后补齐或完整创建。
2. 决定 CalendarPage 是否渲染计划条目。若接入，必须同时处理未完成、已完成、补训、按日期开始模板训练。
3. SyncPage 增加清除同步配置和删除 secret 的入口。
4. 对 `metValue`、`avatarUrl` 等预留字段做文档状态标注，未进入当前验收前不宣称已实现。

---

## 6. 防止再犯的措施

### 6.1 增加字段契约矩阵

在 P0-P3 文档或新增维护文档中为每个持久化字段记录：

| 字段 | 权威文档 | 页面是否展示 | 页面是否可编辑 | service 输入 | mapper | repository | migration | WebDAV 往返 | 当前状态 |
|---|---|---|---|---|---|---|---|---|---|

新增字段必须先填矩阵，再编码。若字段只是预留，状态写“预留，不进入当前 UI 验收”。

### 6.2 建立非默认字段回归 fixture

当前很多问题不会被默认数据发现，因为默认值是 `kg`、`false`、`null`。应建立一组包含非默认值的本地 snapshot：

- lb 训练组和 kg/lb 混合训练。
- warmup、dropset、failure、rpe、restSeconds。
- supersetGroup 非空。
- 模板动作条目有稳定 id。
- settings.weightUnit 为 lb。
- WebDAV passwordRef 本地存在、远端清空。

这些 fixture 至少覆盖 mapper、service update、DocumentStore export/import 和 sync snapshotToFiles。

### 6.3 把 null/undefined 语义写成测试

所有可清空字段都必须有测试：

- `undefined` 表示不修改。
- `null` 表示显式清空。
- 页面上的“清除/未设置/取消选择”必须传到 `null`。

优先覆盖：`plan_template_id`、`note`、`mood`、`rest_seconds`、WebDAV `passwordRef`。

### 6.4 页面保存必须做“读后原样保存”测试

对编辑页类功能增加测试或脚本检查：

- 打开包含非默认字段的记录。
- 不改该字段。
- 保存。
- 比较不相关字段保持不变。

优先覆盖 WorkoutEditPage、TemplateEditPage、ExerciseDetailPage 的编辑弹层和 SyncPage。

### 6.5 计算逻辑只能有一个权威实现

容量、总组、工作组、展示单位、距离和时长不应在页面重复手写。页面只消费 core/service 给出的结果，或调用同一个纯函数。

需要禁止的模式：

```ts
set.weight * set.reps
`${value}kg`
```

除非代码附近明确已经完成单位换算并带有 displayUnit。

### 6.6 每次新增字段按 M1 链路验收

任何训练、动作、计划、模板、资料、设置、同步字段新增或调整时，必须逐项核对：

```text
页面 state
-> service payload
-> core doc type
-> repository
-> DocumentStore
-> IndexedDB/Capacitor Filesystem
-> 读取/响应映射
```

同步字段额外核对：

```text
settings/profile/doc
-> SyncService
-> WebDavClient
-> JSON shard
-> remote backup/tmp/MOVE
```

### 6.7 文档明确“已实现、限制、预留”

不要只因为字段在模型中存在，就把它写成已实现能力。文档中应固定使用三种状态：

- 已实现：有页面入口、service 校验、持久化、读取展示和测试。
- 已知限制：service 或模型具备部分能力，但页面未接入。
- 预留：字段存在但当前不承诺用户功能。

---

## 7. 建议验收顺序

1. 先跑字段矩阵，确认 A1-A6 是否全部有明确 owner。
2. 修复 A1/A2 后，增加 kg/lb 混合训练测试，并人工核对首页、列表、详情、分享。
3. 修复 A3/A4 后，建立一条含 warmup、rpe、failure、dropset、superset 的训练，执行编辑保存和 WebDAV 往返。
4. 修复 A6 后，保存模板前后比较模板动作 id。
5. 修复 A5 后，走查草稿恢复、清除模板、清空备注、取消感受。
6. 对 A7-A11 做产品决策：本轮实现、标注已知限制，或明确预留。

---

## 8. 会改变结论的新证据

以下新证据会改变本审计结论：

- 已存在但未被本次搜索覆盖的设置页面或字段编辑入口。
- 已存在未提交的测试或分支，已经统一了容量换算和重量单位默认值。
- 产品明确决定某些字段为长期预留，并已在 P0-P3 文档中标注不进入当前 UI 验收。
- 现有真实数据永远不可能包含 supersetGroup 或非默认模板动作 ID，这会降低 A4/A6 的当前风险，但不改变其实现不稳定事实。
