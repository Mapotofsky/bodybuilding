# IronLog 技术路线分析

> 日期：2026-06-22
> 适用分支：`android-single-user-json-webdav`
> 本文档解释当前技术决策、替代方案和维护边界；功能范围以《概要设计文档》为准。

---

## 1. 决策摘要

IronLog 从“前后端分离的多用户 Web 应用”转为“Android-first 的单人本地应用”。这一变化的首要目标不是提高并发能力，而是取消服务器、数据库和账号运维成本，同时保留可迁移到未来文档数据库/小程序的领域模型。

| 问题 | 当前决策 | 原因 |
|---|---|---|
| 运行容器 | React + Vite + Capacitor Android | 最大化复用现有 React 页面，同时获得文件与原生 HTTP 能力。 |
| 本地数据 | JSON 文档分片 | 数据可读、可备份、可按月同步，并能映射到 MongoDB/CloudBase 文档。 |
| Web 开发存储 | IndexedDB | 浏览器开发不依赖本地文件权限，避免把训练主体放进 localStorage。 |
| Android 存储 | Capacitor Filesystem | 将 JSON 文件保存在应用私有数据目录，不需要外部数据库。 |
| 同步 | WebDAV 手动同步 | 用户自带远端空间，不需要 IronLog 运营服务器。 |
| WebDAV 传输 | Capacitor HTTP + 原生 OkHttp 插件 | Android WebView 对 `MKCOL`、`MOVE`、`PROPFIND` 的兼容性不足，需要原生通道。 |
| 架构边界 | Repository + DocumentStore + Service | 让 React 页面不依赖数据来源，未来可替换为 CloudBase adapter。 |

---

## 2. 已放弃的路线

### 2.1 FastAPI + PostgreSQL 作为当前运行路径

旧方案适合多用户、中心化权限和 SQL 查询，但要求用户或项目维护 Python 环境、数据库、迁移、部署与备份。单人开源版本不需要这些成本。旧实现已移至 `ironlog/legacy/backend/`，保留为历史参考，不启动、不发布、不依赖。

### 2.2 单一大 JSON 文件

单文件实现简单，但每次训练都会重写全量数据，多设备同步冲突面大，损坏影响面也大。当前按静态文件和 `workouts/YYYY-MM.json` 分片，训练数据增量主要局限在单月文件。

### 2.3 直接从 React 页面访问 Capacitor 或 WebDAV

这会让浏览器、Android 和未来小程序逻辑纠缠。当前规定 `pages -> services -> repositories -> platform`，同步只能经 `sync/`，`core/` 保持无平台依赖。

### 2.4 把 WebDAV 当数据库

WebDAV 提供文件读写与目录操作，不提供查询、事务或冲突合并。当前只把它作为 JSON 分片的远端备份/同步目标；所有交互查询都从本地快照完成。

---

## 3. 当前技术栈

### 3.1 前端与测试

| 类别 | 当前组件 | 用途 |
|---|---|---|
| UI 框架 | React 18 + TypeScript | 页面、状态和类型安全。 |
| 构建 | Vite 5 | 浏览器开发与 `dist/` 产物。 |
| 路由 | React Router 6 | 本地页面路由。 |
| 样式 | Tailwind CSS 4 | Emerald/Slate 移动端界面。 |
| 状态 | Zustand | Toast、ConfirmDialog 等局部全局状态。 |
| 日期/图表 | date-fns、Recharts | 日期计算和训练趋势展示。 |
| 测试 | Vitest | migration、DocumentStore、计划与同步逻辑测试。 |

Axios、JWT 和 React Query 不再是当前运行链路。依赖列表仍应以 `ironlog/frontend/package.json` 为准，删除旧依赖前必须先全文搜索。

### 3.2 Android 平台

| 组件 | 用途 |
|---|---|
| `@capacitor/core` / `@capacitor/android` | 将 `dist/` 嵌入 Android WebView。 |
| `@capacitor/filesystem` | 在 Android `Directory.Data` 保存 JSON 分片。 |
| `@capacitor/preferences` | 保存小型平台设置和当前密码引用对应的值。 |
| `@capacitor/core` 的 `CapacitorHttp` | 原生执行 GET/PUT/DELETE。 |
| `WebDavHttpPlugin.java` | OkHttp 执行 MKCOL/MOVE/PROPFIND。 |

`capacitor.config.ts` 固定 `appId=app.ironlog.local`、`webDir=dist`、`androidScheme=https`。AndroidManifest 仅声明当前必要的 `INTERNET` 权限，并注册 FileProvider。

### 3.3 存储与模型

| 层 | 代码 | 责任 |
|---|---|---|
| 文档模型 | `src/core/models.ts` | BaseDoc、WorkoutDoc、TemplateDoc、Manifest 等纯类型。 |
| 默认数据与迁移 | `src/core/defaultData.ts`、`src/core/migrations.ts` | 默认动作、schema v1 归一化、分片路径。 |
| 数据访问 | `src/repositories/localJsonRepository.ts` | CRUD、tombstone、manifest 更新时间。 |
| 平台存储 | `src/platform/documentStore.ts` | IndexedDB / Capacitor Filesystem / Preferences。 |
| 页面服务 | `src/services/*.ts` | 兼容页面调用面，完成 snake/camel 映射与本地查询。 |

当前 `schemaVersion` 为 1。它代表文档格式版本，不等同 Android `versionCode` 或 npm 版本。

---

## 4. WebDAV 同步策略

### 4.1 远端目录与文件

用户填写的 URL 会规范化为以 `ironlog-data/` 为根的目录。同步数据包含 manifest、静态 JSON、训练月分片和 `backups/` 下的远端备份。

```text
<用户 WebDAV URL>/ironlog-data/
  manifest.json
  profile.json
  settings.json
  exercises.json
  templates.json
  workouts/YYYY-MM.json
  backups/<timestamp>-<shard-name>
```

### 4.2 当前同步算法

1. 读取本地 Settings 和密码引用；缺少 URL、用户名、密码时停止。
2. 获取远端 manifest；不存在则作为首次上传处理。
3. 读取 manifest 中全部静态分片和训练月分片。
4. 对同 ID 文档按 `updatedAt` 选择较新版本，并记录 LWW 日志。
5. 将合并结果写回本地 DocumentStore。
6. 创建远端目录；备份当前远端分片。
7. 对每个分片 `PUT .tmp-*`，成功后 `MOVE` 至正式路径。
8. 删除远端不再存在的训练月分片；更新本地 `lastSyncAt`。

### 4.3 当前限制

- SyncService 尚未把 ETag/If-Match 用作上传前置条件；WebDavClient 虽有该参数入口，但同步流程未传入。
- LWW 依赖设备时间，不能表达字段级合并、离线共同编辑或因果关系。
- 备份没有保留数量上限，会持续增长。
- Preferences 不等价于 Keystore 加密；密码保护是发布前需要继续加强的事项。
- 浏览器模式的 WebDAV 请求受 CORS 和服务器方法支持影响；Android 原生通道更可靠。

这些限制必须在提交“同步已完成”或对外发布说明中如实保留。

---

## 5. 未来迁移到小程序/CloudBase 的边界

可复用：领域 JSON 结构、string UUID、schema migration、训练统计和日历的纯逻辑、Repository 接口。

需要替换：React 页面、Capacitor 平台存储、WebDAV Client、Android 原生插件。未来应实现 `CloudBaseRepository` 或等价 adapter，而不是把 CloudBase SDK 写入当前 `core`。

CloudBase/MongoDB 的建议集合映射：`profiles`、`settings`、`exercises`、`plans`、`templates`、`workouts`。`WorkoutDoc` 保持聚合文档，避免在迁移时重建旧 SQL join table。

---

## 6. 技术风险与处理顺序

| 优先级 | 风险 | 处理方向 |
|---|---|---|
| 高 | WebDAV 密码不是硬件级安全存储 | 引入 Keystore/安全存储 adapter，并迁移现有 Preferences 值。 |
| 高 | LWW 可能静默丢失冲突字段 | 增加同步基线、ETag 条件写和可查看/恢复的冲突副本。 |
| 中 | JSON 文件写入中断或损坏 | 本地写入临时文件/校验、导入校验与恢复流程。 |
| 中 | cyclic 排程没有依据最近完成训练推进 | 将调度纯函数迁入 core，补历史完成场景测试。 |
| 中 | Android 构建受网络/SDK 影响 | 锁定 Gradle/JDK 版本，并在 CI 或可复现环境验证 APK。 |
| 低 | 训练数据增长导致月分片变大 | 维持月分片，必要时按月或按更细时间窗口继续拆分。 |

---

## 7. 维护准则

- 任何数据字段修改必须沿“页面 -> service -> core doc -> repository -> DocumentStore -> 读取映射”走查。
- 新存储能力先定义 Repository 接口，再实现平台 adapter。
- 新同步行为先明确本地真源、远端文件、失败恢复和冲突策略。
- 新的详细设计必须从《概要设计文档》的模块编号和边界生成，并包含字段契约、调用契约、场景和验收条件。
