# IronLog 技术路线分析

> 当前路线：Android-first、本地优先、开源单人版  
> 最后更新：2026-06-11  
> 文档用途：说明为什么采用当前技术栈，以及未来修改时不得突破的架构边界。

---

## 1. 路线结论

采用：

```text
React + Vite + TypeScript + Capacitor + 本地 JSON Repository + WebDAV 文件同步
```

当前运行路径：

```text
React 页面
  -> services
  -> LocalJsonRepository
  -> DocumentStore
  -> IndexedDB / Capacitor Filesystem
```

这条路线优先满足：

- Android-first。
- 离线可用。
- 开源单人版低部署成本。
- 未来可迁移 CloudBase/MongoDB。

---

## 2. 路线对比

### 2.1 旧在线服务路线

技术：

- FastAPI。
- SQLAlchemy async。
- PostgreSQL。
- Alembic。
- React Web。
- Axios。
- JWT。

优点：

- 多用户能力自然。
- 查询表达能力强。
- 服务端统计方便。

不适合当前目标的原因：

- 需要服务器和数据库。
- 开源单人用户部署门槛高。
- 账号/JWT/权限体系对个人训练记录是额外负担。
- 离线能力弱。
- WebDAV/本地备份不是一等公民。

当前处理：

- 旧后端归档到 `ironlog/legacy/backend`。
- 只作为字段和历史行为参考。

### 2.2 纯 PWA 路线

优点：

- 无 Android Studio。
- 开发简单。

问题：

- Android 文件系统能力弱于 Capacitor。
- WebDAV 容易受 CORS 影响。
- APK 分发和原生能力不足。

当前处理：

- 保留 Web 开发模式。
- 生产目标优先 Capacitor Android。

### 2.3 React Native 路线

优点：

- 原生移动体验更强。

问题：

- 需要重写 UI。
- 现有 React/Vite 页面复用成本低于重写。
- 当前目标是快速完成可用 APK，而不是全面移动端重构。

当前处理：

- 不采用。

### 2.4 Capacitor 路线

优点：

- 复用现有 React 页面。
- 可构建 APK。
- 可使用 Filesystem/Preferences/Http。
- 迁移成本低。
- 仍保留 Web 开发体验。

当前采用。

---

## 3. 分层职责

### 3.1 pages

页面文件：

```text
src/pages/HomePage.tsx
src/pages/WorkoutCreatePage.tsx
src/pages/WorkoutEditPage.tsx
src/pages/WorkoutDetailPage.tsx
src/pages/CalendarPage.tsx
src/pages/PlansPage.tsx
src/pages/PlanDetailPage.tsx
src/pages/TemplateEditPage.tsx
src/pages/ProfilePage.tsx
src/pages/SyncPage.tsx
```

职责：

- UI 状态。
- 用户交互。
- 导航。
- 调用 services。

不得：

- import `LocalJsonRepository`。
- import `DocumentStore`。
- import `WebDavClient`。

例外：

- 当前 `SyncPage` 为设置/同步入口，允许调用同步 service 和 repository settings。后续可再抽 `services/sync.ts`。

### 3.2 services

当前文件：

```text
src/services/workout.ts
src/services/exercise.ts
src/services/plan.ts
src/services/profile.ts
src/services/localMappers.ts
```

职责：

- 页面调用兼容。
- DTO 映射。
- 本地聚合查询。
- 错误转换。

服务层必须保持的契约：

- `getWorkout(id: string): Promise<Workout>`
- `createWorkout(payload): Promise<Workout>`
- `updateWorkout(id, payload): Promise<Workout>`
- `copyWorkout(id, targetDate): Promise<Workout>`
- `getExercises(params): Promise<Exercise[]>`
- `getExerciseHistory(exerciseId): Promise<ExerciseHistoryRecord[]>`
- `getPlans(): Promise<PlanSummary[]>`
- `getCalendar(from, to): Promise<CalendarDay[]>`

### 3.3 core

当前文件：

```text
src/core/models.ts
src/core/id.ts
src/core/defaultData.ts
src/core/migrations.ts
src/core/repositories.ts
src/core/migrations.test.ts
```

职责：

- 文档类型。
- schemaVersion。
- 默认动作库。
- migration。
- repository 接口。
- 纯工具。

未来应加入：

- `stats.ts`
- `calendar.ts`
- `importExport.ts`

### 3.4 repositories

当前文件：

```text
src/repositories/localJsonRepository.ts
```

职责：

- 单机本地数据源。
- 统一 CRUD。
- tombstone。
- manifest shard 更新。
- partial update 清理 undefined。

修改注意：

- 任何 update 都不得让 undefined 覆盖已有字段。
- delete 不得物理删除。
- list 默认过滤 deleted。

### 3.5 platform

当前文件：

```text
src/platform/documentStore.ts
```

职责：

- Web/Android 存储差异。
- IndexedDB。
- Capacitor Filesystem。
- Capacitor Preferences。
- snapshot 与 shard 文件互转。

### 3.6 sync

当前文件：

```text
src/sync/webdavClient.ts
src/sync/syncService.ts
```

职责：

- WebDAV 方法封装。
- SyncService 流程。
- merge。
- backups。
- conflict log。

---

## 4. 数据模型与未来 CloudBase 映射

当前文档集合：

```text
ProfileDoc       -> profiles
SettingsDoc      -> settings
ExerciseDoc      -> exercises
TrainingPlanDoc  -> plans
TemplateDoc      -> templates
WorkoutDoc       -> workouts
ScheduleEntryDoc -> schedule_entries
```

映射原则：

- `id` 可作为 `_id`。
- `deletedAt` 用于软删除。
- `schemaVersion` 支持前端迁移。
- `WorkoutDoc.exercises.sets` 保持嵌套数组。

不建议迁移为 SQL 风格：

```text
workout_exercises
workout_sets
template_exercises
```

这些在单机文档模型里会增加同步冲突和读写复杂度。

---

## 5. WebDAV 技术策略

WebDAV 只是一组远端文件：

```text
manifest.json
profile.json
settings.json
exercises.json
templates.json
workouts/index.json
backups/
```

上传规则：

```text
PUT path.tmp-timestamp
MOVE path.tmp-timestamp -> path
```

好处：

- 避免半写入。
- 失败时正式文件不被破坏。

备份规则：

- 上传前读取远端旧文件。
- 写入 `backups/timestamp-file-name`。

冲突规则第一版：

- 比较 `updatedAt`。
- last-write-wins。
- 记录冲突日志。

未来可改：

- per-document conflict UI。
- 训练记录按月份分片增量同步。
- ETag/If-Match 更严格覆盖保护。

---

## 6. Android 技术策略

Capacitor 配置：

```text
appId: app.ironlog.local
appName: IronLog
webDir: dist
```

命令：

```bash
npm run android:sync
npm run android:open
```

插件：

- `@capacitor/filesystem`
- `@capacitor/preferences`
- `@capacitor/core` Http 能力

构建风险：

- Gradle wrapper 首次下载依赖网络。
- Android SDK/JDK 环境不完整会阻塞 APK 编译。

---

## 7. 技术债清单

### 必须优先处理

- 将 calendar cyclic 逻辑抽到 core 并补测试。
- 将 SyncPage 的 repository 调用再收敛到 service。
- 增加导入/导出 UI。
- 增加 WebDAV 错误分类。

### 可以后置

- code splitting 降低 chunk warning。
- 更强 secret storage 插件。
- 更精细的冲突 UI。
- 统计图表重构。

---

## 8. 开发守则

新增功能默认遵循：

1. 先定义 core doc 类型。
2. 再定义 repository/service 契约。
3. 再改页面。
4. 最后补 migration/test。

删除功能默认遵循：

1. 搜索所有引用。
2. 先删页面入口。
3. 再删 service。
4. 再删类型/工具。
5. grep 验证。

同步相关默认遵循：

1. 本地先写成功。
2. 同步失败不得影响本地可用。
3. 密码不得进入 JSON。
4. WebDAV 未配置时应用必须正常。
