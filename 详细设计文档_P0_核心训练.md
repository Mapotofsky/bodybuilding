# P0 详细设计文档：核心训练与动作记录

> 对应概要设计：M1 核心训练与历史解释兼容性
> 状态：当前实现基线；主题、完整动作详情与 AI 路线不得破坏本模块的数据契约。个人动作统计扩展为规划中。
> 前置依赖：P2 本地文档存储与迁移。P1 模板与日历只通过 `planTemplateId` 与本模块关联。

---

## 1. 目标、边界与不变量

本模块在没有网络、账户、后端或 WebDAV 配置的情况下，完成训练创建、逐组记录、草稿恢复、编辑、复制、删除、动作历史和文本分享。

本模块不负责：远端同步、模板编排、用户认证、云端分析、主题选择、AI 请求或联网检索。页面不能直接读写 JSON 或平台文件；所有读写必须通过 service 和 LocalJsonRepository。主题只能改变呈现；未经用户确认的 AI 候选不得创建、更新或覆盖 WorkoutDoc。

核心不变量：

1. `WorkoutDoc` 是完整训练聚合，内嵌动作和组。
2. workout、动作条目和组条目都使用新的 string ID；复制训练不能复用嵌套 ID。
3. 删除训练写 tombstone，常规列表和统计不显示 tombstone。
4. 单位、休息秒数、模板关联在创建、保存、读取、编辑和复制链路中不能丢失。
5. 训练创建页的模板入口有 URL 参数和页内选择器两条，P1 规定的过滤行为必须保持。
6. `endTime=null` 的训练是未结束草稿；恢复或结束草稿均不得丢失已保存的动作和组。
7. `WorkoutExerciseDoc.exerciseType` 是训练时快照；动作替代或后续改类型不能改变历史训练的录入规则和统计解释。

---

## 2. 持久化模型

### 2.1 WorkoutDoc

定义位置：`frontend/src/core/models.ts`。

```ts
interface WorkoutDoc extends BaseDoc {
  date: string;                         // YYYY-MM-DD
  startTime: string | null;             // ISO 8601
  endTime: string | null;               // ISO 8601
  planTemplateId: string | null;
  note: string | null;
  mood: number | null;                  // 当前 UI 使用 1~5
  exercises: WorkoutExerciseDoc[];
}
```

训练按 `date.slice(0, 7)` 写入 `workouts/YYYY-MM.json`。同一月文件包含正常记录和 `deletedAt != null` 的 tombstone；内存 `DataSnapshot.workouts` 只是已加载分片的聚合。

### 2.2 嵌套动作和组

```ts
interface WorkoutExerciseDoc {
  id: string;
  exerciseId: string;
  exerciseType: "strength" | "cardio" | "reps_only" | "static_hold";
  sortOrder: number;
  supersetGroup: number | null;
  sets: WorkoutSetDoc[];
}

interface WorkoutSetDoc {
  id: string;
  setNumber: number;
  weight: number | null;
  reps: number | null;
  unit: "kg" | "lb";
  durationSec: number | null;
  distanceM: number | null;
  rpe: number | null;
  isWarmup: boolean;
  isDropset: boolean;
  isFailure: boolean;
  restSeconds: number | null;
}
```

`sortOrder` 表示动作在训练内的顺序；`setNumber` 从 1 起连续编号。`exerciseType` 固化记录时的动作解释：力量记录重量和次数，有氧记录距离和时长，仅次数记录次数，静态保持记录时长。编辑保存时必须保留不适用字段已有的 `null` 语义和所有其他字段，避免覆盖丢失。

### 2.3 页面兼容类型

页面仍使用历史 `snake_case` 类型，例如 `start_time`、`exercise_id`、`rest_seconds`。转换只允许发生在：

```text
pages -> services/workout.ts -> LocalJsonRepository -> WorkoutDoc
                         ^
                   services/localMappers.ts
```

页面不得引用 `WorkoutDoc` 后直接写入 DocumentStore。

---

## 3. 本地调用契约

### 3.1 Workout service

文件：`frontend/src/services/workout.ts`。

| 函数 | 输入 | 结果 | 规则 |
|---|---|---|---|
| `getWorkouts` | `month` / `from` / `to` | `WorkoutSummary[]` | 默认过滤 tombstone，日期范围为闭区间。 |
| `getWorkout` | `id: string` | `Workout` | 不存在或已删除时抛错。 |
| `getLatestWorkoutDraft` | 无 | `Workout \| null` | 返回最后更新的有效未结束训练（`endTime=null`）。 |
| `createWorkout` | `WorkoutCreatePayload` | `Workout` | service 转 camelCase；repository 为 workout/嵌套项分配 ID。 |
| `updateWorkout` | `id`、部分 payload | `Workout` | `undefined` 不覆盖已有字段；传入 exercises 时全量替换动作列表。 |
| `completeWorkoutDraft` | `id` | `Workout` | 以草稿最后 `updatedAt` 作为 `endTime`，不使用用户当前选择新建的时间。 |
| `deleteWorkout` | `id` | `void` | 写入 tombstone。 |
| `copyWorkout` | `id`、目标日期 | `Workout` | 生成新的 workout/动作/组 ID，保留模板、组内容、备注和感受，清空开始/结束时间。 |
| `shareWorkout` | `id` | `WorkoutShareData` | 纯本地计算总组数、容量、时长和动作摘要。 |

`WorkoutCreatePayload` 的字段必须与 `WorkoutDoc` 的映射成对维护。新增字段时按如下链路检查：

```text
WorkoutCreatePage state
-> buildPayload
-> WorkoutCreatePayload
-> service mapper
-> WorkoutDoc
-> LocalJsonRepository
-> toWorkout / toWorkoutSummary
-> Detail/Edit/List 页面
```

### 3.2 动作引用依赖摘要

动作库浏览、自定义动作 CRUD、删除/替代重定向和详情信息呈现由 P1 权威定义。P0 仅依赖有效 ExerciseDoc 选择训练动作，并保证 WorkoutExerciseDoc 的 exerciseId 与 exerciseType 快照可供 P1 的历史显示和本节统计读取；P0 不定义动作库 UI、动作元数据或删除业务规则。

### 3.3 规划中的个人动作统计聚合

当前详情只提供累计组数、最近使用和有限历史组；下列扩展为规划中。P0 定义统计的唯一计算口径，P1 只呈现结果，P2 不持久化统计快照。

```ts
interface ExercisePersonalStats {
  completedWorkoutCount: number;
  totalSetCount: number;
  workingSetCount: number;
  lastCompletedDate: string | null;
  strength: {
    totalReps: number;
    volume: number;
    bestWeight: number | null;
    displayUnit: "kg" | "lb";
  };
  cardio: { totalDistanceM: number; totalDurationSec: number };
  repsOnly: { totalReps: number };
  staticHold: { totalDurationSec: number };
}
```

聚合输入是未 tombstone、`endTime != null` 的 WorkoutDoc。动作匹配沿用 P1 的替代重定向解析：若历史 ID 被解析到当前详情动作，则计入该动作；原始历史 ID 和 `exerciseType` 快照均不改写。`completedWorkoutCount` 按不同 WorkoutDoc 计数；`totalSetCount` 包含热身组，`workingSetCount` 与所有表现指标排除 `isWarmup=true` 的组。

每个指标必须按历史 `WorkoutExerciseDoc.exerciseType` 解释，而不是按当前 ExerciseDoc.type 重解释：strength 的次数、容量和最大重量只使用同时具有所需非 null 字段的工作组。所有 strength 组先按 `SettingsDoc.weightUnit` 换算到用户当前设置的 kg 或 lb，再计算容量与最大重量；换算使用固定常量 `1 lb = 0.45359237 kg`，只在展示层按统一规则舍入。cardio 累计距离和时长；reps_only 累计次数；static_hold 累计时长。不得以 `0` 的 falsy 判断替代 null 判断，也不得在首期引入估算 1RM、医疗结论或训练处方。

规划调用链为：

```text
ExerciseDetailPage
  -> 现有动作详情 / 历史 service
  -> P0 纯统计聚合
  -> LocalJsonRepository 读取 ExerciseDoc 与 WorkoutDoc 月分片
  -> DocumentStore
```

空历史返回数值 0 与 `lastCompletedDate=null`，不是读取错误；读取失败由 service 向页面报告，不能以空统计掩盖。统计仅随本地训练增删改和同步后的本地合并重新计算，不创建远端查询或缓存索引。

---

## 4. 页面流程

### 4.1 训练创建：WorkoutCreatePage

路由：`/workouts/new`，可接受：

```text
/workouts/new
/workouts/new?date=YYYY-MM-DD
/workouts/new?template_id=<template-id>
```

进入页时先查询未结束草稿；有草稿时显示恢复对话，不进入新的 session state。选择“继续”会恢复同一训练 ID、已保存的动作/组、模板、日期、感受和备注；选择“新建”会先通过 `completeWorkoutDraft` 结束旧草稿，再进入空白创建流程。

无草稿时状态机：

```text
select -> training -> rest -> training | select | finish
finish -> 保存后跳转 WorkoutDetailPage
```

关键步骤：

1. 首屏加载动作、启用计划和最后一个未结束草稿；若存在 `template_id`，加载模板并激活。
2. 激活模板后调用 `.filter()`，非模板动作不得渲染。页内选择器激活模板时必须得到相同行为。
3. 第一次选择动作时写入训练开始时间并启动总计时。
4. 完成一组时先更新内存 session，再立即调用 `createWorkout` 或 `updateWorkout`。
5. 休息结束、换动作或结束训练时，把 `restSeconds` 写回该动作最后完成的一组，再持久化。
6. finish 阶段保存 mood、note 和 `endTime`，跳转至详情页。

失败行为：本地写入异常必须通过 Toast 告知；不得静默丢弃已输入的 session state。未配置 WebDAV 不得阻塞任何步骤。

### 4.2 训练详情：WorkoutDetailPage

路由：`/workouts/:id`。

页面展示日期、模板 badge、动作、每组重量/次数/休息、总组数、容量、时长、备注和感受。菜单包含编辑、复制、分享、删除。

- 复制使用日期 bottom sheet；该弹层必须高于 Layout 的底部导航。
- 删除必须通过 ConfirmDialog 确认，再调用 `deleteWorkout`。
- 分享优先调用 `navigator.share`，不可用时复制文本；不生成或上传服务端图片。

### 4.3 编辑：WorkoutEditPage

路由：`/workouts/:id/edit`。

编辑读取本地 WorkoutDoc 映射；保存时传入完整动作/组列表。新增组时 `setNumber = 当前组数 + 1`；删除组后必须重编号。编辑空字段时应以 `null` 表示显式清空，以 `undefined` 表示不改动。

### 4.4 列表、首页和日历入口

- WorkoutsPage 按 `month=YYYY-MM` 查询；总量和次数只基于该月有效训练。
- HomePage 的最近训练不按月限制，本月统计只查询该月。
- CalendarPage 读取网格范围内训练记录，但月统计仅使用本月边界。
- 日历选中日期创建训练时传递 `date` 参数。

---

## 5. 复制、删除和时间边界

### 5.1 复制训练

复制语义是“复制训练内容，创建新的待完成训练记录”。

| 字段 | 复制规则 |
|---|---|
| `id`、嵌套动作/组 `id` | 必须新建。 |
| `date` | 使用调用者指定目标日期。 |
| `startTime`、`endTime` | 设为 `null`。 |
| `planTemplateId` | 保留。 |
| `note`、`mood`、sets 和 `restSeconds` | 保留。 |
| 元字段 | 由 repository 新建，不能沿用来源时间戳。 |

### 5.2 删除训练

删除不物理移除数组项，而是设置 `deletedAt`。因此：

- `listWorkouts`、动作历史、日历完成状态和统计必须忽略 tombstone。
- 同步仍会传递 tombstone，避免另一设备复活已删除记录。
- 未来真正清理 tombstone 前，需要同时考虑所有已同步设备，不在当前范围内。
- P5 的 AI 计划导入只能创建计划、模板和经确认的自定义动作；不得通过替代、批量更新或导入流程重写历史 Workout 的 exerciseId 或 exerciseType。

### 5.3 日期口径

`date` 是训练归属日期，不从 ISO 开始时间推导。月统计使用 `monthStart <= date <= monthEnd`；日历加载范围可扩展到周网格，但不应扩大月统计范围。

---

## 6. 测试与验收

### 6.1 必测场景

| 场景 | 期望 |
|---|---|
| 空白训练创建 | 本地生成 WorkoutDoc 与嵌套 ID，详情可读。 |
| 单组保存后返回页面 | 已完成组、重量、次数和 restSeconds 不丢失。 |
| 草稿恢复 | 未结束训练再次进入创建页时可继续；选择新建后旧草稿以最后活动时间结束，已保存组不丢失。 |
| 多类型动作 | 力量、有氧、仅次数、静态保持分别校验并在详情、编辑、历史和统计中按 `exerciseType` 解释。 |
| 编辑训练 | 增删组后 setNumber 连续，未编辑字段不被 `undefined` 覆盖。 |
| 复制训练 | 新 ID、目标日期、模板关联和 restSeconds 均正确。 |
| 删除训练 | 列表/统计/动作历史不再包含，原始分片保留 tombstone。 |
| 无网络 | 创建、编辑、查看、日历和动作历史均可运行。 |
| 个人动作统计（规划） | 仅聚合已完成且未删除训练；热身、替代重定向、用户当前单位换算和历史 exerciseType 均按第 3.3 节口径处理。 |
| 切换重量单位（规划） | 同一历史训练按新的 SettingsDoc.weightUnit 重新换算显示；不得生成第二份统计或改写原始组单位。 |

### 6.2 交付验证

```powershell
cd D:\workspaces\vscodeWorkspace\project\bodybuilding\ironlog\frontend
npm run build
npm test
rg -n "window\.(confirm|alert|prompt)" src
```

涉及模板入口时同时按 P1 的两条入口走查；涉及持久化字段时同时检查 P2 的分片和迁移规则。
