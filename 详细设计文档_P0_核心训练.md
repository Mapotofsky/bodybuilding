# 详细设计文档 P0：核心训练

> 当前版本：本地单人版核心训练链路  
> 最后更新：2026-06-11  
> 文档用途：指导训练创建、训练编辑、训练详情、复制训练、动作历史和训练中即时保存的开发。

---

## 1. 用户目标

用户打开 IronLog 后，不需要登录、不需要网络、不需要服务器，就可以开始训练并可靠保存数据。

核心闭环：

```text
开始训练 -> 选择动作 -> 完成组 -> 记录休息 -> 保存 -> 查看详情 -> 复制/编辑/删除
```

---

## 2. 相关文件

页面：

```text
src/pages/HomePage.tsx
src/pages/WorkoutCreatePage.tsx
src/pages/WorkoutEditPage.tsx
src/pages/WorkoutDetailPage.tsx
src/pages/WorkoutsPage.tsx
src/pages/CalendarPage.tsx
src/pages/ExerciseDetailPage.tsx
```

服务：

```text
src/services/workout.ts
src/services/exercise.ts
src/services/localMappers.ts
```

数据：

```text
src/core/models.ts
src/repositories/localJsonRepository.ts
src/platform/documentStore.ts
```

工具：

```text
src/utils/workout.ts
```

---

## 3. 数据契约

### 3.1 WorkoutDoc

```ts
interface WorkoutDoc {
  id: string;
  date: string;
  startTime: string | null;
  endTime: string | null;
  planTemplateId: string | null;
  note: string | null;
  mood: number | null;
  exercises: WorkoutExerciseDoc[];
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null;
  schemaVersion: number;
}
```

字段规则：

- `date` 为 `YYYY-MM-DD`。
- `startTime/endTime` 为 ISO datetime。
- `planTemplateId` 可为空，表示非模板训练。
- `deletedAt` 不为空时，该训练不进入列表、日历、统计。

### 3.2 WorkoutExerciseDoc

```ts
interface WorkoutExerciseDoc {
  id: string;
  exerciseId: string;
  sortOrder: number;
  supersetGroup: number | null;
  sets: WorkoutSetDoc[];
}
```

字段规则：

- `exerciseId` 指向 `ExerciseDoc.id`。
- `sortOrder` 决定详情页展示顺序。
- 第一版保留 `supersetGroup`，UI 可暂不强化。

### 3.3 WorkoutSetDoc

```ts
interface WorkoutSetDoc {
  id: string;
  setNumber: number;
  weight: number | null;
  reps: number | null;
  unit: "kg" | "lb";
  durationSec: number | null;
  distanceM: number | null;
  rpe: number | null;
  isWarmup: boolean;
  isDropset: boolean;
  isFailure: boolean;
  restSeconds: number | null;
}
```

字段规则：

- `restSeconds` 记录上一组结束后的休息时间。
- 容量只用 `weight * reps`。
- 有氧字段可为 null。

---

## 4. service 契约

`src/services/workout.ts` 必须提供：

```ts
getWorkouts(params?: { month?: string; from?: string; to?: string }): Promise<WorkoutSummary[]>
getWorkout(id: string): Promise<Workout>
createWorkout(body: WorkoutCreatePayload): Promise<Workout>
updateWorkout(id: string, body: Partial<WorkoutCreatePayload>): Promise<Workout>
deleteWorkout(id: string): Promise<void>
copyWorkout(id: string, targetDate: string): Promise<Workout>
shareWorkout(id: string): Promise<WorkoutShareData>
```

页面侧仍使用 snake_case payload：

```ts
plan_template_id
start_time
end_time
exercise_id
sort_order
set_number
rest_seconds
```

service 内部转换为 doc 字段：

```ts
planTemplateId
startTime
endTime
exerciseId
sortOrder
setNumber
restSeconds
```

---

## 5. 创建训练流程

### 5.1 入口

入口 1：首页：

```text
/workouts/new
```

入口 2：日历选中日期：

```text
/workouts/new?date=YYYY-MM-DD
```

入口 3：今日计划：

```text
/workouts/new?template_id=<templateId>
```

入口 4：训练创建页内选择计划/模板。

### 5.2 选择阶段

页面加载：

1. `getExercises()`。
2. `getPlans()` 并过滤 `is_active`。
3. 如果存在 `template_id`，调用 `getTemplate(templateId)`。

模板激活后：

```ts
const templateExerciseIds = new Set(activeTemplate.exercises.map((te) => te.exercise_id));
const filteredExercises = allExercises.filter((e) => {
  if (activeTemplate && !templateExerciseIds.has(e.id)) return false;
  if (filterCat && e.category !== filterCat) return false;
  if (searchQ && !e.name.includes(searchQ)) return false;
  return true;
});
```

这里必须是真过滤。

### 5.3 训练阶段

用户选择动作后：

1. 启动总计时。
2. 加载动作历史。
3. 设置默认重量/次数。
4. 进入 training phase。

完成一组：

1. 构造 `WorkoutSet`。
2. 写入 `sessionExercises`。
3. 调用 `persistWorkout(updated)`。
4. 进入 rest phase。
5. 启动休息计时。

休息结束：

1. 将当前 `restSeconds` 写入上一组。
2. 调用 `persistWorkout(updated)`。
3. 下一组或换动作。

### 5.4 完成训练

保存时：

1. 停止总计时。
2. 写入 mood。
3. 写入 note。
4. 写入 endTime。
5. 导航到详情页。

---

## 6. 编辑训练流程

读取：

```text
getWorkout(id) -> Workout -> LocalExercise[]
```

保存 payload：

```ts
{
  date,
  note,
  mood,
  start_time,
  end_time,
  exercises: [
    {
      exercise_id,
      sort_order,
      sets: [...]
    }
  ]
}
```

关键要求：

- 编辑保存是完整替换 exercises。
- set 删除后必须重新编号。
- 未传字段不得覆盖为 undefined。
- `rest_seconds` 必须保留。

---

## 7. 复制训练流程

`copyWorkout(id, targetDate)`：

1. 读取 source WorkoutDoc。
2. 创建新 WorkoutDoc。
3. `date = targetDate`。
4. `planTemplateId = source.planTemplateId`。
5. `note/mood` 保留。
6. exercises 深拷贝。
7. sets 深拷贝。
8. nested ids 全部重建。
9. startTime/endTime 清空。

验收：

- 复制后详情可打开。
- 复制训练仍显示模板名/颜色。
- 复制训练的每组重量、次数、restSeconds 保留。

---

## 8. 删除训练

删除只做：

```text
deletedAt = now
updatedAt = now
```

不得从数组中物理删除。

影响：

- `getWorkouts()` 默认不返回。
- `getWorkout(id)` 默认不返回。
- 日历不展示。
- 统计不计算。

---

## 9. 动作历史

`getExerciseHistory(exerciseId, limit)` 从本地 workouts 聚合：

```text
workouts
  -> exercises where exercise.exerciseId === exerciseId
  -> sets
  -> date desc, setNumber asc
```

返回：

```ts
{
  date,
  set_number,
  weight,
  reps,
  unit,
  rest_seconds
}
```

---

## 10. 统计口径

WorkoutSummary：

- `exercise_count = exercises.length`
- `total_sets = sum(exercise.sets.length)`
- `total_volume = sum(weight * reps)`
- `exercise_ids = exercises.map(exerciseId)`

注意：

- `weight = 0` 或 `reps = 0` 不贡献容量。
- null 不贡献容量。
- tombstone workout 不参与统计。

---

## 11. 边界 case

### 11.1 正常路径

无模板创建训练，完成两组，保存后详情可见。

### 11.2 URL 模板入口

从今日计划进入，URL 带 `template_id`，动作列表只显示模板动作。

### 11.3 页面内模板入口

从主入口进入训练页，在页面内选择模板，动作列表只显示模板动作。

### 11.4 今日完成

某模板今日已有 workout，日历/首页今日计划显示完成状态。

### 11.5 范围外完成

月视图加载 calStart~calEnd，但本月统计只计算 monthStart~monthEnd。

### 11.6 无历史记录

动作历史为空时，输入框默认空，不报错。

### 11.7 删除记录

删除后详情不可访问，列表/日历/统计不显示。

---

## 12. 禁止事项

- 不得恢复 `/workouts` HTTP API。
- 不得在页面中直接操作 IndexedDB。
- 不得使用 number id。
- 不得用 sort 替代模板过滤。
- 不得物理删除 WorkoutDoc。
- 不得丢失 `restSeconds`。
- 不得使用原生弹窗。

---

## 13. 开发验收清单

- [ ] 新建训练成功。
- [ ] 训练中完成组后本地保存。
- [ ] restSeconds 在详情页可见。
- [ ] 编辑训练不丢组数据。
- [ ] 复制训练保留模板关联。
- [ ] URL 模板入口过滤正确。
- [ ] 页面内模板入口过滤正确。
- [ ] 删除训练后不显示。
- [ ] `npm run build` 通过。
- [ ] `npm test` 通过。
