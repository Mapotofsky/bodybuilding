# P6 详细设计文档：身体数据与时间段备注

> 对应概要设计：P4.1 身体数据独立模块与日期/时间段备注
> 状态：规划中；当前代码仍由 `ProfileDoc.height` / `ProfileDoc.weight` 保存身高体重，尚无本文新文档
> 前置依赖：P2 本地文档存储与迁移、P3 WebDAV 同步、P4/P4.1 入口与主题语义

---

## 1. 目标、边界与当前事实

P6 提供两个高内聚模块：

1. 身体数据：以稀疏历史测量记录追踪身高、体重、体脂和围度。
2. 时间段备注：以日期范围背景标记解释训练、身体趋势或生活状态变化。

当前代码事实：

- `ProfileDoc` 仍包含 `height`、`weight`，`ProfilePage` 可编辑这两个单值字段。
- `SettingsDoc.weightUnit` 已存在，当前用于训练重量展示/输入偏好。
- `DataSnapshot`、DocumentStore 和 SyncService 当前没有 `body-metrics.json` 或 `timeline-notes.json`。

本模块不负责训练记录、训练备注、动作成绩事件、RM 计算、统计页面布局或分享图。统计页、动作页和分享图只能读取 P6 派生结果或轻量标记，不能在各自页面实现身体数据管理逻辑。

---

## 2. 身体数据持久化模型

### 2.1 BodyMetricDoc

定义位置规划为 `frontend/src/core/models.ts`。

```ts
type BodyMeasurementKey =
  | "neck" | "shoulder" | "chest" | "waist" | "hip"
  | "upperArmLeft" | "upperArmRight"
  | "forearmLeft" | "forearmRight"
  | "thighLeft" | "thighRight"
  | "calfLeft" | "calfRight";

interface BodyMeasurementsCm {
  neck: number | null;
  shoulder: number | null;
  chest: number | null;
  waist: number | null;
  hip: number | null;
  upperArmLeft: number | null;
  upperArmRight: number | null;
  forearmLeft: number | null;
  forearmRight: number | null;
  thighLeft: number | null;
  thighRight: number | null;
  calfLeft: number | null;
  calfRight: number | null;
}

interface BodyMetricDoc extends BaseDoc {
  recordedAt: ISODateTime;
  heightCm: number | null;
  weightKg: number | null;
  bodyFatPercent: number | null;
  measurementsCm: BodyMeasurementsCm;
  note: string | null;
}
```

持久化文件：`body-metrics.json`，静态业务文档，不按月分片。数组内保留 tombstone，随 manifest 和 WebDAV 往返。

### 2.2 字段语义

- `recordedAt` 是测量事实发生时间，必须是 `ISODateTime` 且 `<= now`；允许补录过去时间，不允许未来时间。
- `heightCm` 固定使用 cm。
- `weightKg` 固定以 kg 持久化；输入和展示可按 `SettingsDoc.weightUnit` 在 kg/lb 间转换。
- `bodyFatPercent` 为百分比数值，不保存 `%` 字符。
- `measurementsCm` 使用固定键；未测量字段写 `null`，不通过省略字段表达。
- `note` 只解释本次身体测量，例如“晨起空腹”；不能创建只有 `note` 而没有任何测量值的记录。
- `null` 表示本条记录未测量该指标，不表示清空历史当前值。

首版不支持自定义围度项、自由键值、动态字段、`confirmedFields` 或完整快照标记。

### 2.3 ProfileDoc 替换规则

P6 实施时直接用 `BodyMetricDoc` 替换旧 `ProfileDoc.height` / `ProfileDoc.weight` 身体数据模型：

- 从 core model、types、profile service、ProfilePage 和相关测试中移除 `ProfileDoc.height`、`ProfileDoc.weight`。
- 不编写旧测试数据 migration。
- 不在读取层兼容旧字段。
- 不把旧 `ProfileDoc.height` / `ProfileDoc.weight` 生成初始身体记录。
- 开发/测试数据清空后按新模型重建，运行指南已说明清除 IndexedDB / Android 模拟器旧测试数据。

`ProfileDoc` 保留 `nickname`、`avatarUrl`、`gender`、`birthDate`，作为本地个人资料和未来账号映射预留。

---

## 3. 身体数据 service 与派生口径

规划 service：`src/services/bodyMetrics.ts`。页面只调用 service，不直接访问 repository。

| 函数 | 行为 |
|---|---|
| `listBodyMetrics(params)` | 读取有效记录，支持时间范围和排序；默认过滤 tombstone 与未来记录。 |
| `getBodyMetric(id)` | 读取单条有效记录；不存在或 tombstone 时抛错。 |
| `createBodyMetric(payload)` | 校验并创建稀疏测量记录。 |
| `updateBodyMetric(id, payload)` | 只修改该条记录，不复制旧指标形成完整快照。 |
| `deleteBodyMetric(id)` | 写 tombstone。 |
| `getCurrentBodyMetrics()` | 按每个指标各自取最新非空、非未来、非 tombstone 记录。 |
| `getBodyMetricTrend(metricKey, range)` | 按固定范围返回单指标趋势。 |
| `getPairedMeasurementTrend(pairKey, range)` | 返回左右成对围度双序列。 |
| `getPeriodBodyMetricSummary(period)` | 给 P4.1 统计页使用，只读派生摘要。 |

### 3.1 输入校验

service 必须校验：

- `recordedAt` 是有效 ISO 时间且不晚于当前时间。
- 至少一个身体指标非 `null`。
- `heightCm`、`weightKg`、`bodyFatPercent`、围度数值均为有限数值并在实现前固定合理范围。
- `bodyFatPercent` 范围为百分比，不允许字符串。
- `measurementsCm` 只允许固定键；拒绝或忽略未知键，但不得写回自由字段。
- `note` trim 后为空则保存 `null`，非空长度上限需在实现前固定并测试。

页面可用 string 保留可清空、可重输输入；提交时由 service 转换。不得使用 `Number(value) || fallback`。

### 3.2 当前值派生

当前值不是单条最新记录，而是按指标维度独立派生：

```text
有效记录 = deletedAt == null
       && recordedAt <= now
       && 对应指标值 != null

当前值 = 对该指标按 recordedAt 倒序、updatedAt 倒序后的第一条有效记录
```

同一天允许多条记录。日粒度趋势按具体指标取当天最后一条非空记录。例如早上记录体重、晚上只记录腰围，则当天体重取早上最后非空体重，腰围取晚上最后非空腰围。

删除或编辑记录后，不生成补偿记录；当前值和趋势从剩余有效记录自然回退。

### 3.3 趋势范围

首版趋势图固定范围：

- `30天`
- `90天`
- `1年`
- `全部`

不实现自定义区间、周/月粒度聚合或插值补点。不同指标记录频率不一致时允许折线自然断点。跨单位指标不混画；左右成对围度属于同单位同部位对照，必须同图双序列展示。

---

## 4. 身体数据页面

入口：`我的 -> 身体数据`。首版不进入底部导航。

页面结构：

1. 当前值摘要：基础指标区展示身高、体重、体脂；围度指标区展示固定围度项。
2. 左右成对围度：同一行展示左右最新值、差值和各自来源时间；缺失一侧时显示单侧值和空态。
3. 趋势图区：选择指标与固定范围；左右成对围度显示双线。
4. 历史记录列表：按 `recordedAt` 倒序，显示本条记录中实际测量的指标和 note 摘要。
5. 新增/编辑表单：允许只确认或修改部分指标；上次值可作为 UI 预填，但未确认字段不写入本次记录。
6. 删除：使用 ConfirmDialog，调用 tombstone 删除。

体重输入和展示使用当前 `SettingsDoc.weightUnit` 转换；身高和围度固定 cm。页面不得把 lb 写入 `weightKg`。

---

## 5. 时间段备注持久化模型

### 5.1 TimelineNoteDoc

```ts
type TimelineNoteRangeType = "single_day" | "date_range" | "open_ended";

interface TimelineNoteDoc extends BaseDoc {
  content: string;
  rangeType: TimelineNoteRangeType;
  startDate: ISODate;
  endDate: ISODate | null;
  workoutId: DocId | null;
}
```

持久化文件：`timeline-notes.json`，静态业务文档，不按月分片。

### 5.2 语义边界

三类备注必须分清：

- `WorkoutDoc.note`：解释某一次训练。
- `BodyMetricDoc.note`：解释某一次身体测量。
- `TimelineNoteDoc`：解释某一天或某段时间的外部背景，如器械变化、通勤、压力、睡眠、训练环境。

如果只是记录身体状态、训练背景或生活背景而没有身体测量值，应创建 `TimelineNoteDoc`，不得创建 note-only `BodyMetricDoc`。

首版 `TimelineNoteDoc` 不新增 `title`、`category`、`type`、标签、`exerciseId` 或 `templateId`。日历或趋势图短摘要由 `content` 截断生成。

### 5.3 范围校验

service 必须校验：

- 所有范围类型均要求 `startDate <= today`，禁止未来开始的备注。
- `single_day`：`endDate === startDate`。
- `date_range`：`endDate != null` 且 `endDate >= startDate`。
- `open_ended`：`endDate === null`。
- `workoutId` 可为空；若不为空，必须指向当前有效训练或保留为可解析历史引用策略，不能静默指向不存在训练。

### 5.4 生效查询

查询某一范围 `[queryStart, queryEnd]` 时：

1. 将 `queryEnd` 裁剪到 `today`。
2. 将 open-ended 备注的有效结束视为 `today`。
3. 使用闭区间重叠判断：

```text
note.startDate <= clippedQueryEnd
&& effectiveNoteEnd >= queryStart
```

不得为了持续中备注在未来日期生成可见条目。同一天可有多条生效备注，系统不自动合并；合并、拆分、删除只能由用户显式操作。

---

## 6. 时间段备注页面与接入

入口：

- `我的 -> 时间段备注` 或身体数据/统计页内的轻量入口。
- 日历日期详情和趋势图标记可进入该日期生效备注列表。

页面能力：

- 新增、编辑、删除 `TimelineNoteDoc`。
- 列表按 `startDate` 倒序展示。
- 首版不提供复杂分类、筛选或搜索。
- 删除使用 ConfirmDialog，写 tombstone。

接入方式：

- 日历页：只显示轻量标记或数量，详情弹层列出内容。
- 统计页：在图表上显示标记，帮助解释变化。
- 身体趋势页：显示标记，但不改变趋势计算。
- 分享图：首版默认不读取 TimelineNote。

---

## 7. 本地存储、migration 与同步

P2 需要新增：

- `DataSnapshot.bodyMetrics: BodyMetricDoc[]`
- `DataSnapshot.timelineNotes: TimelineNoteDoc[]`
- `STATIC_SHARD_PATHS` 增加 `body-metrics.json`、`timeline-notes.json`
- `snapshotToFiles()` / `filesToSnapshot()` 往返
- `buildShardList()` 静态分片
- repository CRUD 与 tombstone 支持
- SyncService 合并 `bodyMetrics`、`timelineNotes`

schema 演进：

- 新增文档时递增 schemaVersion 并显式初始化空数组。
- P6 替换 `ProfileDoc.height/weight` 不做旧测试数据迁移或兼容读取。
- 新字段随 WebDAV 普通业务分片同步，不保存 WebDAV URL、username、passwordRef、password 或未来 API key。

---

## 8. 字段契约摘要

完整矩阵见 P2。P6 新增字段必须覆盖：

| 字段 | 页面 | service/core | repository/migration | 同步 |
|---|---|---|---|---|
| `BodyMetricDoc.recordedAt` | 表单、历史、趋势排序 | 校验 ISODateTime 且 `<= now` | 初始化、读写、tombstone | `body-metrics.json` |
| `heightCm` / `weightKg` / `bodyFatPercent` | 当前值、表单、趋势 | 稀疏派生；体重 kg/lb 转换 | 固定字段，未测量为 null | `body-metrics.json` |
| `measurementsCm` | 固定围度、左右对比 | 固定键校验、双序列派生 | 未测量键写 null | `body-metrics.json` |
| `BodyMetricDoc.note` | 单条测量备注 | 不允许 note-only 记录 | trim 后空为 null | `body-metrics.json` |
| `TimelineNoteDoc.content` | 备注列表、标记详情 | trim 与长度校验 | 固定字段 | `timeline-notes.json` |
| `rangeType/startDate/endDate` | 表单、日历/趋势标记 | 闭区间重叠、禁止未来开始 | 固定字段 | `timeline-notes.json` |
| `workoutId` | 可选关联训练 | 解析或为空 | 保留引用 | `timeline-notes.json` |

---

## 9. 验收与回归

实现完成后至少验证：

- 新增身体记录允许稀疏指标，但拒绝 note-only 记录。
- `recordedAt` 不能未来；未来记录即使通过同步进入，也不得参与当前值、趋势和统计。
- 当前值按每个指标最新非空记录派生，编辑/删除后自然回退。
- 体重以 kg 持久化，输入/展示按 `SettingsDoc.weightUnit` 转换；身高和围度固定 cm。
- 围度只接受固定键，左右成对围度可同图双序列展示。
- 趋势范围仅为 30天、90天、1年、全部；不插值、不跨单位混画。
- Profile 资料页不再编辑身高体重；需要展示时读取 `BodyMetricDoc` 派生当前值。
- TimelineNote 三种范围类型校验正确，禁止未来开始。
- 生效查询裁剪到今天，open-ended 不向未来生成可见条目。
- 日历和趋势图只显示轻量备注标记，不改变统计计算。
- `body-metrics.json` 和 `timeline-notes.json` 进入 manifest、DocumentStore export/import、WebDAV pull/merge/push、backup、tmp/MOVE。

本轮文档修改不运行 build/test/android sync；代码实现交付时按 AGENTS.md 执行项目级验证和移动端走查。
