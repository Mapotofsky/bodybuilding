# P4.1 详细设计文档：训练日历统计与界面优化

> 对应概要设计：P4.1 竞品补强与界面优化阶段；承接 P0/P1/P2/P3/P4 当前实现边界
> 状态：规划中；当前代码尚未实现本文新增页面和统计视图
> 前置依赖：P0 训练聚合与统计口径、P1 动作与模板引用、P2 本地文档存储、P3 WebDAV 同步、P4 主题 token

---

## 1. 目标、边界与当前事实

本模块把当前实际训练月历扩展为“日历 / 统计”模块，并重构“我的”页信息架构和训练详情分享图入口。它只组织页面、统计派生和展示入口，不新建训练数据真源，也不改变计划排程、身体数据或动作成绩记录的权威归属。

当前代码事实：

- `CalendarPage` 只调用 `getWorkouts(from, to)` 展示实际训练记录，月统计已按自然月边界过滤；不渲染计划条目。
- `HomePage` 通过 P1 的 `getCalendar()` 展示今日计划和补训建议；flexible 计划不生成待训练提醒。
- `ProfilePage` 当前混合本地资料、身高体重、重量单位、主题选择和同步入口。
- `WorkoutDetailPage` 当前分享为文本或系统分享能力，不生成 PNG 分享图。
- P4 已实现 `themeId` 模型、settings service、主题解析和基础 CSS token，但页面仍大量直接使用 `emerald-*`、`slate-*` 等 Tailwind 色彩工具类；全页面语义迁移未完成。

本模块不负责：

- `BodyMetricDoc` 的新增、编辑、删除、趋势派生和同步；这些由 P6 定义。
- `TimelineNoteDoc` 的持久化模型、范围查询和编辑；这些由 P6 定义。
- RM 公式、动作成绩事件、重算和动作级趋势；这些由 P7 定义。
- 未来计划排期、提醒、多人共享、AI、服务端和云端统计。

---

## 2. 职责分层

| 能力 | 权威文档 | P4.1 中的职责 |
|---|---|---|
| 训练记录、容量、时长、组数 | P0 | 只消费统一 core/service 口径，不在页面手写 `weight * reps`。 |
| 模板、动作名称、分类、目标肌群 | P1 | 解析训练摘要、category 部位短标签和细粒度肌群分布输入。 |
| 新增业务文档和分片 | P2 | P4.1 只引用 `BodyMetricDoc`、`TimelineNoteDoc`、`ExercisePerformanceRecordDoc` 的读取结果。 |
| WebDAV 同步 | P3 | 分享图默认不进入 manifest；P6/P7 新文档随同步往返。 |
| 主题 token 与导航 | P4 | P4.1 新增/重构页面必须使用语义 token，不扩大硬编码色彩。 |
| 身体数据与时间段备注 | P6 | 统计页只读展示派生摘要和备注标记，不管理记录。 |
| RM 与动作成绩记录 | P7 | 统计页和分享图只读消费 PR/RM 摘要，不生成成绩事件。 |

设计原则：

1. 日历和统计是同一模块的两个视图，共用范围选择和统计 service。
2. 年度复盘是统计模块 `year` 周期下的报告化呈现，不是独立数据模块。
3. “我的”页是入口中心，不再承担所有资料与设置编辑。
4. 分享图是本地生成的输出物，不是业务文档，不参与同步和 LWW 合并。

---

## 3. 日历视图

### 3.1 数据来源

日历视图读取：

- `WorkoutDoc`：实际训练记录、训练日期、模板关联、训练备注。
- `TemplateDoc` / `TrainingPlanDoc`：实际训练已关联模板的名称和颜色，可继续用于训练记录卡片既有展示，不作为日历格部位短标签的替代。
- `ExerciseDoc`：训练内动作的 `category`，用于短标签。
- `TimelineNoteDoc` 派生结果：只显示当天生效备注的轻量标记或数量。

日历视图不读取未来计划条目，不展示未完成计划，不实现部位筛选，也不因部位状态提示隐藏实际训练。

### 3.2 日历格内容

每个日期格按下列顺序生成展示摘要：

1. 取当天未 tombstone 的实际训练。
2. 从训练动作的 `ExerciseDoc.category` 生成部位短标签；有关联模板时，也不替代日历格部位短标签。
3. 多次训练或多个 category 时展示 2-3 个短标签，其余折叠为 `+N`。
4. `TimelineNoteDoc` 只在 `date <= today` 的日期格显示轻量标记或数量，不展示未来日期的备注状态。

category 颜色使用稳定映射表，例如 `chest`、`back`、`legs`、`shoulders`、`arms`、`core`、`cardio`、`compound`、`stretch`。映射表输出语义颜色角色或 CSS 变量，不能在页面散落写死 hex 或 Tailwind 色值。CalendarPage 的短标签使用 `ExerciseDoc.category`；细粒度肌群分布才使用 `primaryMuscleGroupIds` / `secondaryMuscleGroupIds`。

### 3.3 交互

- 顶部提供“日历 / 统计”分段切换。
- 日历视图保留月份切换、返回今天、日期选择和从选中日期新建训练。
- 点击当天训练摘要进入训练详情。
- 选中日期后，下方列表顶部展示该日期生效的 `TimelineNoteDoc` 内容；编辑入口跳转到 P6 备注管理页面。
- 当前不提供部位筛选、未来计划排期、复杂备注分类筛选。

---

## 4. 统计与周期复盘视图

### 4.1 周期范围

统计视图提供 `week` / `month` / `year` 周期切换：

| 周期 | 范围 | 对比基准 | 页面形态 |
|---|---|---|---|
| week | 周一到周日 | 上一自然周 | 仪表盘 |
| month | 自然月 | 上一自然月 | 仪表盘 |
| year | 自然年 | 上一自然年 | 报告化复盘 |

所有统计归属使用 `WorkoutDoc.date`，不从 `startTime` 推导。当前自然周期未结束时，UI 必须标注“截至今日”或等价文案。首版不实现全部、自定义区间或滚动 7 天窗口。

### 4.2 训练统计口径

统计 service 读取未 tombstone 且已完成的训练；草稿不进入统计。基础 KPI：

- 训练次数。
- 总组数。
- 训练时长，首版用 `endTime - startTime`；缺少完整起止时间的训练不计入时长图表，但仍计入训练次数、组数和容量。
- 总容量，必须调用 P0/core 的统一训练容量口径。
- 周期差值，首版只覆盖训练次数、总组数和总容量。

容量曲线：

- `week`：按 7 个自然日聚合，空日期显示 0。
- `month`：按自然月每天聚合，相邻月网格日期不计入曲线。
- `year`：主线为每月日均容量，口径为当月总容量 / 当月自然天数；背景线为原始每日容量的固定 7 日滑动平均，只作视觉辅助，不参与 KPI、差值或 PR/RM。

打卡视图：

- `week`：7 天横向打卡条或小型日历条。
- `month`：自然月日历打卡图，相邻月日期只作空白占位。
- `year`：全年打卡热力图；移动端应缩小强度格并按月分行，避免 365 个日期在单列网格中形成过长列表。
- 强度统一按当天训练次数分档：0 次空白，1 次低，2 次中，3 次及以上高。

### 4.3 细粒度肌群分布

肌群分布使用 `ExerciseDoc.primaryMuscleGroupIds` / `secondaryMuscleGroupIds`：

- 排除 `isWarmup=true` 的组。
- 主目标肌群权重为 `1.0`。
- 次要目标肌群权重为 `0.5`。
- 缺失目标肌群的动作不参与肌群分布，也不从 `category` 兜底。
- 计算覆盖全部有效肌群；图表默认展示 Top 8，其余合并为“其他”，详情列表可展示完整排名。

### 4.4 身体指标只读摘要

统计页只读消费 P6 身体数据 service 的派生结果：

- `week` / `month` 展示本周期内有有效记录的全部身体指标维度的小趋势或周期变化，不固定为体重、体脂、腰围三项。
- `year` 展示年度内有有效记录的全部身体指标维度的年度变化摘要。
- 若上一自然周期没有对应身体指标记录，统计页只展示当前周期可用值，不使用箭头暗示跨周期变化。
- 统计页身体指标栏中，左右成对围度合并为同一行展示左右值；若在趋势图中展示该成对指标，则沿用 P6 的同图双序列口径。
- 身体指标不参与训练次数、组数、训练时长、容量或 PR/RM 的计算。
- 统计页不提供身体测量新增、编辑、删除或历史列表管理。

### 4.5 PR/RM 全局摘要

统计页只读消费 P7 的 `ExercisePerformanceRecordDoc` 派生摘要，页面不重新生成成绩事件。

首版展示：

- 本周期新增记录数，拆分真实 PR 数和基于 RPE 修正 RM 刷新数。
- Top 提升动作，最多 3 个，按同一动作、同一指标的相对提升比例排序：`(newValue - previousBestValue) / previousBestValue`。
- 最近刷新记录，最多 5 条，按达成时间倒序。

首次记录或无法派生旧值的记录不进入 Top 提升，只计入新增记录数和最近刷新记录。本周期没有刷新时显示空态，不用历史最佳记录填充。点击记录进入动作详情或来源训练详情。

### 4.6 备注标记

统计图表可显示 P6 的 `TimelineNoteDoc` 轻量标记，用于解释曲线变化。备注不参与训练、身体数据或成绩事件计算，不自动合并，也不写入 `WorkoutDoc.note`。

---

## 5. “我的”页拆分

当前 `/profile` 页面混合多个职责。P4.1 将它重构为入口中心：

| 入口 | 首版职责 | 持久化归属 |
|---|---|---|
| 个人资料 | nickname、avatarUrl、gender、birthDate | `ProfileDoc` |
| 应用设置 | themeId、weightUnit | `SettingsDoc` |
| 同步备份 | WebDAV 本机-only 端点和手动同步 | P3 |
| 身体数据 | 当前值、历史、趋势、新增/编辑/删除 | P6 `BodyMetricDoc` |
| 小工具 | 工具集合入口；首版包含 RM 计算器，并可列出未实现的力量训练 / 有氧训练 RPE 说明页占位 | P7，无默认设置持久化 |
| 分享/导出 | 当前仅作为入口预留；训练分享在训练详情 | P4.1，本地输出 |

个人资料编辑页不再展示或编辑身高、体重。“我的”页若展示当前身高或体重，必须读取 P6 派生的 `BodyMetricDoc` 最新非空值，不读取旧 `ProfileDoc.height` / `ProfileDoc.weight`。

首版应用设置只保存 `themeId` 和 `weightUnit`。统计默认周期、分享图默认样式、身体数据默认图表范围不新增 Settings 字段。

---

## 6. 训练详情分享图

### 6.1 范围

分享图首版只覆盖训练详情页，不覆盖身体数据、统计复盘或动作详情。输出为本地 PNG，固定宽度 1080px，高度随内容自适应。

分享图不上传服务端，不进入 manifest，不写入 WebDAV，不新增 `ShareImagePreferenceDoc` 或 Settings 子字段。

### 6.2 数据来源

分享图读取：

- `WorkoutDoc` 与 service 映射结果：日期、模板名、训练时长、总组数、容量、动作摘要、组明细、`WorkoutDoc.note`。
- `ExerciseDoc.category`：生成主要短标签，不从 `MuscleGroupId` 推导。
- P7 派生的本次训练 PR/RM 刷新摘要：只显示数量和最多 2-3 条动作指标摘要。
- 当前主题 token：生成视觉样式。

分享图默认不读取 `TimelineNoteDoc`，不读取 `BodyMetricDoc`，不显示昵称、头像、身高、体重、体脂或围度。

### 6.3 模板与交互

- 首版只有一个固定模板，不做多模板、主题包或模板编辑器。
- 默认展示训练摘要和动作摘要，不展开组明细。
- 动作摘要行显示组数、最佳组和动作总容量。
- 提供运行时“显示明细”选项；开启后同一模板纵向扩展为长图，展示所有动作和所有训练组明细，不设条数上限，不分页，也不保存为默认偏好。
- 预览页提供保存图片和系统分享两个动作，不提供复制文字兜底。
- 生成失败时 Toast 提示并保留预览或重试入口，不创建业务记录。

---

## 7. 主题语义迁移门禁

P4 已实现 themeId 链路和基础 token，但当前页面仍未完成语义迁移。P4.1 新增或重构页面必须满足：

1. 页面背景、surface、正文、次要文字、边框、焦点、危险、警告、成功、禁用状态使用 CSS 变量或共享语义 class。
2. 图表颜色使用 `--color-chart-*` 或在主题定义中新增语义图表 token。
3. category 颜色映射集中定义，输出语义角色或 CSS 变量；不得在多个页面散落 hex 或 Tailwind 色值。
4. 分享图使用当前主题 token，但不提供分享图主题选择或默认样式偏好。
5. 新页面不得新增 `blue-*`、`gray-*`，也不得扩大直接写 `emerald-*`、`slate-*` 的范围。
6. 360px 与 412px Android 宽度下，分段控件、图表、日历格、底部导航和浮动按钮不得遮挡、裁切或横向溢出。

旧页面语义迁移可以分阶段推进，但 P4.1 新增界面不能以旧硬编码方式继续扩大技术债。

---

## 8. Service 与调用链

规划调用链：

```text
CalendarPage
  -> services/calendarStats 或 services/analytics
  -> core period range / workout aggregation / muscle distribution
  -> LocalJsonRepository
  -> DocumentStore

CalendarPage 备注标记
  -> services/timelineNote.listEffectiveNotes()
  -> P6 core range overlap

统计身体指标摘要
  -> services/bodyMetrics.getPeriodMetricSummaries()
  -> P6 core sparse metric derivation

PR/RM 全局摘要
  -> services/performance.getPeriodPerformanceSummary()
  -> P7 core record derivation

WorkoutDetailPage 分享图
  -> services/shareImage.prepareWorkoutShareImage()
  -> WorkoutDoc + ExerciseDoc + P7 performance summary
  -> browser/capacitor save/share adapter
```

页面不得直接访问 repository、DocumentStore、WebDAV 或平台文件。统计 core 必须保持纯 TypeScript；浏览器 canvas、PNG 导出和系统分享适配只能在 service/platform 层。

---

## 9. 验收与回归

实现完成后至少验证：

- 日历格显示 category 部位短标签，2-3 个后折叠为 `+N`；该规则不得改变选中日期下方训练记录卡片的既有展示。
- 日历页不隐藏任何实际训练记录，不渲染未来计划条目。
- 月统计和 month 容量曲线不计入相邻月网格日期。
- `week` / `month` / `year` 共享同一统计口径；year 是报告化呈现，不持久化复盘快照。
- 训练容量调用 P0/core 统一口径，页面不重复计算。
- 肌群分布排除热身组，主/次目标肌群加权正确，缺失目标肌群不从 category 兜底。
- 身体指标摘要只读消费 P6，不提供管理入口。
- PR/RM 摘要只读消费 P7，不生成或修补成绩事件。
- 备注标记只作为解释层，不改变训练、身体数据或成绩计算。
- “我的”页拆分后，个人资料不再编辑身高体重；应用设置只保存 themeId 和 weightUnit。
- 分享图仅本地生成 PNG，不进入 manifest 或 WebDAV；默认不包含身体数据和 TimelineNote。
- P4.1 新增/重构页面通过主题语义迁移门禁和 360px/412px 窄屏走查。

本轮文档修改不运行 build/test/android sync；代码实现交付时仍按 AGENTS.md 执行项目级验证。
