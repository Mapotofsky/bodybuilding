# IronLog 概要设计文档

> 当前版本：开源单人版 Android-first 本地优先应用  
> 最后更新：2026-06-11  
> 文档用途：作为当前分支的产品范围、架构边界、模块拆分和开发验收基准。

---

## 1. 一句话目标

IronLog 是一个面向个人训练者的开源单人版训练日志应用：**Android-first、本地优先、不依赖服务器、训练数据以 JSON 文档分片保存，可通过 WebDAV 手动同步/备份**。

它不是一个在线 SaaS，不运营账号体系，不维护社区内容，不要求用户部署数据库。

---

## 2. 战略约束

### 2.1 必须满足

- Android 设备上可独立使用。
- 无网络时可以完整记录训练。
- 不需要登录、注册、JWT、用户权限。
- 不需要 FastAPI、PostgreSQL、Alembic 或任何后端进程。
- 训练数据可导出为文档型 JSON。
- WebDAV 只承担远端文件同步/备份。
- 数据模型可自然映射到未来 CloudBase/MongoDB。

### 2.2 明确不做

- 多用户账号系统。
- 云端数据库。
- 社区动态、Wiki、评论、点赞、关注。
- 公共排行榜。
- 服务端分析任务。
- WebDAV 实时查询。

### 2.3 可延后但要预留

- 数据导入/导出 UI。
- 冲突解决 UI。
- 微信小程序适配。
- 体重/围度历史。
- 更复杂的训练统计。

---

## 3. 当前代码结构

```text
ironlog/
  frontend/
    android/                  # Capacitor Android 工程
    src/
      App.tsx                 # 路由入口，无 ProtectedRoute
      pages/                  # 页面
      services/               # 页面调用面，保持旧函数名，内部转本地仓储
      core/                   # 纯 TS 类型、migration、默认数据、测试
      repositories/           # LocalJsonRepository
      platform/               # IndexedDB / Capacitor Filesystem / Preferences
      sync/                   # WebDAV client + SyncService
      types/                  # 页面兼容类型，id 已改为 string
  legacy/
    backend/                  # 旧 FastAPI/PostgreSQL 后端归档
```

运行链路：

```text
Page state
  -> services/*
  -> LocalJsonRepository
  -> DocumentStore
  -> Web: IndexedDB
  -> Android: Capacitor Filesystem
```

同步链路：

```text
Local snapshot
  -> SyncService
  -> WebDavClient
  -> remote JSON shards
  -> backups/
```

---

## 4. 模块边界

### 4.1 pages

页面负责：

- 表单状态。
- 用户交互。
- 导航。
- 调用 services。
- 展示 Toast/Confirm。

页面不得：

- 直接读写 IndexedDB。
- 直接调用 Capacitor Filesystem。
- 直接调用 WebDAV。
- 写入 JSON 分片。
- 判断同步合并规则。

### 4.2 services

services 是页面的稳定接口层。旧页面大多仍调用：

- `getWorkouts`
- `createWorkout`
- `updateWorkout`
- `copyWorkout`
- `getExercises`
- `getExerciseHistory`
- `getPlans`
- `getCalendar`
- `getTemplate`

services 负责：

- 保持页面调用面稳定。
- snake_case 页面类型与 camelCase doc 类型互转。
- 聚合本地 repository 查询。
- 计算分享数据、动作历史、列表 summary。

services 不负责：

- 持久化细节。
- WebDAV IO。
- 真实数据文件路径。

### 4.3 core

core 必须是纯 TypeScript。

允许：

- 文档类型。
- schemaVersion。
- migration。
- 默认动作库。
- 统计纯函数。
- 日历调度纯函数。

禁止依赖：

- React。
- DOM。
- Capacitor。
- IndexedDB。
- WebDAV。

### 4.4 repositories

repository 是领域数据访问层。

职责：

- CRUD。
- tombstone 删除。
- partial update 忽略 `undefined`。
- 默认过滤 `deletedAt !== null`。
- 维护 manifest shard 更新时间。

禁止：

- 把 WebDAV 当主数据源。
- 让页面直接拿内部 snapshot 修改。

### 4.5 platform

platform 适配不同运行环境。

Web 开发：

- IndexedDB 保存 snapshot。
- IndexedDB secret key 保存 WebDAV 密码。

Android：

- Capacitor Filesystem 保存 JSON。
- Capacitor Preferences 保存 secret。

### 4.6 sync

sync 负责远端文件同步。

职责：

- WebDAV 请求。
- tmp 上传。
- MOVE 发布。
- backups。
- last-write-wins merge。
- 冲突日志。

禁止：

- 在页面中直接写 WebDAV 请求。
- 把 WebDAV 当数据库分页查询。

---

## 5. 数据分片设计

逻辑文件结构：

```text
ironlog-data/
  manifest.json
  profile.json
  settings.json
  exercises.json
  templates.json
  workouts/
    index.json
    2026-06.json
```

当前实现：

- canonical workout shard 为 `workouts/index.json`。
- 同时导出 `workouts/YYYY-MM.json`，用于备份和未来迁移。

### 5.1 manifest.json

用途：

- 标记 schemaVersion。
- 标记 deviceId。
- 记录 shard 更新时间。
- 未来用于增量同步判断。

### 5.2 profile.json

保存本地个人资料，不代表账号。

### 5.3 settings.json

保存本地设置和 WebDAV passwordRef，不保存密码明文。

### 5.4 exercises.json

保存默认动作和自定义动作。

### 5.5 templates.json

保存：

- plans。
- templates。
- manual schedule entries。

### 5.6 workouts/index.json

保存 WorkoutDoc 聚合文档。

---

## 6. 文档通用字段

每个可持久化文档必须包含：

```ts
{
  id: string;
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null;
  schemaVersion: number;
}
```

规则：

- id 必须是 string。
- 不允许新增自增 number id。
- 删除必须写 tombstone。
- 默认列表必须过滤 tombstone。
- migration 必须补齐新增字段。

---

## 7. 核心业务场景

### 7.1 新建训练

入口：

- 首页开始训练。
- 日历日期创建训练。
- 今日计划按模板创建训练。

必须保证：

- date 参数生效。
- template_id 参数生效。
- 页面内模板选择器也生效。
- 模板激活后动作列表是真过滤。
- 训练中完成组后立即保存。
- restSeconds 写回上一组。

### 7.2 编辑训练

必须保证：

- 读取本地 WorkoutDoc。
- 编辑后完整替换 exercises/sets。
- 不丢失 restSeconds。
- 不把未传字段覆盖成 undefined。

### 7.3 复制训练

必须保证：

- 新 workout id。
- 新 nested exercise/set id。
- 保留 planTemplateId。
- 保留 sets 数据。
- 清空 startTime/endTime。
- 目标日期正确。

### 7.4 日历

必须保证：

- 月视图可加载 calStart~calEnd。
- 月统计只计算 monthStart~monthEnd。
- workout 完成状态来自本地 workouts。
- WebDAV 未配置不影响日历。

### 7.5 同步

必须保证：

- 未配置时状态为 unconfigured。
- 保存密码不写入 JSON。
- 手动同步失败有错误提示。
- 上传先 tmp，后 MOVE。
- 上传前写 backups。
- 冲突日志可展示。

---

## 8. 验收基线

每次较大改动后必须执行：

```bash
cd ironlog/frontend
npm run build
npm test
npm run android:sync
```

必须 grep：

```bash
rg -n "window\.(confirm|alert|prompt)" src
rg -n "blue-|gray-" src
rg -n "axios|auth|token|access_token|refresh_token|/api|services/api|Bearer" src vite.config.ts package.json
```

期望：

- 原生弹窗零残留。
- 旧主题类零残留。
- 旧 HTTP/auth/JWT 运行路径零残留。
- WebDAV Basic Authorization 如出现，必须说明不是登录/JWT。

---

## 9. 当前已知限制

- APK 命令行构建依赖 Gradle 分发包下载。
- cyclic 日历第一版为基础推导，后续可抽到 core 并补更多测试。
- WebDAV 冲突解决第一版为 last-write-wins。
- Web 开发环境的 secret 存储强度不等同 Android 原生安全存储。

---

## 10. 后续开发优先级

P0：

- 核心训练链路稳定。
- 本地数据导入导出。
- migration 测试覆盖。

P1：

- 计划/模板交互细化。
- cyclic 调度纯函数化。

P2：

- WebDAV 兼容性测试。
- 冲突 UI。

P3：

- 更多本地统计。
- 微信小程序数据适配层调研。
