# IronLog 概要设计文档

> 版本：单人版架构基线 v1
> 日期：2026-06-22
> 状态：与 `android-single-user-json-webdav` 分支代码同步
> 读者：维护者、贡献者，以及需要据此编写详细设计或实现功能的开发者

---

## 1. 文档目的与当前范围

IronLog 是一个面向个人训练者的开源训练记录应用。当前版本采用 **Android-first、本地优先、无账号、无服务器** 的运行模型：用户可以离线完成训练记录、模板编排和日历查看；可选地通过 WebDAV 同步 JSON 数据分片。

本文档是当前版本的架构边界和验收基准。详细设计文档必须从本文档的模块边界、数据模型和运行约束推导，不得把历史 FastAPI/PostgreSQL/社区规划当作当前实现目标。

### 1.1 当前必须支持的能力

| 编号 | 能力 | 当前实现状态 |
|---|---|---|
| M1 | 离线训练记录：创建、逐组记录、编辑、复制、删除、分享文本 | 已实现 |
| M2 | 内置与自定义动作库、动作历史和动作详情 | 已实现 |
| M3 | 训练计划、模板、模板动作过滤、月历和手动排程 | 已实现，cyclic 推导为基础版本 |
| M4 | JSON 文档分片、schema migration、浏览器 IndexedDB 与 Android 文件存储 | 已实现 |
| M5 | WebDAV 配置、连通性测试、手动同步、远端备份和 LWW 合并 | 已实现，存在安全与冲突策略限制 |
| M6 | Capacitor Android 工程和 WebDAV 原生 HTTP 插件 | 已实现；APK 命令行构建依赖本机 Gradle 环境 |

### 1.2 明确不在当前版本范围内

- 登录、注册、JWT、账户权限、多人共享和服务端 API。
- FastAPI、PostgreSQL、Alembic 的运行时依赖。
- 社区、Wiki、评论、点赞、排行榜、管理后台。
- PDF 体检报告、OCR、健康档案、云端分析任务。
- 自动后台同步、端到端加密、可视化冲突解决、用户可操作的数据导入/导出 UI。

历史服务端实现已归档在 `ironlog/legacy/backend/`，仅作迁移参考，不能成为新功能的运行依赖。

### 1.3 术语

| 术语 | 含义 |
|---|---|
| 文档 | 可独立持久化和同步的 JSON 对象，如 `WorkoutDoc`、`TemplateDoc`。 |
| 分片 | 一个逻辑 JSON 文件，例如 `workouts/2026-06.json`。 |
| tombstone | 通过 `deletedAt` 标记删除而不立刻物理删除的记录。 |
| manifest | 描述设备、schema 版本和分片列表的 `manifest.json`。 |
| 本地真源 | 当前设备上的本地 `DataSnapshot`；WebDAV 不承担实时数据库职责。 |
| LWW | Last Write Wins，按 `updatedAt` 选择较新文档的第一版合并策略。 |

---

## 2. 架构总览

### 2.1 运行形态

```text
React 页面
  -> services（页面稳定调用面）
  -> LocalJsonRepository（领域 CRUD）
  -> DocumentStore（平台抽象）
  -> Web：IndexedDB
  -> Android：Capacitor Filesystem / Preferences

可选同步：
LocalJsonRepository -> SyncService -> WebDavClient -> WebDAV JSON 文件
```

应用不启动 HTTP 后端，也不需要数据库服务。浏览器开发模式和 Android 包装模式共用 `frontend/src` 的业务代码；平台差异由 `platform/` 和 `sync/` 封装。

### 2.2 代码边界

```text
ironlog/
  frontend/
    android/                  # Capacitor Android 项目与原生 WebDAV 插件
    src/
      pages/                  # 路由页面和表单状态
      components/             # Layout、Toast、ConfirmDialog 和 UI 组件
      services/               # 页面使用的稳定函数接口与 snake/camel 映射
      core/                   # 纯 TypeScript 模型、默认数据、迁移、测试
      repositories/           # LocalJsonRepository
      platform/               # IndexedDB / Capacitor Filesystem / Preferences
      sync/                   # WebDAV Client、SyncService、原生桥接
  legacy/
    backend/                  # 已归档的旧 FastAPI/PostgreSQL 实现
```

边界规则：

1. 页面只能调用 `services/*`，不得直接访问 IndexedDB、Filesystem、Preferences 或 WebDAV。
2. `core/*` 必须是纯 TypeScript；不得依赖 React、DOM、Capacitor、IndexedDB 或 WebDAV。
3. `LocalJsonRepository` 负责本地 CRUD、tombstone 和 manifest 更新；`SyncService` 负责远端文件同步。
4. 页面兼容类型可以维持 `snake_case`，持久化文档统一使用 `camelCase`；映射只能放在 `services/localMappers.ts` 或对应 service 中。

---

## 3. 数据设计

### 3.1 逻辑文件布局

```text
ironlog-data/
  manifest.json
  profile.json
  settings.json
  exercises.json
  templates.json
  workouts/
    2026-06.json
    2026-07.json
```

- `profile.json`：本地个人资料，不代表在线账户。
- `settings.json`：本地偏好和 WebDAV 配置引用；同步副本不能携带密码引用与最后同步时间。
- `exercises.json`：预置动作与用户自定义动作。
- `templates.json`：计划、模板和手动排程条目。
- `workouts/YYYY-MM.json`：该月全部训练记录，包括 tombstone；这是训练数据的唯一持久化真源。
- `manifest.json`：`app`、`schemaVersion`、`deviceId`、`updatedAt` 与分片更新时间。

### 3.2 通用不变量

所有可持久化领域文档必须满足：

```ts
interface BaseDoc {
  id: string;
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null;
  schemaVersion: number;
}
```

- 新增领域 ID 一律为 string UUID，不新增自增 number ID。
- 删除必须写入 `deletedAt` 并更新 `updatedAt`；常规查询必须过滤 tombstone。
- 新字段必须经 `core/migrations.ts` 归一化，不能只在页面上提供默认值。
- `WorkoutDoc` 是聚合文档，内部嵌套动作和组；不得恢复 SQL 风格的 `workout_exercises`、`workout_sets` 持久化表。

### 3.3 核心领域关系

```text
ProfileDoc                 SettingsDoc

ExerciseDoc <--- TemplateDoc.exercises[*].exerciseId
     ^
     |                 TrainingPlanDoc 1 --- N TemplateDoc
     |                                      |
WorkoutDoc.exercises[*]                     +--- ManualScheduleEntryDoc
  └─ WorkoutSetDoc[]

WorkoutDoc.planTemplateId --- TemplateDoc.id（可为空）
```

详细字段、映射和变更规则见 P0、P1、P2 详细设计。

---

## 4. 功能模块与场景

### 4.1 M1 核心训练与动作库

入口包括首页开始训练、训练列表、日历日期和按模板开始训练。训练创建包含 `select -> training -> rest -> finish` 状态；每完成一组即写入本地仓储，休息秒数在进入下一组/换动作/结束时回写到上一组。

训练详情、编辑、复制和分享均以 `WorkoutDoc` 为数据源。复制必须产生新的训练、动作和组 ID，同时保留模板关联、动作/组数据并清空开始/结束时间。动作历史从本地训练聚合查询，不能依赖远端同步状态。

### 4.2 M2 计划、模板与日历

计划支持 `weekly`、`cyclic`、`flexible` 三种模式；模板仅保存动作顺序和文字备注，不保存预设重量、次数或组数。

模板训练有两条入口，必须行为一致：

1. `?template_id=X` URL 参数。
2. WorkoutCreatePage 内的计划/模板选择器。

激活模板后，动作列表必须使用 `.filter()` 仅渲染模板动作，不允许用排序替代过滤。`getCalendar` service 能合并自动条目、手动条目和实际训练完成状态，首页已用它展示今日计划；当前 CalendarPage 仍是“实际训练记录月历”，尚未渲染计划条目。月统计的日期范围必须是 `monthStart~monthEnd`，不能使用含相邻月日期的网格范围。

### 4.3 M3 本地存储、迁移与同步

浏览器开发时使用 IndexedDB；原生 Android 时使用 Capacitor Filesystem 保存 JSON，Preferences 保存小型平台设置。初始化时 `migrateSnapshot` 会创建默认数据并归一化文档；当前 schema 版本为 1。

WebDAV 是可选能力。未配置时所有训练、模板和日历功能仍必须离线可用。同步流程是拉取远端分片、合并、本地落盘、备份远端、临时文件上传和 `MOVE` 发布；当前冲突策略为 LWW，并以日志形式提示。

### 4.4 M4 Android 平台

Capacitor 将 Vite `dist/` 同步到 Android 项目。Android 端使用自定义 `WebDavHttp` OkHttp 插件处理 `MKCOL`、`MOVE`、`PROPFIND`，普通 `GET`、`PUT`、`DELETE` 使用 Capacitor HTTP。应用需要 `INTERNET` 权限。

---

## 5. 同步与安全边界

### 5.1 同步语义

- WebDAV URL 若未以 `ironlog-data` 结尾，客户端自动在其后追加该目录。
- 同步前拉取远端 `manifest.json` 与其中列出的分片；远端不存在 manifest 时视为首次上传。
- 上传时每个分片先写入 `.tmp-{timestamp}`，再通过 `MOVE` 覆盖正式文件。
- 上传前将远端已有分片复制到 `backups/`。当前实现没有自动清理历史备份。
- `settings.json` 的远端版本强制清空 `passwordRef` 与 `lastSyncAt`。
- 当前实现以 `updatedAt` 比较同 ID 文档；时间不同就记录一次“使用 LWW”的日志。它不是基于共同祖先的真实三方冲突检测。

### 5.2 安全限制

- 密码不写入 JSON 分片，也不会同步到 WebDAV。
- 当前 Android 实现把密码保存在 Capacitor Preferences；它是隔离的键值存储，但不是本项目实现的硬件级加密凭据库。发布版如要承诺更高安全等级，必须替换为 Android Keystore/安全存储适配器。
- WebDAV 使用 Basic Authorization。用户应优先使用 HTTPS 与权限受限的专用目录/专用账户。

---

## 6. 路由与用户界面

| 路径 | 页面 | 作用 |
|---|---|---|
| `/` | HomePage | 近期训练、月统计、今日计划与开始训练入口 |
| `/workouts` | WorkoutsPage | 按月训练列表 |
| `/workouts/new` | WorkoutCreatePage | 新建或按模板训练 |
| `/workouts/:id` | WorkoutDetailPage | 详情、编辑、复制、分享 |
| `/workouts/:id/edit` | WorkoutEditPage | 编辑训练 |
| `/plans` | PlansPage | 计划列表 |
| `/plans/:id` 等 | Plan/Template 页面 | 计划和模板管理 |
| `/calendar` | CalendarPage | 月视图与训练记录 |
| `/exercises/:id` | ExerciseDetailPage | 动作详情与历史 |
| `/profile` | ProfilePage | 本地个人资料与同步入口 |
| `/sync` | SyncPage | WebDAV 配置、测试连接和手动同步 |

全局确认必须使用 ConfirmDialog，通知使用 Toast；不得使用浏览器原生确认、提示或警告对话框。界面主色采用 emerald/slate。

---

## 7. 非功能性要求与验收

### 7.1 可用性与兼容性

- 无网络、未配置 WebDAV 时，核心训练功能可完整使用。
- Web 端开发环境与 Android 端共用数据模型和 service 调用面。
- Android APK 构建依赖 Android Studio/JDK/Gradle；构建环境不可用时应报告环境问题，而不是改写业务代码。

### 7.2 交付前验证

```powershell
cd D:\workspaces\vscodeWorkspace\project\bodybuilding\ironlog\frontend
npm run build
npm test
npm run android:sync
```

还必须检查：

```powershell
rg -n "window\.(confirm|alert|prompt)" src
rg -n "blue-|gray-" src
rg -n "axios|auth|token|access_token|refresh_token|/api|services/api|Bearer" src vite.config.ts package.json
```

第三个检索中 WebDAV Basic Authorization 是允许的例外，但必须在交付说明中明确其用途。

---

## 8. 后续扩展边界

未来可增加 CloudBase/MongoDB 数据适配器、微信小程序 UI、导入导出 UI、体重/围度、丰富统计与多人能力。扩展必须通过新的 Repository/平台适配器接入；不得把云端查询、账户逻辑或小程序 API 直接写入 `core` 或当前 React 页面。

社区、Wiki、服务端鉴权和健康报告等历史规划在重新立项前不维护详细设计，也不应出现在当前产品宣称中。
