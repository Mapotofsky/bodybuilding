# IronLog 概要设计文档

> 版本：单人版架构基线 v1（含已批准路线图）
> 日期：2026-06-23
> 状态：当前代码事实与已批准规划分层维护；规划项均明确标注为“规划中”
> 读者：维护者、贡献者，以及需要据此编写详细设计或实现功能的开发者

---

## 1. 文档目的与当前范围

IronLog 是一个面向个人训练者的开源训练记录应用。当前版本采用 **Android-first、本地优先、无账号、无服务器** 的运行模型：用户可以离线完成训练记录、模板编排和日历查看；可选地通过 WebDAV 同步 JSON 数据分片。

本文档是当前版本的架构边界和验收基准。详细设计文档必须从本文档的模块边界、数据模型和运行约束推导，不得把历史 FastAPI/PostgreSQL/社区规划当作当前实现目标。

### 1.1 当前必须支持的能力

| 编号 | 能力 | 当前实现状态 |
|---|---|---|
| M1 | 离线训练记录、训练聚合、草稿、编辑、复制、删除、历史 exerciseType 解释与个人动作统计聚合 | 已实现；个人统计以本地已完成训练派生，不持久化统计快照 |
| M2 | 计划、模板、模板动作过滤、动作详情/历史、自定义动作与动作库 UI | 已实现；动作库列表、Tab、完整详情信息与自定义动作替代删除已接入 |
| M3 | JSON 文档、本地模型、migration、Repository 与 DocumentStore | 已实现；批准计划批量提交原语规划中 |
| M4 | WebDAV 同步、远端脱敏、Android 平台与秘密隔离 | 已实现，存在安全与冲突策略限制 |
| M5 | 多套完整主题、主题设置与 UI 导航 | 已部分实现；已有 5 套主题、themeId、SettingsPage 主题选择与 token 解析，全页面语义色彩迁移仍未完成 |
| M6 | 可选 AI 工作流、候选计划、来源与安全 | 规划中；当前没有 provider、API key、AI 调用或联网 agent |

### 1.2 明确不在当前版本范围内

- 登录、注册、JWT、账户权限、多人共享和服务端 API。
- FastAPI、PostgreSQL、Alembic 的运行时依赖。
- 社区、Wiki、评论、点赞、排行榜、管理后台。
- PDF 体检报告、OCR、健康档案、云端分析任务。
- 自动后台同步、端到端加密、可视化冲突解决、用户可操作的数据导入/导出 UI。

历史服务端实现已归档在 `ironlog/legacy/backend/`，仅作迁移参考，不能成为新功能的运行依赖。

### 1.3 已批准但尚未实现的范围

- AI 是可选外部能力。API key 只进平台 secret；计划只能经候选校验、未知动作处理、预览确认和 repository 批量导入写入本地。

### 1.4 P4.1 阶段优先方向：竞品补强与界面优化

P4.1/P6/P7 已部分落地：训练日历已扩展为日历/统计模块，“我的”页已拆分入口，身体数据、时间段备注、小工具、RM 计算、RPE 说明、PR/RM 成绩事件和训练详情 PNG 分享图已经接入当前代码。后续仍需持续核对统计细节、移动端人工验收、全页面语义色彩迁移和相关文档同步。该阶段仍保持 Android-first、本地优先、无账号、无服务器的运行模型；WebDAV 多人共享只作为远期模型预留。

P5 AI 智能体与安全后置到 P4.1 阶段需求落地并重新核对数据边界之后再推进。本文不展开 P4.1 的字段、公式、页面细节或验收矩阵，具体范围以 P4.1 需求文档及后续受影响的详细设计修订为准。

### 1.5 术语

| 术语 | 含义 |
|---|---|
| 文档 | 可独立持久化和同步的 JSON 对象，如 `WorkoutDoc`、`TemplateDoc`。 |
| 分片 | 一个逻辑 JSON 文件，例如 `workouts/2026-06.json`。 |
| tombstone | 通过 `deletedAt` 标记删除而不立刻物理删除的记录。 |
| manifest | 描述设备、schema 版本和分片列表的 `manifest.json`。 |
| 本地真源 | 当前设备上的本地 `DataSnapshot`；WebDAV 不承担实时数据库职责。 |
| LWW | Last Write Wins，按 `updatedAt` 选择较新文档的第一版合并策略。 |

---

## 2. 架构总览

### 2.1 运行形态

```text
React 页面
  -> services（页面稳定调用面）
  -> LocalJsonRepository（领域 CRUD）
  -> DocumentStore（平台抽象）
  -> Web：IndexedDB
  -> Android：Capacitor Filesystem / Preferences

可选同步：
LocalJsonRepository -> SyncService -> WebDavClient -> WebDAV JSON 文件
```

应用不启动 HTTP 后端，也不需要数据库服务。浏览器开发模式和 Android 包装模式共用 `frontend/src` 的业务代码；平台差异由 `platform/` 和 `sync/` 封装。

### 2.2 代码边界

```text
ironlog/
  frontend/
    android/                  # Capacitor Android 项目与原生 WebDAV 插件
    src/
      pages/                  # 路由页面和表单状态
      components/             # Layout、Toast、ConfirmDialog 和 UI 组件
      services/               # 页面使用的稳定函数接口与 snake/camel 映射
      core/                   # 纯 TypeScript 模型、默认数据、迁移、测试
      repositories/           # LocalJsonRepository
      platform/               # IndexedDB / Capacitor Filesystem / Preferences
      sync/                   # WebDAV Client、SyncService、原生桥接
  legacy/
    backend/                  # 已归档的旧 FastAPI/PostgreSQL 实现
```

边界规则：

1. 页面只能调用 `services/*`，不得直接访问 IndexedDB、Filesystem、Preferences 或 WebDAV。
2. `core/*` 必须是纯 TypeScript；不得依赖 React、DOM、Capacitor、IndexedDB 或 WebDAV。
3. `LocalJsonRepository` 负责本地 CRUD、tombstone 和 manifest 更新；`SyncService` 负责远端文件同步。
4. 页面兼容类型可以维持 `snake_case`，持久化文档统一使用 `camelCase`；映射只能放在 `services/localMappers.ts` 或对应 service 中。

---

### 2.3 详细设计权威归属

| 文档 | 唯一权威职责 | 必要交叉仅限 |
|---|---|---|
| P0 | WorkoutDoc 聚合、逐组记录、个人动作统计聚合与历史 exerciseType 兼容性 | 依赖 P1 动作引用解析。 |
| P1 | 计划、模板、动作库 UI、详情信息呈现、动作引用、删除与重定向业务规则 | 依赖 P0 历史快照/统计与 P2 持久化。 |
| P2 | 本地模型、migration、Repository/DocumentStore 与批量提交原语 | 为 P3 提供序列化边界，为 P5 提供提交原语。 |
| P3 | WebDAV、Android、远端脱敏、同步与秘密隔离 | 读取 P2 设置/分片，不定义 AI 工作流。 |
| P4 | 主题 token、导航与移动端视觉验收 | 依赖 P2 themeId 持久化、P1 动作库入口。 |
| P4.1 | 竞品补强与界面优化阶段需求，定义下一阶段范围、优先级和影响面 | 不替代既有详细设计；具体字段和验收需在对应详细设计中修订。 |
| P5 | AI 工作流、候选 JSON、来源、安全、预览与导入规则 | 调用 P2 批量提交原语，不重定义存储或同步规则。 |

## 3. 数据设计

### 3.1 逻辑文件布局

```text
ironlog-data/
  manifest.json
  profile.json
  settings.json
  exercises.json
  templates.json
  body-metrics.json
  timeline-notes.json
  workouts/
    2026-06.json
    2026-07.json
  exercise-performance/
    2026-07.json
  assets/
    avatar/
      profile-local.txt
```

- `profile.json`：本地个人资料，不代表在线账户。
- `settings.json`：可同步本地偏好；不得包含 WebDAV URL、用户名、密码引用或密码明文。
- `exercises.json`：预置动作与用户自定义动作。
- `templates.json`：计划和模板条目。
- `body-metrics.json`：身体指标稀疏测量记录。
- `timeline-notes.json`：日期/时间段背景备注。
- `workouts/YYYY-MM.json`：该月全部训练记录，包括 tombstone；这是训练数据的唯一持久化真源。
- `exercise-performance/YYYY-MM.json`：真实 PR 与基于 RPE 修正 RM 的刷新事件。
- `assets/avatar/*`：头像资源文件。`profile.json` 只保存资源路径引用，资源文件随 manifest、tmp/MOVE 和 backup 参与 WebDAV 往返。
- `manifest.json`：`app`、`schemaVersion`、`deviceId`、`updatedAt` 与分片更新时间。

### 3.2 通用不变量

所有可持久化领域文档必须满足：

```ts
interface BaseDoc {
  id: string;
  createdAt: string;
  updatedAt: string;
  deletedAt: string | null;
  schemaVersion: number;
}
```

- 新增领域 ID 一律为 string，不新增自增 number ID；新建用户记录通常使用 UUID，内置数据和本地单例可使用稳定 string ID。
- 删除必须写入 `deletedAt` 并更新 `updatedAt`；常规查询必须过滤 tombstone。
- 新字段通常必须经 `core/migrations.ts` 归一化，不能只在页面上提供默认值；当前动作详情元数据为开发/虚拟机测试阶段的明确例外，不兼容快照按 P2 第 3.5 节清空后重建，不能冒充兼容 migration 或用于真实用户发布。
- `WorkoutDoc` 是聚合文档，内部嵌套动作和组；不得恢复 SQL 风格的 `workout_exercises`、`workout_sets` 持久化表。

### 3.3 核心领域关系

```text
ProfileDoc                 SettingsDoc

ExerciseDoc <--- TemplateDoc.exercises[*].exerciseId
     ^
     |                 TrainingPlanDoc 1 --- N TemplateDoc
     |                                      |
WorkoutDoc.exercises[*]
  └─ WorkoutSetDoc[]

WorkoutDoc.planTemplateId --- TemplateDoc.id（可为空）
```

详细字段、映射和变更规则见 P0、P1、P2 详细设计。

---

### 3.4 设置、同步端点与秘密

WebDAV URL、用户名、密码引用和密码明文都是本机-only 同步端点配置，不属于 `SettingsDoc` 的可同步业务偏好，不得进入 `settings.json`、manifest、JSON 分片或远端 backup。密码明文只能进入平台 secret；清除同步配置只删除本机端点配置和对应 secret，不删除训练、动作、模板、资料或头像资源。

`SettingsDoc.themeId` 已存在并随 `settings.json` 同步。AI 字段目前不存在；实现时 AI 非敏感配置进入 SettingsDoc 并经显式 migration 演进，apiKeyRef 等秘密引用只指向平台 secret，且不得进入远端 settings.json、backups 或日志。具体字段、版本、序列化和批量导入事务由 P2 定义。

## 4. 功能模块与场景

### 4.1 M1 核心训练与历史解释

入口包括首页开始训练、训练列表、日历日期和按模板开始训练。未结束的训练（`endTime=null`）会在进入创建页时先显示草稿恢复选择：继续时恢复原记录，选择新建时以草稿最后一次 `updatedAt` 结束原记录。训练创建包含 `select -> training -> rest -> finish` 状态；每完成一组即写入本地仓储，休息秒数在进入下一组/换动作/结束时回写到上一组。训练动作保存 `exerciseType` 快照，分别支持负重训练（重量/次数）、心肺训练（距离/时长）、自重训练（次数）和静力训练（时长）。

训练详情、编辑、复制和分享均以 `WorkoutDoc` 为数据源。复制必须产生新的训练、动作和组 ID，同时保留模板关联、动作/组数据并清空开始/结束时间。动作历史从本地训练聚合查询，不能依赖远端同步状态。个人动作统计同样只从有效且已完成的本地训练聚合；指标口径、单位和 `exerciseType` 快照解释由 P0 定义，不写回为另一份统计真源。

### 4.2 M2 计划、模板、动作库与日历

计划支持 `weekly`、`cyclic`、`flexible` 三种模式；模板仅保存动作顺序和文字备注，不保存预设重量、次数或组数。

模板训练有两条入口，必须行为一致：

1. `?template_id=X` URL 参数。
2. WorkoutCreatePage 内的计划/模板选择器。

激活模板后，动作列表必须使用 `.filter()` 仅渲染模板动作，不允许用排序替代过滤。`getCalendar` service 能合并自动排程条目和实际训练完成状态，HomePage 已用它展示今日计划和补训建议；flexible 模式不生成待训练提醒。CalendarPage 已扩展为“日历 / 统计”模块，日历视图仍只查询并渲染实际训练记录，尚未渲染计划条目。月统计的日期范围必须是 `monthStart~monthEnd`，不能使用含相邻月日期的网格范围。

动作库的完整详情已实现：目标肌群和现有 `description` 中的纯文本动作要领是随 `ExerciseDoc` 保存、同步的离线元数据，不能从当前 `category` 猜测；个人统计是 M1 从训练月分片派生的数据。内置动作由默认数据提供受控内容；自定义动作创建/编辑复用同一组件和 service 字段契约，动作库、模板和训练入口不得维护分叉表单。详情不依赖远端 API、外链视频、训练热量估算规划或 AI，缺少元数据时必须明确显示“暂无”，而不是编造训练结论。

### 4.3 M3 本地存储与迁移

浏览器开发时使用 IndexedDB；原生 Android 时使用 Capacitor Filesystem 保存 JSON，Preferences 保存小型平台设置。初始化时 `migrateSnapshot` 会创建默认数据并归一化文档；当前 schema 版本为 2。身体数据、时间段备注和动作成绩事件已进入本地 JSON 文档、migration、DocumentStore 与 WebDAV 往返。

WebDAV 是可选能力。未配置时所有训练、模板和日历功能仍必须离线可用。同步流程是拉取远端分片、合并、本地落盘、备份远端、临时文件上传和 `MOVE` 发布；当前冲突策略为 LWW，并以日志形式提示。

### 4.4 M4 WebDAV 同步与 Android 平台

Capacitor 将 Vite `dist/` 同步到 Android 项目。Android 端使用自定义 `WebDavHttp` OkHttp 插件处理 `MKCOL`、`MOVE`、`PROPFIND`，普通 `GET`、`PUT`、`DELETE` 使用 Capacitor HTTP。应用需要 `INTERNET` 权限。

---

### 4.5 M5 主题与 UI 导航

主题必须是完整视觉语义方案，而非只替换主色。当前已有 5 套主题、稳定 themeId、SettingsPage 主题选择、未知主题回退和基础 token 解析；主题选择不依赖账号。动作库 Tab 已作为默认主导航入口存在。全页面语义色彩迁移仍未完成，旧页面仍可能保留 emerald/slate Tailwind 工具类；详细 token、主题 ID 与导航验收见 P4。

### 4.6 M6 可选 AI 与安全（规划中）

AI 不改变本地数据真源，WebDAV 不是 AI 后端。计划建议只输出严格 schema 的候选 JSON；未知动作决议和用户预览确认后，调用 importApprovedPlan() 在 repository 副本中一次批量写入，保存失败不得改变正式 snapshot。联网回答需要来源 URL、可得时发布日期、访问时间和不确定性；AI 不得诊断疾病、替代医生或给出处方。详细边界见 P5。

## 5. 同步与安全边界

### 5.1 同步语义

- WebDAV URL 若未以 `ironlog-data` 结尾，客户端自动在其后追加该目录。
- 同步前拉取远端 `manifest.json` 与其中列出的分片；远端不存在 manifest 时视为首次上传。
- 上传时每个分片先写入 `.tmp-{timestamp}`，再通过 `MOVE` 覆盖正式文件。
- 上传前将远端已有分片复制到 `backups/`。当前实现没有自动清理历史备份。
- `settings.json` 不包含 WebDAV URL、用户名、密码引用或密码明文；本机-only 端点配置不参与远端合并。
- 头像资源文件和训练分片一样由 manifest 记录，上传前备份远端旧文件，发布时先写临时文件再 `MOVE` 覆盖正式路径。
- 当前实现以 `updatedAt` 比较同 ID 文档；时间不同就记录一次“使用 LWW”的日志。它不是基于共同祖先的真实三方冲突检测。

### 5.2 安全限制

- 密码不写入 JSON 分片，也不会同步到 WebDAV。
- 当前 Android 实现把密码保存在 Capacitor Preferences；它是隔离的键值存储，但不是本项目实现的硬件级加密凭据库。发布版如要承诺更高安全等级，必须替换为 Android Keystore/安全存储适配器。
- WebDAV 使用 Basic Authorization。用户应优先使用 HTTPS 与权限受限的专用目录/专用账户。

---

## 6. 路由与用户界面

| 路径 | 页面 | 作用 |
|---|---|---|
| `/` | HomePage | 近期训练、月统计、今日计划与开始训练入口 |
| `/workouts` | WorkoutsPage | 按月训练列表 |
| `/workouts/new` | WorkoutCreatePage | 新建或按模板训练 |
| `/workouts/:id` | WorkoutDetailPage | 详情、编辑、复制、分享 |
| `/workouts/:id/edit` | WorkoutEditPage | 编辑训练 |
| `/plans` | PlansPage | 计划列表 |
| `/plans/:id` 等 | Plan/Template 页面 | 计划和模板管理 |
| `/calendar` | CalendarPage | 日历/统计模块；日历视图不渲染计划条目 |
| `/exercises` | ExerciseLibraryPage | 动作库浏览、搜索、分类筛选与自定义动作入口 |
| `/exercises/:id` | ExerciseDetailPage | 动作详情与历史 |
| `/profile` | ProfilePage | “我的”入口中心与当前资料摘要 |
| `/profile/details` | PersonalProfilePage | 本地个人资料编辑 |
| `/settings` | SettingsPage | 主题与重量单位设置 |
| `/body-metrics` | BodyMetricsPage | 身体数据当前值、历史、趋势和新增/编辑 |
| `/timeline-notes` | TimelineNotesPage | 日期/时间段背景备注管理 |
| `/tools` | ToolsPage | 小工具入口 |
| `/tools/rm` | RmCalculatorPage | RM 计算器 |
| `/tools/rpe-strength` | StrengthRpeGuidePage | 力量训练 RPE 说明 |
| `/tools/rpe-cardio` | CardioRpeGuidePage | 有氧训练 RPE 与心率分区说明 |
| `/sync` | SyncPage | WebDAV 配置、测试连接和手动同步 |

全局确认必须使用 ConfirmDialog，通知使用 Toast；不得使用浏览器原生确认、提示或警告对话框。新建或重构页面应优先使用语义 class 与 CSS 变量，避免扩大硬编码 Tailwind 色彩。

---

### 6.1 路由与导航状态

- /exercises 与“动作库”Tab：已实现，用于浏览内置和自定义动作并进入现有详情路由。
- 主题选择入口：已实现，位于 `/settings`，不增加账号依赖。
- AI 配置与 AI 工作区：规划中，未配置时明确不可用，不伪造 provider 或命令。

## 7. 非功能性要求与验收

### 7.1 可用性与兼容性

- 无网络、未配置 WebDAV 时，核心训练功能可完整使用。
- Web 端开发环境与 Android 端共用数据模型和 service 调用面。
- Android APK 构建依赖 Android Studio/JDK/Gradle；构建环境不可用时应报告环境问题，而不是改写业务代码。
- 主题实现必须验证 5 套主题、未知主题回退、文本/焦点/危险状态可读性和 Android 窄屏布局。
- AI 未配置、未同意、网络失败、限流或来源不可核验时，必须无损降级为离线应用。

### 7.2 交付前验证

```powershell
cd D:\workspaces\vscodeWorkspace\project\bodybuilding\ironlog\frontend
npm run build
npm test
npm run android:sync
```

还必须检查：

```powershell
rg -n "window\.(confirm|alert|prompt)" src
rg -n "blue-|gray-" src
rg -n "axios|auth|token|access_token|refresh_token|/api|services/api|Bearer" src vite.config.ts package.json
```

第三个检索中 WebDAV Basic Authorization 是允许的例外，但必须在交付说明中明确其用途。

---

## 8. 后续扩展边界

未来可增加 CloudBase/MongoDB 数据适配器、微信小程序 UI、导入导出 UI、多人能力和更完整的统计/分享增强。扩展必须通过新的 Repository/平台适配器接入；不得把云端查询、账户逻辑或小程序 API 直接写入 `core` 或当前 React 页面。

社区、Wiki、服务端鉴权和健康报告等历史规划在重新立项前不维护详细设计，也不应出现在当前产品宣称中。

已批准路线图顺序：P0-P3 的动作库、详情、统计、JSON 分片和 WebDAV 往返已作为当前实现基线；P4/P4.1/P6/P7 已部分落地并需继续同步修订受影响的详细设计；P5 AI 配置、候选计划批量导入和联网 agent 在当前本地数据边界重新核对后再推进。
