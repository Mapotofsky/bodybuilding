# 详细设计文档 P3：基础统计

> 当前版本：本地聚合统计，不依赖后端分析服务  
> 最后更新：2026-06-11  
> 文档用途：定义训练统计口径，避免列表、首页、日历、详情页出现不一致。

---

## 1. 统计原则

统计只从本地 `WorkoutDoc[]` 计算。

默认过滤：

```ts
workout.deletedAt === null
```

禁止：

- 从 UI 文案反推统计。
- 使用日历补齐日期作为月份统计口径。
- 把 WebDAV 远端作为统计查询源。
- 新增服务端统计任务。

---

## 2. 当前统计入口

首页：

- 最近训练。
- 本月训练次数。
- 本月容量。
- 最近容量趋势。
- 今日计划和补训提示。

训练列表：

- 当前月份训练次数。
- 当前月份总组数。
- 当前月份总容量。

日历：

- 本月训练次数。
- 每日训练条目点位。

训练详情：

- 动作数。
- 总组数。
- 总容量。
- 训练时长。
- 每个动作容量。

动作详情：

- 累计使用组数。
- 最近使用日期。
- 历史 set 列表。

---

## 3. WorkoutSummary 口径

```ts
interface WorkoutSummary {
  id: string;
  date: string;
  start_time: string | null;
  end_time: string | null;
  note: string | null;
  mood: number | null;
  exercise_count: number;
  total_sets: number;
  total_volume: number;
  plan_template_id: string | null;
  template_name: string | null;
  template_color: string | null;
  plan_color: string | null;
  exercise_ids: string[];
  created_at: string;
}
```

计算：

```text
exercise_count = workout.exercises.length
total_sets = sum(exercise.sets.length)
total_volume = sum((set.weight || 0) * (set.reps || 0))
exercise_ids = workout.exercises.map(exercise.exerciseId)
```

注意：

- null weight/reps 视为 0。
- durationSec/distanceM 不计入力量容量。
- restSeconds 不计入容量，但在详情和历史中展示。

---

## 4. 月份统计口径

### 4.1 训练列表

`WorkoutsPage` 使用：

```ts
getWorkouts({ month: "YYYY-MM" })
```

要求：

- 只返回该月份 date 前缀匹配的 workout。
- tombstone 不返回。

### 4.2 日历页

日历为了显示完整周，会加载：

```text
calStart~calEnd
```

但本月统计必须再过滤：

```ts
w.date >= monthStartStr && w.date <= monthEndStr
```

禁止：

- 直接使用 `workouts.length` 作为本月数量。

---

## 5. 首页统计口径

首页请求：

```ts
getWorkouts().then(data => data.slice(0, 10))
getWorkouts({ month: todayMonth })
getCalendar(todayStr, todayStr)
```

本月训练：

```text
thisMonthCount = monthWorkouts.length
```

本月容量：

```text
thisMonthVolume = sum(monthWorkouts.total_volume)
```

容量趋势：

```text
recentWorkouts.reverse().map({ date: date.slice(5), vol })
```

注意：

- 趋势只代表最近记录，不等于完整时间序列。
- label 必须来自 payload.date，不使用 Recharts 默认 index。

---

## 6. 动作详情统计

动作累计使用：

```text
usage_count = sum(set count where workoutExercise.exerciseId === targetExerciseId)
```

最近使用：

```text
last_used_date = max(workout.date where target exercise appears)
```

动作历史：

排序：

```text
date desc, setNumber asc
```

字段：

- date。
- set_number。
- weight。
- reps。
- unit。
- rest_seconds。

---

## 7. 训练详情统计

训练时长：

```text
duration = round((endTime - startTime) / 60000)
```

展示条件：

- startTime 和 endTime 都存在。
- duration > 0。

动作容量：

```text
exerciseVolume = sum(set.weight * set.reps)
```

总容量：

```text
totalVolume = sum(exerciseVolume)
```

---

## 8. 未来 core 统计函数

建议新增：

```text
src/core/stats.ts
```

函数：

```ts
summarizeWorkout(workout: WorkoutDoc): WorkoutSummaryLike
summarizeMonth(workouts: WorkoutDoc[], month: string): MonthSummary
summarizeExercise(workouts: WorkoutDoc[], exerciseId: string): ExerciseStats
buildVolumeTrend(workouts: WorkoutDoc[], limit: number): TrendPoint[]
```

要求：

- 输入纯数据。
- 输出纯对象。
- 不依赖 React/DOM/Capacitor。
- 必须有单元测试。

---

## 9. 边界 case

- workout 无 exercises：统计为 0。
- exercise 无 sets：该动作存在但 set 数为 0。
- set weight 为 null：容量不增加。
- set reps 为 null：容量不增加。
- tombstone workout：所有统计排除。
- 复制训练：计入目标日期。
- 编辑训练：统计重新计算。
- 跨月日历格：不计入本月统计。

---

## 10. 开发验收清单

- [ ] 首页本月次数正确。
- [ ] 首页本月容量正确。
- [ ] 日历本月统计不包含补齐日期。
- [ ] 训练详情总容量与列表一致。
- [ ] 动作详情累计组数正确。
- [ ] 删除训练后统计减少。
- [ ] 复制训练后目标月份统计增加。
- [ ] Recharts tooltip label 显示日期而不是 index。
